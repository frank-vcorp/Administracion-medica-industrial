# QA-VERDICT — ARCH-20260820-01 Fase 5 (Snapshot versionado / congelación histórica)

- **ID tarea:** `ARCH-20260820-01` Fase 5 — Snapshot versionado (congelación histórica)
- **ID intervención SOFIA:** `IMPL-20260820-06`
- **ID QA:** QA-20260820-06
- **Auditor:** GEMINI (sesión independiente)
- **SPEC activa:** `context/SPECs/SPEC_ARCH-20260820-01-CALIBRACION-FUENTE-UNICA.md` v1.1 (§10 Snapshot/versionado, §14 Fase 5, §5.5 hashes, §15 reglas 7, §16 CB-08/CB-18, §19 CA-G09/CA-G10)
- **ADR:** `context/decisions/ADR-20260820-01-CALIBRACION-FUENTE-UNICA.md` v1.1 (§2.8 Snapshot versionado, §4.2 trade-offs, §6 decisión 3 y 7)
- **Handoff consumido:** `context/interconsultas/HANDOFF_ARCH-20260820-01_FASE5_SOFIA_CALIBRACION-FUENTE-UNICA.md`
- **IMPL-REPORT consumido:** `context/interconsultas/IMPL-REPORT_ARCH-20260820-01_FASE5.md`
- **State previo:** Fases 1–4 verificadas localmente; Fases 3/4/5 sin commit sobre HEAD `0cce88f`. CURRENT.md confirma Fase 5 `READY_FOR_VERIFYING`, GEMINI obligatorio por cambio de schema.
- **Alcance de auditoría:** **Reforzada** (obligatoria por la matriz de GEMINI §4: cambio de schema/migración, contrato público de dos modelos, consumidores backend+frontend; diff material). Hand-off §11 también la declara obligatoria.

---

## 1. Delimitación y fuentes

Incremento evaluado — diff NO committeado sobre `HEAD = 0cce88f`. Aislado por:

| Archivo | Cambio | Verificado |
|---|---|---|
| `backend/app/services/ai/snapshot_versioning.py` | **NUEVO**. Helper `sha256_prefixed` + `build_snapshot_versioning_payload`. | ✓ |
| `backend/tests/test_snapshot_versioning_fase5.py` | **NUEVO**. 12 tests Fase 5. | ✓ (12/12 PASS) |
| `backend/app/main.py` | Import helper; 3 callers (`v2_upload_and_analyze`, `v2_prediagnosis_from_params`, `v2_event_test_upload_xml_audiometry`) propagan hashes + schema + versionId/Number en `extraction_snapshot.audit` y `prediagnosis_snapshot.audit`. Resolución V3 vía `medical_test_id` en proceso. | ✓ |
| `frontend/prisma/schema.prisma` | 8 columnas nuevas (4 por modelo), todas nullable aditivas. | ✓ (diff estrictamente aditivo verificado) |
| `frontend/prisma/migrations/20260820200000_add_snapshot_versioning/migration.sql` | **NUEVO**. Solo `ADD COLUMN ... NULL`. | ✓ (validado por inspección + `prisma validate` + self-diff) |
| `frontend/src/actions/calibration-v3.actions.ts` | Helper `getPublishedVersionForSnapshot` + `extractSnapshotVersioningFromBackendAudit` + `_sha256Prefixed` (Node `crypto`). | ✓ |
| `frontend/src/actions/event-test.actions.ts` | 3 sitios de creación de snapshots (`persistXmlDirectSnapshots`, `persistCalibrationDisabledSnapshot`, callers de upload/regenerate) propagan los nuevos campos + `calibrationSource`/`calibrationVersionId`. | ✓ |
| `frontend/src/actions/ai-prediagnosis.actions.ts` | 2 sitios (`triggerStudyAIAnalysis`, `triggerStructuredStudyAIPrediagnosis`) propagan los nuevos campos. | ✓ |
| `frontend/src/app/events/[id]/_lib/event-page-data.ts` | Serializa los nuevos campos + flag `calibration_version_mismatch`. | ✓ |
| `frontend/src/components/clinical/ClinicalExtractionRenderer.tsx` | Props `frozenPresentationSchema` y `calibrationVersionMismatch`; `resolvePresentationSchema` prioriza frozen > vigente > legacy. | ✓ |
| `frontend/src/components/clinical/PapeletaWorkspace.tsx` | Pasa `frozenPresentationSchema` y `calibration_version_mismatch` desde el snapshot del data loader. | ✓ |
| `frontend/src/components/clinical/__tests__/ClinicalExtractionRenderer.fase5.test.ts` | **NUEVO**. 8 tests Fase 5 (SSR puro con `renderToStaticMarkup`). | ✓ (8/8 PASS) |

**Archivos NO modificados (contratos protegidos, handoff §5):**
- `backend/app/services/ai/calibration_resolver.py` — verificado `git diff HEAD` → vacío. **Cero touch.**
- `backend/app/services/ai/extractor.py`, `_call_dr7_medical_chat`, `DoctorStudyReview`, `EventTest`, dictamen/aptitud/PDF — no tocados.
- `frontend/src/components/calibration/CalibrationWorkspaceClient.tsx`, `frontend/src/lib/study-ai.ts` (no tocados por Fase 5; modificados por Fase 3 verificada en `main`).
- `arch-20260819-02-tarjetas-muestra` — rama independiente sin mezcla (`git branch -a` muestra rama viva; `grep tarjetas-muestra` en frontend/src = 0 coincidencias).

---

## 2. Matriz de auditoría — trazabilidad AC ↔ implementación ↔ evidencia

| AC / CB | Criterio | Implementación (archivo:línea) | Evidencia | Resultado |
|---|---|---|---|---|
| **AC-5.1** | Corrida nueva persiste `calibrationVersionId` + `presentationSchemaSnapshot` + hashes en ambos snapshots | `backend/app/main.py:1333, 1387-1393, 1422-1426, 1862, 1834-1838`; frontend `event-test.actions.ts:467-481, 502-528`, `ai-prediagnosis.actions.ts:300-316, 484-525` | `test_extraction_prompt_hash_is_sha256_of_prompt` PASS; `test_clinical_hashes_populated_only_when_clinical_criteria_present` PASS; `test_version_identifiers_are_populated` PASS; `test_presentation_schema_snapshot_is_included_when_enabled` PASS; `test_AC_5_1_prioriza_hash_backend` PASS; `test_AC_5_1_fallback_sha256_local` PASS | **PASS** |
| **AC-5.2** | Snapshot pre-V5 (`null` en columnas) se renderiza con flag `calibration_version_mismatch=true` sin romperse | `event-page-data.ts:271-291, 305-309` calcula `calibration_version_mismatch`; `PapeletaWorkspace.tsx:1300-1313` propaga; `ClinicalExtractionRenderer.tsx:512-518` `resolvePresentationSchema` cae a `presentationSchema` o `getStudySchema()` legacy | `test_AC_5_2_snapshot_pre_V5_no_rompe_render` PASS; `test_CB_08_pre_V5_fallback_sin_throw` PASS | **PASS** |
| **AC-5.3** | Tras re-publicar, histórico post-V5 se renderiza idéntico (frozen gana) | `ClinicalExtractionRenderer.tsx:512-518` prioridad `frozenPresentationSchema` > `presentationSchema`; backend `snapshot_versioning.py:99-103` congela el schema post-fusión efectiva | `test_AC_5_3_dos_renders_iguales_con_frozen` PASS; `test_AC_5_3_negativo_frozen_distinto` PASS | **PASS** |
| **CB-08** | Pre-V5 + flag `calibration_version_mismatch=true` en audit | `event-page-data.ts:284, 308` añade `calibration_version_mismatch: !hasFrozen*`; `ClinicalExtractionRenderer.tsx:546-547` deja la prop trazada (no rompe) | test frontend CB-08 PASS; cobertura en `_mismatch` boolean | **PASS** |
| **CB-18** | Cambio posterior en `FamilyTemplate` no re-renderiza históricos (congela versión efectiva fusionada) | `snapshot_versioning.py:99-103` extrae `presentation.schema` del `AICalibrationVersionResolved` ya fusionado por el resolver en `_resolve_v3`/`_merge_v3_with_family` | Cobertura implícita por composición (resolver + helper backend + helper frontend). **Limitación documentada:** no hay test E2E específico para edición de `FamilyTemplate`. Consistente con Fase 5 scope. | **PASS_WITH_WARNINGS** (P3 — limitación documentada) |
| **F-3 (QA-20260820-05)** | Integración `POST /api/v2/studies/upload-and-analyze` con `medical_test_id` → resolver → snapshot congelado | `backend/app/main.py:949-1033` (form param `medical_test_id` + `get_default_resolver().resolve(test_row, "published")`) → `:1310-1314, 1333` propagan a `calibration_version` + `build_snapshot_versioning_payload` → audit → frontend server action → columnas Prisma | Cadena probada por composición: 12 tests helper backend × resolver Fase 1 (43/43) × 8 tests frontend Fase 5. Limitación documentada en IMPL-REPORT §6.3 | **PASS_WITH_WARNINGS** (P3 — cobertura proporcional) |
| **`document_extraction`** | `clinicalCriteria=null` ⇒ solo capa extractiva persiste | `snapshot_versioning.py:111-121` solo popula hashes clínicos si `clinicalCriteria` no es None | `test_clinical_hashes_are_none_for_document_extraction` PASS | **PASS** |
| **`operationMode=manual_service`** | Resolver devuelve `None` → snapshot pre-V5 legible con `calibration_version_mismatch=true` | `build_snapshot_versioning_payload(None)` retorna todos nulls; `event-page-data.ts:284` setea `mismatch=true` | Cubierto por `test_returns_all_null_when_calibration_version_is_none` PASS + camino del renderer | **PASS** |
| **H11 canal muerto** | `medical_calibration` no se pasa activo | `prediagnostic.py:880, 1011, 1116` pasa `None` en todos los callers activos; `main.py:1526` lo pasa como kwarg DEPRECADO con marcador `shim` | `test_AC_4_3b_medical_calibration_no_aparece_en_main_prediagnosis_callers` (AST multi-línea, F-1 fix) PASS; `grep -c medical_calibration= backend/app/main.py backend/app/services/ai/prediagnostic.py` = 6 sitios, todos marcados deprecados o `None` | **PASS** |

---

## 3. Validaciones independientes (re-ejecutadas)

| Validación | Comando | Entorno | Exit / Resultado |
|---|---|---|---|
| Prisma schema válido | `cd frontend && DATABASE_URL=postgresql://test:test@127.0.0.1:5432/test npx prisma validate` | local | exit 0 — `The schema at prisma/schema.prisma is valid` |
| Self-diff (drift) | `npx prisma migrate diff --from-schema-datamodel prisma/schema.prisma --to-schema-datamodel <copy>` | local | `-- This is an empty migration.` (sin drift accidental) |
| Migration additividad | Inspección de `migration.sql` (12 líneas) | local | ✓ Solo `ADD COLUMN ... NULL` sobre `study_extraction_snapshots` y `ai_prediagnosis_snapshots`. Cero `DROP`/`NOT NULL`/`ALTER TYPE`/`UPDATE`/`DELETE`. |
| Migration no aplicada | `ls frontend/prisma/migrations/20260820200000_add_snapshot_versioning/` + revisión `git status` | local | ✓ Sólo `migration.sql`; sin `migration_lock.toml`/`_prisma_migrations`. Ningún marker de apply. `git status` confirma árbol de trabajo sin commit. `npx prisma migrate status` falla por DB offline (P1001) — esperado, sin evidencia de apply. |
| Tests Fase 5 backend | `cd backend && python3 -m pytest tests/test_snapshot_versioning_fase5.py -v` | Python 3.14 | **12 passed** en 0.31s |
| Tests Fase 5 frontend | `cd frontend && npx vitest run src/components/clinical/__tests__/ClinicalExtractionRenderer.fase5.test.ts` | Node 20 + Vite 2.1.9 | **8 passed** en 41ms |
| Tests Fase 4 (no regresión) | `cd backend && python3 -m pytest tests/test_ai_pipeline.py -k TestPrediagnosisFase4ARCH20260820_01 -q` | Python 3.14 | **10 passed** (incluye `test_AC_4_3b_medical_calibration_no_aparece_en_main_prediagnosis_callers` AST multi-línea) |
| Tests Resolver Fase 1 (no regresión) | `cd backend && python3 -m pytest tests/test_calibration_resolver.py -q` | Python 3.14 | **43 passed** |
| Tests Fase 3 (no regresión) | `cd frontend && npx vitest run src/actions/__tests__/calibration-fase3.actions.test.ts src/actions/__tests__/calibration-fase3.event-test.test.ts` | Node 20 | **21 passed** (18 + 3) |
| TypeScript check | `cd frontend && npx tsc --noEmit -p tsconfig.json` | tsc | exit 0 — **0 errores** |
| Vitest suite completa | `cd frontend && npx vitest run` | Node 20 | **655 passed / 15 failed** — los 15 en `medical-exam.actions.test.ts` (no tocado por Fase 5; `git diff HEAD --name-only -- src/actions/__tests__/medical-exam*` = vacío). Baseline idéntica a IMPL-REPORT §4. |
| Pytest `test_ai_pipeline.py` completo | `cd backend && python3 -m pytest tests/test_ai_pipeline.py -q` | Python 3.14 | **31 failed / 83 passed** — idéntico a baseline IMPL-REPORT §4 / QA-20260820-05 §4. Inspección de un fallo individual (`test_m3_fallback_inmediato_si_api_key_ausente`) confirma `ExtractionAuthError(M3_CREDENTIALS_UNAVAILABLE)`: preexistente en la capa extractiva M3 sin key. No regresión. |
| Resolver no tocado | `git diff HEAD -- backend/app/services/ai/calibration_resolver.py` | git | vacío (verificado) |
| No mezcla tarjetas-muestra | `git branch -a` + `grep -r tarjetas-muestra frontend/src` | git + grep | ✓ rama `arch-20260819-02-tarjetas-muestra` independiente, 0 coincidencias en `frontend/src` |

---

## 4. Hallazgos priorizados

### P0 / P1 — Críticos / Altos
*(Ninguno detectado.)*

### P2 — Medios

*(Ninguno detectado.)*

### P3 — Bajos

#### W-1 — P3 Baja: Limitación documentada de cobertura E2E para CB-18

**Evidencia:** SPEC §16 CB-18 exige "cambio posterior en una `FamilyTemplate` (añadir analito) → los snapshots históricos no se re-renderizan porque congelaron la versión efectiva fusionada". IMPL-REPORT §5 admite explícitamente: *"Sin test E2E específico para este CB (consistente con scope Fase 5)"*. La cobertura es por composición: `build_snapshot_versioning_payload` extrae `presentation.schema` del `AICalibrationVersionResolved` ya fusionado por `_resolve_v3`/`_merge_v3_with_family` en `calibration_resolver.py:417-...`. El registry de `FamilyTemplate` está documentado como vacío en Fase 1/2 (stub en `calibration-v3.actions.ts:485-488`).

**Impacto:** riesgo bajo — la lógica de congelación es directa (`payload["presentationSchemaSnapshot"] = schema_block` en `snapshot_versioning.py:103`), y el test backend `test_presentation_schema_snapshot_is_included_when_enabled` (PASS) verifica que el builder extrae el schema ya fusionado. Sin embargo, sin un test E2E que cree una `FamilyTemplate`, modifique su `defaults` y re-renderice un snapshot pre-cambio, no hay verificación dinámica del CB-18.

**Reproducción:** imposible sin un E2E completo que excede scope Fase 5.

**Owner recomendado:** INTEGRA (decide si E2E de CB-18 pertenece a Fase 5 o a Fase 6/7 con un registry de `FamilyTemplate` poblado).

**Condición de cierre:** documentar en `context/SYSTEM.md` (F-4 P3 de Fase 4) o diferir con OK explícito de Frank.

#### W-2 — P3 Baja: Cobertura F-3 proporcional (sin test E2E HTTP+Prisma+FastAPI+seed)

**Evidencia:** QA-20260820-05 F-3 P3 listó la falta de test de integración `POST /api/v2/studies/upload-and-analyze` con `medical_test_id` → resolver → snapshot congelado. Fase 5 lo cierra **por composición**: 12 tests unitarios backend × 43 tests Fase 1 resolver × 8 tests frontend. IMPL-REPORT §6.3 documenta la limitación como no inventada.

**Impacto:** riesgo bajo — la cadena es mecanizada y cada eslabón está probado; un E2E real con BD seed+mock resolver sería desproporcionado.

**Owner recomendado:** INTEGRA (consistente con F-3 de Fase 4).

**Condición de cierre:** Aceptación explícita del dueño (ya consta en `CURRENT.md`).

#### W-3 — P3 Baja: Migración física requiere autorización explícita (operativo, no defecto)

**Evidencia:** `frontend/prisma/migrations/20260820200000_add_snapshot_versioning/migration.sql` existe pero **NO está aplicada**. `npx prisma migrate status` falla por DB offline (P1001). `CURRENT.md` y `IMPL-REPORT §7` lo documentan. Cumple HANDOFF §4 STOP.

**Impacto:** cero mientras no se aplique. Una vez aplicada, es estrictamente aditiva (`ADD COLUMN ... NULL`) y reversible con `ALTER TABLE ... DROP COLUMN` sin pérdida de filas.

**Owner:** Frank (vía ATLAS). Aplicar a staging/producción requiere:
1. `pg_dump` previo (responsabilidad operativa, no de SOFIA).
2. OK explícito de Frank (AGENTS §11).

**Condición de cierre:** autorización humana separada — **NO bloquea QA, bloquea el siguiente paso operativo**.

---

## 5. Riesgo operativo

### Preparación por entorno

| Entorno | Estado | Notas |
|---|---|---|
| **Calidad** | **LISTO** | typecheck 0 errores, vitest 655/670 (15 baseline), pytest 83/114 (31 baseline M3_CREDENTIALS_UNAVAILABLE), 12 nuevos backend + 8 nuevos frontend PASS. |
| **Staging** | **NO_LISTO** | Migración física no aplicada; depende de autorización explícita de Frank + `pg_dump` previo. |
| **Producción** | **NO_LISTO** | Mismo requisito + AGENTS §11 (autorización humana explícita en conversación vigente). |

### Bloqueador operativo separado

- **Aplicar `npx prisma migrate deploy` en staging/prod** requiere OK explícito de Frank. GEMINI **no ejecuta, no recomienda ejecución** — sólo documenta el procedimiento y el STOP. Cualquier intento de hacerlo sin OK humano es **violación de AGENTS §11**.

### Riesgos no bloqueantes identificados

- `prisma.JsonNull` para `presentationSchemaSnapshot` es usado explícitamente en `event-test.actions.ts:471-473, 489-490` y `ai-prediagnosis.actions.ts:307-309, 489-491`. Correcto (columna `Json?`).
- `_sha256Prefixed` en frontend usa `node:crypto` síncrono (`calibration-v3.actions.ts:838-841`). Consistente con Node 20 server actions; comentado para evitar `require` async.
- El frontend `getPublishedVersionForSnapshot` **no** normaliza V1/V2 (a diferencia del resolver backend que sí lo hace). Sin embargo, el resolver también devuelve `versionId=None, versionNumber=None` para V1/V2 adaptados (verificado `calibration_resolver.py:401-402`). Por tanto **ambas rutas son consistentes** en V1/V2 → nulls + flag mismatch.

---

## 6. Autoauditoría (anti-código)

- [x] Delimité el incremento exacto (5 archivos modificados Fase 5 + 4 archivos NUEVOS + 1 migration.sql). No expandí scope.
- [x] Verifiqué SPEC/ADR vigentes (v1.1) y handoff.
- [x] Revisé evidencia independiente (no sólo IMPL-REPORT): ejecuté los 4 gates, typecheck, suite focal y full.
- [x] No edité código, tests, migraciones, config, `discovery/`, SPEC ni `PROYECTO.md`.
- [x] No imprimí secretos/PII (snapshot_versioning.py sólo hashea prompts del contrato público, no keys ni secretos).
- [x] Cada finding tiene evidencia (línea/archivo) + impacto + condición de cierre.
- [x] Separé severidad QA (P3) de niveles L1/L2/L3 de reparación.
- [x] Separé QA / staging / producción (sección 5).
- [x] No invoqué subagentes (DEBY/CRONISTA/SOFIA) ni declaré `DONE`.
- [x] Handoff vuelve a ATLAS con acción concreta (sección 7).

---

## 7. Handoff a ATLAS

### Veredicto

**`PASS_WITH_WARNINGS`** — la implementación cumple AC-5.1, AC-5.2, AC-5.3, CB-08, H11, contracto aditivo Prisma, gates backend (12 nuevos + 43 Fase 1 + 10 Fase 4) y gates frontend (8 nuevos + 21 Fase 3 + 0 typecheck errors), sin regresión sobre baselines conocidas (15 vitest preexistentes en `medical-exam.actions.test.ts`, 31 pytest preexistentes `M3_CREDENTIALS_UNAVAILABLE`). Quedan tres W-P3 documentados (CB-18 E2E, F-3 E2E HTTP+Prisma, autorización de apply a staging/prod). El contrato público de dos modelos cambia (8 columnas nullable aditivas), pero la reversibilidad es total (rollback = revertir commit + `ALTER TABLE ... DROP COLUMN` no destructivo).

### Acción

ATLAS debe:
1. **Aceptar el veredicto QA-20260820-06** (P3 aceptados documentados — coherentes con CURRENT.md y F-3/F-4 de Fase 4 ya aceptados).
2. **Decidir sobre commit/push Fase 5** (autorización explícita separada de Frank — Fase 5 no commitea por sí sola; `git status` confirma árbol con Fases 3/4/5 sin commit).
3. **Solicitar autorización de Frank para aplicar la migración a staging** (procedimiento no ejecutado por GEMINI; AGENTS §11 + handoff Fase 5 §4 STOP).
4. **No delegar fixes a SOFIA** — no hay hallazgos P1/P2 que requieran corrección. Los P3 son aceptaciones del dueño, no defectos.

### Gate siguiente

- Si Frank autoriza commit/push + apply migración a staging → INTEGRA puede consolidar commit conjunto o separado (CRONISTA aplica la transición) y luego ATLAS solicita verificación operativa (QA staging + smoke E2E post-apply con un snapshot real `calibration_version_mismatch=true`).
- Si Frank difiere el apply → Fase 5 queda `READY_FOR_VERIFYING` + estado QA aprobado localmente, sin avance a staging hasta nueva OK.

---

**Estado QA:** `PASS_WITH_WARNINGS` (3 P3 aceptables).
**Estado SOFIA:** `READY_FOR_VERIFYING` (sin cambios).
**Estado CRONISTA:** no aplica hasta nueva instrucción de INTEGRA/Frank.
