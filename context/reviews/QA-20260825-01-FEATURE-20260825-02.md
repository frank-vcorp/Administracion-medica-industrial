# QA-20260825-01 — FEATURE-20260825-02 — Entregable validado de Audiometría

- **QA-VERDICT:** `BLOCKED` (V3 no ejecutable: sin entorno/sesión/DB; V1/V2 PASS)
- **ID tarea:** FEATURE-20260825-02
- **Auditoría:** GEMINI — gate final V3 (una sola auditoría por incremento)
- **Fecha:** 2026-08-25
- **SPEC:** `context/SPECs/SPEC-FEATURE-20260825-02-AUDIOMETRIA-ENTREGABLE.md` (READY_FOR_SOFIA)
- **ADR:** `context/decisions/ADR-20260825-01-AUDIOMETRIA-ENTREGABLE-COMUN.md` (ACCEPTED)
- **Incremento auditado:** 3 archivos modificados + 12 archivos nuevos (ver delimitación). El worktree contiene cambios históricos no relacionados; se auditaron sólo los archivos del incremento.
- **Alcance de auditoría:** Refuerzada (ruta nueva autenticada, contrato `DoctorStudyReview`/`EventTest.clinicalContext`, PDF con PII clínica, dispatch por tipo de estudio y riesgo de regresión Espirometría).

## 1. Delimitación y fuentes

**Diff auditado (del IMPL-REPORT, coincidente con el worktree):**

Modificados:
- `frontend/src/actions/ai-prediagnosis.actions.ts` (dispatch `studyType === 'Audiometria'` en `submitDoctorStudyReview`)
- `frontend/src/components/clinical/StudyAIPrediagnosisPanel.tsx` (prop `studyType`, enrutado del botón de descarga)
- `frontend/src/components/clinical/PapeletaWorkspace.tsx` (sección/cuestionario/panel Audiometría + propagación `clinical_context` + paso de `studyType`)

Nuevos:
- `frontend/src/schemas/clinical/audiometria-questionnaire.schema.ts`
- `frontend/src/lib/clinical/audiometria-questionnaire-validate.ts`
- `frontend/src/actions/audiometria-questionnaire.actions.ts`
- `frontend/src/components/clinical/AudiometriaQuestionnaireModal.tsx`
- `frontend/src/components/clinical/AudiometriaQuestionnaireSummary.tsx`
- `frontend/src/components/clinical/AudiometriaClinicalCriteriaPanel.tsx`
- `frontend/src/components/pdf/AudiometriaValidatedPDF.tsx`
- `frontend/src/lib/audiometry-pdf.tsx`
- `frontend/src/app/api/pdf/audiometry/[reviewId]/route.tsx`
- 4 archivos de test V1 (`audiometria-questionnaire.schema.test.ts`, `audiometria-questionnaire.actions.test.ts`, `AudiometriaQuestionnaireModal.test.ts`, `AudiometriaClinicalCriteriaPanel.test.ts`)

**Fuera del diff auditado (worktree histórico, no evaluado como parte del incremento):**
- `frontend/src/app/events/[id]/_lib/event-page-data.ts` y `frontend/src/services/medical-event.service.ts` presentan cambios **no confirmados** etiquetados `IMPL-FEATURE-20260825-01` (PDF de Espirometría, incremento previo). Son una **dependencia runtime** de este incremento (pueblan `existingReview.pdfGenerated`/`pdfErrorMessage` que consume el botón de descarga), pero no forman parte de FEATURE-20260825-02. Ver §5 hallazgo R1.
- Otros `M`/`D`/`??` en `PROYECTO.md`, `discovery/*`, `context/*`, `backend/__pycache__`, `context/datos AMI/**` son históricos y quedan fuera de alcance (instrucción explícita: no exigir limpieza global).

**Fuentes canónicas contrastadas:** SPEC §4–§7, ADR §Reglas 1–8, `PROYECTO.md` (payload real de Audiometría: `oido_derecho.va`/`vo`/`pta_visible`/`campos_fuente`), `frontend/src/components/clinical/extraction-presentation-schemas.ts` (schema `Audiometria`), `frontend/src/lib/study-ai.ts` (`getCanonicalAIStudyType` → `'Audiometria'`/`'Espirometria'`), `backend/app/services/audiometry_xml_parser.py` (`parse_audiometry_xml`).

## 2. Trazabilidad AC → implementación → evidencia → resultado

| AC | Implementación clave | Evidencia | Resultado |
|---|---|---|---|
| AC-1 Cuestionario en EventTest y recuperación | `saveAudiometriaQuestionnaire` (update atómico `clinicalContext`) + `revalidatePath`; recuperación en `event-page-data.ts:346` (`clinicalContext: et.clinicalContext ?? null`) | Test actions `persiste atómicamente cuando el EventTest pertenece al evento` (PASS); código leído | PASS |
| AC-2 Renderer bilateral TA/VO sin inventar frecuencias | `AudiometriaClinicalCriteriaPanel` tabla itera sólo `frecuenciasDetectadas`; `calcularPTA3` devuelve `null` si falta un componente; `readOido` lee `va`/`vo` presentes | Test `AC-2: NO inventa frecuencias ausentes`; `calcularPTA3` (PASS) | PASS |
| AC-3 Cobertura parcial con 4 frecuencias | `resolveAudiometriaCriteria` advertencia si `frecuenciasDetectadas.length === 4`; sin relleno de 3000/6000/8000 | Test `AC-3: conserva cobertura parcial con 4 frecuencias` (PASS) | PASS |
| AC-4 Extracción separa valores de tabla vs narrativa | Panel lee `oido_derecho.va`/`oido_izquierdo.va` (umbrales) y `campos_fuente.faringe/cad/cai/mtd/mti`; no existe campo que copie `diagnostico_ami`/`recomendacion_ami` | Lectura de `readOido` + `extraction-presentation-schemas.ts` (`campos_fuente`) | PASS |
| AC-5 Interpretación patrón + PTA/AMI y huecos no concluyentes | `calcularPTA3` (PTA3 completo), `clasificarPatron` (graves/agudas), criterio AMI ≤25 dB, `NO_CONCLUYENTE` ante huecos | Tests `criterio AMI NORMAL/ALTERADO`, `marca NO_CONCLUYENTE cuando faltan umbrales` (PASS) | PASS |
| AC-6 Revisión aceptar/editar/rechazar | `StudyAIPrediagnosisPanel` (3 botones preexistente, sin cambios en contrato) | Código + `submitDoctorStudyReview` (no cambia schema Prisma) | PASS |
| AC-7 Sólo aceptación/edicción permite descarga | Ruta rechaza `REVIEWED_REJECTED` → 404; botón sólo para `ACCEPTED|EDITED` | Lectura `route.tsx` + `StudyAIPrediagnosisPanel` | PASS (código) / E2E pendiente |
| AC-8 PDF con datos/evidencia/interpretación/médico/cédula/firma | `AudiometriaValidatedPDF` secciones I–VI + TA/VO + PTA3+fuente + identidad congelada + firma/cédula/membrete | Lectura del componente PDF | PASS (código) / E2E pendiente |
| AC-9 Descarga sin acceso cruzado (IDOR) | Ruta `getServerSession` + rol `REVIEWER_ROLES` + scope por objeto `reviewedByUserId === session.user.id` salvo SUPERADMIN | Lectura `route.tsx` (mismo patrón que Espirometría, QA-20260825-01 P2-C) | PASS (código) / E2E pendiente |
| AC-10 XML conserva parser directo | `parse_audiometry_xml` (`backend/app/services/audiometry_xml_parser.py:24`) y endpoint `/api/v2/event-tests/upload-xml-audiometry` intactos; sin archivos backend en el diff del incremento | `git diff` (0 archivos backend del incremento); lectura del parser | PASS |

**Nota terminológica AC-7:** la SPEC §4.4 dice "Sólo una revisión aceptada genera el PDF" y AC-7 "Sólo aceptación válida permite descargar"; la implementación trata `REVIEWED_EDITED` como válida para generar/descargar el PDF firmado (igual que Espirometría FEATURE-20260825-01). Es la interpretación consolidada del sistema (aceptada + editada = firmada; rechazada = sin PDF). Ver hallazgo F-1.

## 3. Validaciones independientes

| # | Comando | Entorno | Exit | Resultado |
|---|---|---|---|---|
| V1 | `npx vitest run <4 test files audiometría>` | `frontend/` | 0 | **37/37 PASS** (14 schema + 7 actions + 5 modal + 11 panel) |
| V2-regresión | `npx vitest run <espirometry test files>` | `frontend/` | 0 | **156/156 PASS** (EsprClinicalCriteriaPanel 123 + espirometry-pdf 14 + ai-prediagnosis.espirometry-pdf 10 + clinical-context 9) — confirma no regresión Espirometría |
| Typecheck | `npx tsc --noEmit` | `frontend/` | 1 | **Sólo 1 error residual y preexistente**: `EspirometriaClinicalCriteriaPanel.test.ts(1545,74)` regex flag `d` requiere ES2018+. Archivo ajeno al incremento; coincide con lo declarado por SOFIA. Los archivos del incremento compilan limpios |
| V3 Playwright | N/A — **bloqueado** | — | — | Ver §4 |

**Confirmación de evidencia no repetida:** la suite completa V2 (1053 PASS / 15 FAIL preexistentes en `medical-exam.actions.test.ts`) declarada por SOFIA **no** se re-ejecutó completa (regla §11 SOL-20260823-01 §4: no repetir evidencia sin delta); en su lugar se verificó el subconjunto de regresión Espirometría (156/156) y los 37 tests focales del incremento, que son los que el delta podría romper.

## 4. Bloqueo V3 (evidencia exacta)

V3 exige Playwright real (SPEC §6; IDL §12): recorrer cuestionario → panel clínico → revisión → PDF → permisos/consola/requests sobre un Event de prueba desechable, con el mismo build y destino a aprobar. **No ejecutable en esta sesión** porque no existe el entorno:

1. **Sin base de datos:** `pg_isready` → exit 127 (cliente no instalado); ningún listener en `:5432`. El datasource Prisma es `postgresql` (`env("DATABASE_URL")`).
2. **Sin servidor:** ningún listener en `:3000` (frontend) ni `:8000` (backend FastAPI).
3. **Sin sesión/autorización:** `frontend/.env.local` sólo contiene `VERCEL_OIDC_TOKEN` (sin `DATABASE_URL` ni `NEXTAUTH_SECRET` para el entorno de desarrollo; `.env.production.local` los tiene pero sólo aplica a `next start` y apunta a un host externo inalcanzable desde esta máquina).
4. **Sin credenciales de prueba:** no hay test user (`TEST_USER_EMAIL`/`TEST_USER_PASSWORD` sólo en `.env.example.e2e`), sin seed de Event desechable de Audiometría con documento fuente y snapshot.

Conforme a IDL §12 ("Sin entorno ejecutable, el incremento permanece `VERIFYING`") y §21.10 DoD ("sin entorno, estado `VERIFYING`"), el gate V3 queda **no ejecutado** y el incremento permanece en verificación a nivel de cierre DoD, aunque V1/V2 y la revisión de código no arrojan defectos bloqueantes.

## 5. Hallazgos priorizados

Sin P0/P1/P2 bloqueantes en el diff del incremento.

- **F-1 (P3, contrato/terminología):** SPEC §4.4/AC-7 ("sólo aceptación válida") vs. implementación que habilita PDF también para `REVIEWED_EDITED`. Es consistente con el patrón validado de Espirometría y clínicamente correcto (aceptar/editado = firmado). **Owner:** ATLAS. **Cierre:** confirmar en SPEC que "aceptada" incluye "editada" (o corregir redacción §4.4) para cerrar el gap terminológico.

- **F-2 (P3, alcance explícito del autor, sin bloqueo):** el `clinical_context` de Audiometría se persiste en `EventTest.clinicalContext` (AC-1) pero **no** se reenvía al backend IA: `triggerStudyAIAnalysis` usa `extractAndValidateClinicalContext` que sólo valida el `schemaVersion` de Espirometría. SOFIA lo declaró intencional (SPEC no obliga al backend a consumirlo; el cuestionario queda disponible para la UI). **Owner:** ATLAS (backlog). **Cierre:** decidir en fase posterior si el backend consume el contexto auditivo; no bloquea V3.

- **F-3 (P3, entorno de build):** `tsc --noEmit` falla globalmente por 1 error preexistente (`EspirometriaClinicalCriteriaPanel.test.ts:1545`, flag `d`). Ajeno al incremento y confirmado como único error. **Owner:** SOFIA/ATLAS (higiene, opcional). **Cierre:** corregir `target` a ES2018+ en el tsconfig de tests, o anotar como deuda; no relacionado con este incremento.

- **R1 (observación de acoplamiento, no defecto):** el botón de descarga de `StudyAIPrediagnosisPanel` lee `existingReview.pdfGenerated`/`pdfErrorMessage`, que hoy pueblan los cambios **no confirmados** `IMPL-FEATURE-20260825-01` en `event-page-data.ts` y `medical-event.service.ts`. Si al commitear este incremento no se incluyen esos archivos previos, el UI de estado del PDF perdería su cableado. **Owner:** ATLAS. **Cierre:** verificar al empacar el PR que los archivos de `IMPL-FEATURE-20260825-01` viajen conjuntamente (pertenecen al incremento previo de Espirometría).

## 6. Riesgo operativo

- **Datos/PII:** el PDF contiene identidad clínica del trabajador (nombre, ID, empresa) y firma del médico. La URL persistida (`uploads/audiometry-pdfs/<reviewId>.pdf`) **no** es accesible por estática pública (`REPO_UPLOAD_DIR` = `<repo>/uploads`, fuera de `public/`) y sólo se sirve por la ruta autenticada con scope por objeto. Sin fuga detectada.
- **Privacidad/logs:** los `console.error`/`console.warn` del incremento no imprimen payloads clínicos (sólo `err` genérico y path/mensajes de filesystem). Sin `console.log` de objetos completos.
- **XSS:** sin `dangerouslySetInnerHTML` en los archivos nuevos; inputs controlados; PDF por `@react-pdf/renderer` (no HTML). Validación Zod server-side obligatoria en el server action.
- **Concurrencia/atomicidad:** `saveAudiometriaQuestionnaire` es un único `prisma.eventTest.update` (reemplazo del snapshot) con defensa previa de pertenencia evento↔eventTest.
- **Reversión:** rollback = revertir 3 archivos + eliminar 12 nuevos; sin migración Prisma (`clinicalContext` ya existía y es nullable). Camino documentado en IMPL-REPORT y correcto.
- **No publicación de calibración V3:** el incremento no toca `backend` ni módulos de calibración (0 archivos backend en el diff); la calibración de Audiometría permanece en `tested`. Confirmado.

## 7. Preparación por entorno

- **Calidad (unit/V1/V2):** `LISTO` — 37/37 focales + 156/156 regresión Espirometría + typecheck limpio en archivos del incremento.
- **Staging (V3 Playwright):** `NO_EVALUADO` — sin servidor, DB ni sesión para levantar el build a aprobar.
- **Producción:** `NO_EVALUADO` — sin gate V3 y sin autorización de despliegue (prohibido por defecto; sin OK de Frank).

## 8. Handoff a ATLAS

- **Acción:** proveer un entorno ejecutable para cerrar V3 (PostgreSQL + `next dev` con `NEXTAUTH_SECRET`/`DATABASE_URL` + usuario médico de prueba + Event desechable de Audiometría con documento fuente y snapshot) y reactivar sesión independiente de GEMINI para el recorrido Playwright (cuestionario → guardar → editar → panel clínico con TA/VO/PTA/fuente → revisión aceptar/editar/rechazar → descarga autenticada → prueba de IDOR con usuario distinto → consola/requests sin fallos).
- **Gate siguiente:** sin V3, el incremento permanece `VERIFYING` (DoD observable). Con V3 PASS, ATLAS puede decidir cierre; los hallazgos F-1/F-2/F-3 y R1 son P3/observaciones sin carácter bloqueante y pueden aceptarse explícitamente.
- **No aplica** enrutar a SOFIA/DEBY: no hay defectos reproducibles ni gaps de contrato bloqueantes (F-1 es terminológico y lo resuelve ATLAS en SPEC).

## 9. Autoauditoría

Delimitación exacta declarada (§1); SPEC/ADR vigentes verificados (§Delimitación); evidencia independiente ejecutada (vitest + tsc, no sólo el reporte de SOFIA); no se editó código/tests/config/SPEC/discovery/PROYECTO; sin secretos ni PII en este reporte; cada hallazgo con evidencia/impacto/owner/cierre; severidad QA (P0–P3) separada de L1/L2/L3; QA/staging/producción separados (§7); sin invocación de subagentes ni `DONE`; handoff devuelto a ATLAS con acción concreta.