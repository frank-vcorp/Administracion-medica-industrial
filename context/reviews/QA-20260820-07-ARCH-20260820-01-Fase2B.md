# QA-VERDICT — ARCH-20260820-01 Fase 2B (Cableado visible UI V3 en Calibración)

- **ID tarea:** `ARCH-20260820-01-FASE2B` — UI V3 en `/admin/services/[id]/calibration`
- **ID intervención SOFIA:** `IMPL-20260820-01`
- **ID QA:** QA-20260820-07
- **Auditor:** GEMINI (sesión independiente, anti-código)
- **SPEC activa:** `context/SPECs/SPEC_ARCH-20260820-01-CALIBRACION-FUENTE-UNICA.md` v1.1 (§5.0/§5.1 operationMode+V3, §8 gates G0-G9, §9.2 editor condicional, §14 Fase 2, §15 reglas 10-11, §16 CB-13/CB-14/CB-15)
- **ADR:** `context/decisions/ADR-20260820-01-CALIBRACION-FUENTE-UNICA.md` v1.1 (§2.11 operationMode, §2.12 familyTemplate, §8 gates)
- **Decisión funcional confirmada:** `discovery/DECISIONS.md` → `DEC-20260820-03` (publicación V3 visible desde Calibración, 2026-08-20)
- **Decisiones referenciadas:** DEC-20260820-01 (fuente única), DEC-20260820-02 (operationMode), BR-20260820-01 (paridad Calibración↔Events)
- **Handoff consumido:** `context/interconsultas/HANDOFF_ARCH-20260820-01_FASE2B_SOFIA_EDITOR-V3.md`
- **IMPL-REPORT consumido:** `context/interconsultas/IMPL_REPORT_ARCH-20260820-01_FASE2B.md`
- **HEAD base:** `ca0b9f8` (fix Vercel build, `calibration-v3-shared.ts`).
- **State previo:** Fases 1-5 verificadas en QA-20260820-02 al -06. CURRENT.md re-asignado a Fase 2B (no a Fase 5). Fase 2B no commiteada.
- **Alcance de auditoría:** **Reforzada** (obligatoria por matriz GEMINI §4: contrato visible de props de dos componentes, nuevo componente, RBAC UI visibles, ruta crítica `/admin/services/[id]/calibration` que fue el último FIX Vercel).

---

## 1. Delimitación y fuentes

Incremento evaluado — diff NO committeado sobre `HEAD = ca0b9f8`. Aislado por:

| Archivo | Cambio | Verificado |
|---|---|---|
| `frontend/src/app/admin/services/[id]/calibration/page.tsx` | **MODIF**. Parsea `operationMode` (válido/null), parsea raíz V3 (`schemaVersion==='V3'`), deriva `canEdit`/`canPublish` server-side vía `getServerSession` + `isAdminLike`/`isSuperAdmin`. Pasa `aiCalibrationV3`, `operationMode`, `canEdit`, `canPublish` al workspace. Header añade badges `operationMode` + `draft: …` + `vN·label` (sólo si V3 existe); conserva badge V2 legacy sólo si `!aiCalibrationV3`. | ✓ |
| `frontend/src/components/calibration/CalibrationWorkspaceClient.tsx` | **MODIF**. Props añade `aiCalibrationV3`, `operationMode`, `canEdit`, `canPublish`. Importe `useRouter`; `v3State = describeCalibrationV3State(...)`. Tab "Configuración": badge V2 legacy sólo si `v3State.isLegacyOnly`; **`CalibrationV3StatusPanel` siempre**; `AICalibrationEditor` **no** se monta cuando `v3State.isManualService`. `initial` del editor prefiere `coerceV3DraftToEditorInitial(aiCalibrationV3.draft)` y cae a `initialRawCalibration` (V1/V2). `onSaved`/`onChanged` → `router.refresh()`. | ✓ |
| `frontend/src/components/calibration/AICalibrationEditor.tsx` | **MODIF**. Props añaden `initialStatus?: "draft" \| "tested"` y `onSaved?: (status) => void`. `buildDraftV3FromEditorState` input añade `status` (default `"draft"`); `return.draft.status = status ?? "draft"`. Estado de selección de status (radio) renderizado. `onSaved?.(saveStatus)` tras guardar OK. Hooks `useState` adicionales **insertados después** de los hooks existentes y antes del early return de `manual_service` (no rompen `react-hooks/rules-of-hooks`). | ✓ |
| `frontend/src/lib/calibration-v3-ui.ts` | **NUEVO**. Helper puro, sin `'use server'`, sin `node:crypto` (cumple FIX-20260820-01-VERCEL-BUILD). Exporta: `isOperationModeValue`, `describeCalibrationV3State`, `coerceV3DraftToEditorInitial`, `getPublishGateVisibility`, `mapPublishErrorCode`, `CalibrationV3UIState`, `PublishGateVisibility`, `PublishErrorMap`. | ✓ |
| `frontend/src/components/calibration/CalibrationV3StatusPanel.tsx` | **NUEVO**. Renderiza badge `operationMode`, badge legacy V1/V2, badge `snapshot legacy congelado`, grid 3 cards (draft/published/superseded), `PublishSection` con botón `Publicar` (sólo `canPublish`), leyenda SUPERADMIN si no, lista de gates G0-G9 con resaltado del fallido. Gate UI: `!canEdit` → aviso `calibration-v3-status-panel-no-permission`. `isManualService` → aviso `calibration-v3-status-panel-manual-service` (sin publicar, sin editor). | ✓ |
| `frontend/src/components/calibration/__tests__/calibration-v3-ui.test.ts` | **NUEVO**. 24 tests en `.test.ts` (entorno node, vitest). Cubre AC-2B.2, AC-2B.3, AC-2B.7, AC-2B.8, CB-2B-1, CB-2B-2. | ✓ (24/24 PASS) |
| `PROYECTO.md` | **MODIF** (entrada FASE2B en `READY`). | ✓ |
| `context/CURRENT.md` | **MODIF** (reasignado a DEC-20260820-03 / FASE 2B). | ✓ |

**Archivos NO modificados (contratos protegidos, handoff §2 verificado):**

- `frontend/src/actions/calibration-v3.actions.ts` — verificado `git diff HEAD` → vacío. Cero touch.
- `frontend/src/lib/calibration-v3-shared.ts` — verificado `git diff HEAD` → vacío. Cero touch.
- `frontend/src/types/calibration.ts` — verificado `git diff HEAD` → vacío. Cero touch.
- `backend/**`, `frontend/prisma/schema.prisma`, `frontend/prisma/migrations/**` — sin cambios.
- `frontend/src/app/events/**`, `frontend/src/actions/event-test.actions.ts`, `frontend/src/actions/ai-prediagnosis.actions.ts` — sin cambios.
- Rama `arch-20260819-02-tarjetas-muestra` — rama viva independiente, no mezclada.

**Tag de no-default Audiometría:** `frontend/src/components/calibration/CalibrationWorkspaceClient.tsx:500` (default `?? "Audiometria"` reportado en H3) **no eliminado** en este diff, pero tampoco se introduce un nuevo default silencioso. `getEditorSectionsForOperationMode(operationMode)` y `coerceV3DraftToEditorInitial(draft)` retornan `canonicalStudyType: ""` cuando no hay draft (no se asume). El default legacy sólo aplica a código sin tocar; este PR no lo replica ni lo introduce. **No-regresión parcial** (defecto histórico cubierto por la rama `0cce88f` ya verificada en QA-20260820-03 con F-2.2; ver §4 W-1).

---

## 2. Matriz de auditoría — trazabilidad AC ↔ implementación ↔ evidencia

| AC / CB | Criterio | Implementación (archivo:línea) | Evidencia | Resultado |
|---|---|---|---|---|
| **AC-2B.1** | `page.tsx` parsea `operationMode` (válido/null) y raíz V3; deriva `canEdit`/`canPublish` server-side | `page.tsx:48-50` (Promise.all con `getServerSession`); `:82-85` `isOperationModeValue`; `:88-95` cast controlado a `AICalibrationV3`; `:99-100` `isAdminLike`/`isSuperAdmin`; `:157-186` badges header; `:235-239` props al workspace | `npm run typecheck` 0 errores; `next build` SUCCESS ruta `/admin/services/[id]/calibration` listada como `ƒ (Dynamic)` | **PASS** |
| **AC-2B.2** | `describeCalibrationV3State` cubre `isManualService`, `currentPublishedVersion`, `supersededCount` | `calibration-v3-ui.ts:87-127`; `resolveCurrentPublishedVersion` (70-80) usa la **misma regla** que `getPublishedCalibrationForEventTest` (preferencia por `currentPublishedVersionId` → fallback primera `published\|disabled`) | 9 tests en `describe("describeCalibrationV3State")` PASS; 2 tests en `describe("...CB-2B-1...")` PASS | **PASS** |
| **AC-2B.3** | `coerceV3DraftToEditorInitial` mapea `clinicalCriteria→diagnosis`, `extraction→extraction`, `fieldDefinitions/presentation` intactos; `clinicalCriteria=null` → `diagnosis={enabled:false,prompt:""}` | `calibration-v3-ui.ts:145-172`; mapeo `draft.clinicalCriteria?.prediagnosisEnabled` → `diagnosis.enabled`; `draft.clinicalCriteria?.prompt` → `diagnosis.prompt` | 3 tests en `describe("coerceV3DraftToEditorInitial")` PASS: clínico completo, `document_extraction` (CB-2B-2) con `diag.enabled=false`/`prompt=""`, defaults seguros | **PASS** |
| **AC-2B.4** | `buildDraftV3FromEditorState` respeta `status` (`draft`/`tested`, default `draft`); UI permite elegir | `AICalibrationEditor.tsx:206-213` (input `status`); `:292` `return.status = status ?? "draft"`; `:359-361` `useState(initialStatus)`; `:632-665` radio `name="cal-save-status"` con `data-testid="cal-save-status-draft"` / `cal-save-status-tested`; `:409` prop `status: saveStatus`; `:416` `onSaved?.(saveStatus)` | `calibration-v3.actions.test.ts` (41 tests existentes) sigue PASS — la prop nueva es opcional con default retrocompatible; ningún test existente se rompió | **PASS** |
| **AC-2B.5** | Editor se precarga desde draft V3 (`coerceV3DraftToEditorInitial`) o V1/V2 raw si no hay; `document_extraction` sin sección clínica | `CalibrationWorkspaceClient.tsx:508-518` ternario `aiCalibrationV3?.draft ? coerceV3DraftToEditorInitial(...) : initialRawCalibration`; `passa initialStatus={aiCalibrationV3?.draft?.status ?? "draft"}`; sección clínica sigue oculta por `getEditorSectionsForOperationMode("document_extraction")` (existente, verificado en QA-20260820-03 + `calibration-v3.actions.test.ts` test 4) | `coerceV3DraftToEditorInitial` test "document_extraction" PASS; el ternario en cliente aprovecha el helper puro + misma `initialRawCalibration` legacy | **PASS** (revisión visual + lógica derivada) |
| **AC-2B.6** | Panel muestra `operationMode`, draft, `currentPublishedVersion`, `supersededCount` | `CalibrationV3StatusPanel.tsx:107-131` header; `:134-158` grid 3 cards; `data-testid="calibration-v3-draft-status"` y `calibration-v3-published-version` | Revisión de código + logica derivada de `describeCalibrationV3State` (tests 9/9 PASS) | **PASS** (revisión manual del flujo; no se ejecuta browser en este entorno) |
| **AC-2B.7** | "Publicar" sólo con `canPublish=true`; llama `publishAICalibrationV3`; éxito → `versionNumber` + `onChanged`; fallo → `mapPublishErrorCode` + gate resaltado | `CalibrationV3StatusPanel.tsx:198-221` `handlePublish` (invoca `publishAICalibrationV3(testId)`; éxito → `setMessage({...versionNumber})` + `onChanged()`; fallo → `mapPublishErrorCode(res.code)`); `:231-245` botón público sólo si `canPublish`; `:266-291` lista gates `data-failed` resaltado | `mapPublishErrorCode` 11 gates + 6 códigos no-gate + fallback (4 tests PASS); `getPublishGateVisibility` 5 tests PASS; `publishAICalibrationV3` ya existente valida gates G0-G9 server-side | **PASS** |
| **AC-2B.8** | `mapPublishErrorCode` cubre 10 gates + códigos sin gate + fallback seguro | `calibration-v3-ui.ts:308-347` switch cubre G0/G0b/G1/G2/G3/G4/G5/G6/G7/G8/G9 + FORBIDDEN/NO_DRAFT/DRAFT_NOT_TESTED/UNAUTHENTICATED/TEST_NOT_FOUND/INTERNAL_ERROR + `default` fallback | 4 tests en `describe("mapPublishErrorCode")` PASS: 11 mapeos G, 6 códigos no-gate, código desconocido, string vacío | **PASS** |
| **AC-2B.9** | `manual_service`: no editor, no publicar, aviso. `isLegacyOnly`: flujo V1/V2. Snapshot congelado | `CalibrationWorkshopClient.tsx:506-518` `!v3State.isManualService` antes de montar `<AICalibrationEditor>`; `CalibrationV3StatusPanel.tsx:83-99` early return `manual_service` con `data-testid="calibration-v3-status-panel-manual-service"`; `:126-130` badge `snapshot legacy congelado` cuando `hasLegacySnapshot`; `:484-491` badge V2 legacy sólo si `v3State.isLegacyOnly && aiCalibration` | `getPublishGateVisibility` test "manual_service" PASS (G0b applicable=false, G8/G9 N/A P-04, G5 N/A); `describeCalibrationV3State` test "manual_service + V3" PASS | **PASS** |
| **AC-2B.10** | Build Vercel: `prisma generate && next build` 0 errores; typecheck 0; lint 0 nuevos; tests verde. Helpers puros sin `'use server'`, sin `node:crypto` | `prisma generate` (1.70s, ✓); `next build` (SUCCESS, ruta `/admin/services/[id]/calibration` listada); `tsc --noEmit` (exit 0); `eslint` (5 archivos scope, 0 errors / 0 warnings) | `grep -E "node:crypto\|use server" frontend/src/lib/calibration-v3-ui.ts` → 0 ocurrencias; `grep "calibration-v3-shared" frontend/src/lib/calibration-v3-ui.ts` → 0 ocurrencias (validado por inspección: header sólo documenta la prohibición) | **PASS** |
| **AC-2B.11** | No-admin (`canEdit=false`) no ve editor; aviso "Requiere rol ADMIN o superior" | `CalibrationV3StatusPanel.tsx:69-80` early return `!canEdit` → `<div data-testid="calibration-v3-status-panel-no-permission">` con mensaje "Requiere rol ADMIN o superior... Publicación (disponible para SUPERADMIN) también está deshabilitada."; `page.tsx:99-100` `canEdit = isAdminLike(role)` (ADMIN_LIKE_ROLES = SUPERADMIN\|ADMIN); `saveAICalibrationV3` y `publishAICalibrationV3` server-side rechazan con `FORBIDDEN` (defensa en profundidad) | Inspección directa del panel + `roles.ts:22, 28-31` para `isAdminLike`; `calibration-v3.actions.ts:179` documenta "Publicar requiere SUPERADMIN (acción sensible)" | **PASS** (revisión visual + audit código) |
| **CB-2B-1** | `currentPublishedVersionId` roto → cae a primera `published\|disabled` | `calibration-v3-ui.ts:74-80` (preferencia + fallback); mismo algoritmo que `getPublishedCalibrationForEventTest` | Test "CB-2B-1 cae a la primera..." PASS; test "publishedVersions no vacías pero todas superseded" PASS | **PASS** |
| **CB-2B-2** | `clinicalCriteria=null` (document_extraction) → `diagnosis.enabled=false` | `calibration-v3-ui.ts:164-168` mapeo con `clinical?.prediagnosisEnabled ?? false`; sección clínica ya ocultada por `getEditorSectionsForOperationMode` (CB-14, tests 4 Fase 2) | Test "document_extraction…" PASS | **PASS** |
| **CB-2B-3** | `operationMode` ausente/inválido → `null`; no se asume Audiometría | `page.tsx:82-85` `isOperationModeValue` → `null`; `AICalibrationEditor.tsx:330` `getEditorSectionsForOperationMode(null)` retorna editor completo (legacy) | Tests `isOperationModeValue` (2) PASS; `describeCalibrationV3State` "operationMode inválido" PASS; `getPublishGateVisibility` test "operationMode=null" PASS (G0 applicable=false) | **PASS** |
| **CB-2B-4** | Sin V3 ni V2 → `isLegacyOnly=true`, editor con `initialRawCalibration` | `CalibrationWorkspaceClient.tsx:508-518` ternario; `calibration-v3-ui.ts:96` `isLegacyOnly = !hasV3`; `page.tsx:176-186` badge V2 legacy sólo si `!aiCalibrationV3` | Inspección + `describeCalibrationV3State` test "aiCalibrationV3=null" PASS | **PASS** |
| **CB-2B-5** | `publishAICalibrationV3` con `NO_DRAFT`/`DRAFT_NOT_TESTED`/`TEST_NOT_FOUND` → mensaje claro, sin crash | `CalibrationV3StatusPanel.tsx:198-221` `handlePublish` siempre retorna; `mapPublishErrorCode` para `NO_DRAFT`/`DRAFT_NOT_TESTED` → `gate=null` (no se resalta) | Test `mapPublishErrorCode` "códigos sin gate" PASS (incluye `NO_DRAFT`, `DRAFT_NOT_TESTED`, `TEST_NOT_FOUND`) | **PASS** |
| **RBAC UI** | `canEdit` (ADMIN+) sólo UX; `canPublish` (SUPERADMIN) sólo UX; server-side autoritativo | `roles.ts:22, 28, 40` (ADMIN_LIKE_ROLES = SUPERADMIN\|ADMIN; isSuperAdmin = SUPERADMIN); `calibration-v3.actions.ts:179, 192` server-side rechazan no-admin/no-superadmin con `FORBIDDEN` | Inspección directa; `isAdminLike`/`isSuperAdmin` ya auditados en Fases anteriores | **PASS** |
| **No-default Audiometría** | No se introduce default silencioso a `Audiometria`; el defecto H3 histórico (`CalibrationWorkspaceClient.tsx:500`) no se replica | `coerceV3DraftToEditorInitial` retorna `canonicalStudyType: ""` cuando `draft.canonicalStudyType == null`; `getEditorSectionsForOperationMode(operationMode)` ya no produce `Audiometria` por default | Tests `coerceV3DraftToEditorInitial` "defaults seguros" PASS; tests `getEditorSectionsForOperationMode` (Fase 2) PASS | **PASS** |
| **Fallback V1/V2** | Sin raíz V3, el editor usa `initialRawCalibration`; badge V2 legacy sólo si `isLegacyOnly`; `saveAICalibrationV3` inicializa V3 capturando `legacyV1V2Snapshot` | `CalibrationWorkspaceClient.tsx:510-513` ternario; `:484-491` badge V2 legacy conditional; `calibration-v3.actions.ts:228-…` tests 31/32 (Fase 2) sobre `legacyV1V2Snapshot` siguen PASS | Inspección + suite focal 41/41 PASS | **PASS** |
| **Backend/migración/Prisma** | No cambios de backend, no cambios de schema, no migraciones | `git diff HEAD -- backend/ frontend/prisma/` → vacío; `git status` confirma sólo `frontend/src/...` + `lib/calibration-v3-ui.ts` + nuevo panel + nuevo test | `git status --short` + `git diff HEAD` | **PASS** |

---

## 3. Validaciones independientes (re-ejecutadas)

| Validación | Comando | Entorno | Exit / Resultado |
|---|---|---|---|
| **TypeScript check** | `cd frontend && npm run typecheck` (= `tsc --noEmit`) | local Node 22 + tsc 5 | exit 0 — **0 errores** |
| **Tests focales (nuevos + existentes scope)** | `cd frontend && npx vitest run src/components/calibration/__tests__/calibration-v3-ui.test.ts src/actions/__tests__/calibration-v3.actions.test.ts` | Node 22 + Vitest 2.1.9 | **65 passed / 0 failed** (24 nuevos + 41 existentes) en 1.33s |
| **Lint focal** | `cd frontend && npm run lint -- src/components/calibration/AICalibrationEditor.tsx src/components/calibration/CalibrationWorkspaceClient.tsx src/components/calibration/CalibrationV3StatusPanel.tsx src/lib/calibration-v3-ui.ts 'src/app/admin/services/[id]/calibration/page.tsx'` | local ESLint 9 | **0 errors / 0 warnings** |
| **Prisma generate** | `cd frontend && npx prisma generate` | local | **OK** — Generated Prisma Client v5.22.0 en 1.70s |
| **Next build Vercel** | `cd frontend && npx next build` (= `prisma generate && next build`) | local Node 22 | **SUCCESS** — `Route (app)` lista `/admin/services/[id]/calibration` como `ƒ (Dynamic)`. Sin warnings/errors en consola. |
| **Vitest suite completa** | `cd frontend && npm test` (= `vitest run`) | Node 22 + Vitest 2.1.9 | **679 passed / 15 failed** — todos los 15 fallos en `src/actions/__tests__/medical-exam.actions.test.ts` (`ImpresiónAptitudSchema.parse` espera `estado_nutricional`/`salud_bucal` como string). |
| **Verificación baseline fallos preexistentes** | `git stash` + `npx vitest run src/actions/__tests__/medical-exam.actions.test.ts` + `git stash pop` | local | **15 failed / 85 passed** sobre `HEAD` sin cambios — **idéntico al baseline**. No introducido ni regresado por FASE 2B. IMPL-REPORT §6.1 lo documenta. |
| **Hook order** | `grep -n "useState\|useTransition\|useRouter\|use[A-Z]" frontend/src/components/calibration/AICalibrationEditor.tsx` | grep | Todos los hooks se llaman **antes** del early return de `manual_service` (líneas 343-361 → early return 366). `saveStatus` se inserta al final, sin alterar orden. ✓ |
| **Calibration-v3-ui.ts pureza** | `grep -n "use server\|node:crypto\|require(" frontend/src/lib/calibration-v3-ui.ts` | grep | **0 ocurrencias** (sólo comentarios que documentan la prohibición). |
| **Sin import de calibration-v3-shared en modules cliente** | `grep -rn "calibration-v3-shared" frontend/src/components/ frontend/src/app/admin/services/[id]/calibration/` | grep | 0 ocurrencias — los componentes cliente no importan el módulo con `node:crypto`. ✓ |
| **Contratos protegidos** | `git status --short -- frontend/src/actions/calibration-v3.actions.ts frontend/src/lib/calibration-v3-shared.ts frontend/src/types/calibration.ts backend/ frontend/prisma/` | git | Vacío. Cero touch. |
| **No mezcla tarjetas-muestra** | `git branch -a` + `grep -r tarjetas-muestra frontend/src` | git + grep | Rama `arch-20260819-02-tarjetas-muestra` viva independiente, 0 coincidencias en `frontend/src`. |
| **Async params Next.js 16** | `grep -n "const { id } = await params" frontend/src/app/admin/services/\[id\]/calibration/page.tsx` | grep | Línea 51: presente. Cumple AGENTS §16. |

---

## 4. Hallazgos priorizados

### P0 / P1 — Críticos / Altos
*(Ninguno detectado.)*

### P2 — Medios
*(Ninguno detectado.)*

### P3 — Bajos

#### W-1 — P3 Baja: Variable muerta `familyTemplateId` en `getPublishGateVisibility`
**Evidencia:** `frontend/src/lib/calibration-v3-ui.ts:204` declara `const familyTemplateId = draft ? null : null` con comentario `// siempre null (P-04); placeholder del signature.`. El ternario siempre retorna `null`; por tanto `applicable: familyTemplateId !== null` (líneas 285, 291) es siempre `false` para G8/G9. Cumple el objetivo de P-04 (no-inventar), pero es código muerto y ruido en la firma.

**Impacto:** riesgo nulo — el comportamiento es el deseado (G8/G9 siempre N/A por P-04). El placeholder confunde a un lector nuevo del helper.

**Reproducción:** inspección directa del archivo.

**Owner recomendado:** SOFIA (clean code, sin cambio de comportamiento). O basta con eliminar en un próximo touch.

**Condición de cierre:** limpieza menor cuando se toque el helper por otra razón. No bloquea QA.

#### W-2 — P3 Baja: Verificación `manual_service` sin tests dedicados del flujo UI completo
**Evidencia:** AC-2B.9 pide "no editor, no publicar, aviso". Esto se cumple por composición: `v3State.isManualService` en `calibration-v3-ui.ts:94` (test "manual_service + V3" PASS) + `!v3State.isManualService` guard en `CalibrationWorkspaceClient.tsx:508` + early return en `CalibrationV3StatusPanel.tsx:83-99`. No hay un test de integración React que renderice el árbol completo con un `MedicalTest` `manual_service` y confirme visualmente que ni el editor ni el botón aparecen. Consistente con la regla del handoff §8: "vitest en entorno node, sin jsdom/happy-dom, no tests `.test.tsx`".

**Impacto:** riesgo bajo — la cadena de guards es directa y cada componente está probado en aislamiento. La revisión manual del IMPL_REPORT §4 W-2 lo admite como deuda.

**Reproducción:** imposible sin entorno con `@testing-library/react` + jsdom + un `MedicalTest` sembrado.

**Owner recomendado:** INTEGRA (decide si Playwright E2E con sesión sembrada pertenece a Fase 2B o a Fase 6).

**Condición de cierre:** mismo P3 que QA-20260820-06 W-2/F-3 (cobertura proporcional). Aceptación del dueño.

#### W-3 — P3 Baja: Cobertura E2E del flujo completo "guardar tested → publicar" no ejecutada en este entorno
**Evidencia:** AC-2B.4 + AC-2B.7. La cadena `editor (radio) → saveAICalibrationV3 → panel (badge draft: tested) → publishAICalibrationV3 → refresh` está cubierta por composición:
- 24 tests nuevos sobre los helpers puros (PASS).
- 41 tests existentes de `calibration-v3.actions.test.ts` (PASS) — confirman que `saveAICalibrationV3` acepta `status: "tested"` y `publishAICalibrationV3` rechaza con `DRAFT_NOT_TESTED` si no está en `tested`.
- 0 tests de integración React renderizando el flujo completo. Consistente con el handoff §8.

**Impacto:** riesgo bajo — la lógica de `mapPublishErrorCode` cubre los 11 gates + códigos sin gate + fallback; el UI resalta el gate fallido. La única ruta no probada es el cursor de nieve end-to-end con sesión sembrada.

**Reproducción:** imposible sin `@testing-library/react` + sesión ADMIN+/SUPERADMIN sembrada + Prisma test DB.

**Owner recomendado:** INTEGRA (cobertura proporcional, ya documentada en Fases 3/4/5).

**Condición de cierre:** Aceptación del dueño (consistente con QA-20260820-06 W-2).

#### W-4 — P3 Baja: defecto H3 (`?? "Audiometria"`) en `CalibrationWorkspaceClient.tsx` histórico no cubierto por este PR
**Evidencia:** El IMPL_REPORT §5 documenta que la rama `0cce88f` ya eliminó el `?? "Audiometria"` de la rama de upload, pero la página tiene una lógica separada (no tocada por FASE 2B) que podría mantener defaults. `git diff HEAD -- frontend/src/components/calibration/CalibrationWorkspaceClient.tsx` muestra que este PR no toca la línea con `?? "Audiometria"` si persiste (no se introdujo nuevo default; el helper `coerceV3DraftToEditorInitial` retorna `""` si no hay draft, y el badge V2 legacy permanece solo si `isLegacyOnly`).

**Impacto:** cero si no hay default silencioso nuevo; este PR no introduce ninguno. La AC de no-default Audiometría (H3) se cumple por la rama principal.

**Owner:** ninguno accionable (no defecto introducido por FASE 2B).

**Condición de cierre:** ninguno — es un aviso de seguridad, no un accionable.

#### W-5 — P3 Baja: autorização de commit/push pendiente (no defecto)
**Evidencia:** `git status` confirma todos los cambios en working tree (no committeados). `PROYECTO.md` marca "Sin delegar" / "no commit/push sin OK Frank". `CURRENT.md` reasignado a FASE 2B. Cumple AGENTS §11 (autorización humana para `commit`/`push`/`PR`).

**Impacto:** cero mientras no se commitee. Cuando se autorice, el revert es trivial: `git checkout HEAD -- <archivos>` + `rm` de los 3 nuevos (spec del IMPL_REPORT §"Notas de reversión").

**Owner:** Frank (vía ATLAS — autorización humana explícita AGENTS §11).

**Condición de cierre:** OK explícito de Frank en la conversación vigente. **No bloquea QA** — bloquea el commit/push.

---

## 5. Riesgo operativo

### Preparación por entorno

| Entorno | Estado | Notas |
|---|---|---|
| **Calidad** | **LISTO** | typecheck 0 errores; vitest 679/694 (15 baseline preexistentes, **no introducidos**); lint 0 nuevos en 5 archivos scope; `prisma generate` OK; `next build` SUCCESS con la ruta `/admin/services/[id]/calibration` listada. |
| **Staging** | **NO_LISTO** | No requiere migración Prisma (este PR no toca schema). Requiere commit/push primero (autorización Frank) + smoke E2E manual del flujo `/admin/services/[id]/calibration` con un `MedicalTest` sembrado con `operationMode` y `aiCalibration` V3. |
| **Producción** | **NO_LISTO** | Mismo requisito + AGENTS §11 (autorización humana explícita en conversación vigente). |

### Bloqueador operativo separado

- **Commit/push**: requiere OK explícito de Frank. GEMINI **no ejecuta, no recomienda ejecución** — sólo documenta el procedimiento. Cualquier intento de hacerlo sin OK humano es **violación de AGENTS §11**.

### Riesgos no bloqueantes identificados

- El nuevo panel `CalibrationV3StatusPanel` es `client-side`; consume `publishAICalibrationV3` (server action con `'use server'`). Cumple FIX-20260820-01-VERCEL-BUILD: el módulo cliente no exporta funciones no-async ni importa el helper `calibration-v3-shared.ts` (que contiene `node:crypto`).
- `useState(initialStatus ?? "draft")` se inicializa una vez; cambios posteriores del prop `initialStatus` no se reflejan en `saveStatus`. Consistente con el patrón del editor (no se documenta como bug porque `aiCalibrationV3?.draft?.status` es estable por diseño del workspace — ver `CalibrationWorkspaceClient.tsx:516`).
- La sesión `getServerSession` retorna `null` si no hay sesión; en ese caso `role = undefined` → `canEdit=false`/`canPublish=false` → el panel muestra el aviso "Requiere rol ADMIN o superior". El gate server-side de las acciones rechaza con `FORBIDDEN` (defensa en profundidad).
- `coerceV3DraftToEditorInitial` aplica defaults seguros (`provider: "gemini"`, `version: ""`, `schemaVersion: ""`) cuando los campos son `null`. Documenta la migración implícita sin escribir — preserva semántica de `ARCH-20260809-02`.

---

## 6. Autoauditoría (anti-código)

- [x] Delimité el incremento exacto (5 archivos mod + 3 nuevos + 2 docs). No expandí scope.
- [x] Verifiqué SPEC/ADR vigentes (v1.1) y handoff FASE 2B.
- [x] Revisé evidencia independiente (no sólo IMPL-REPORT): ejecuté los 4 gates, typecheck, suite focal y full, build Vercel, lint focal, verifiqué baseline con `git stash`.
- [x] No edité código, tests, migraciones, config, `discovery/`, SPEC ni `PROYECTO.md`.
- [x] No imprimí secretos/PII (no exploré `MedicalTest.options` con datos reales; sólo inspeccioné fixtures de tests).
- [x] Cada finding tiene evidencia (línea/archivo) + impacto + condición de cierre.
- [x] Separé severidad QA (P3) de niveles L1/L2/L3 de reparación.
- [x] Separé QA / staging / producción (sección 5).
- [x] No invoqué subagentes (DEBY/CRONISTA/SOFIA) ni declaré `DONE`.
- [x] Handoff vuelve a ATLAS con acción concreta (sección 7).

---

## 7. Handoff a ATLAS

### Veredicto

**`PASS_WITH_WARNINGS`** — la implementación cumple AC-2B.1 a AC-2B.11, CB-2B-1 a CB-2B-5, RBAC UI (ADMIN+ edita, SUPERADMIN publica), server-side autoritativo en `calibration-v3.actions.ts`, no-default Audiometría, fallback V1/V2 intacto, contratos backend/Prisma/migraciones sin tocar, helpers puros sin `'use server'`/`node:crypto`. Quedan 5 P3 documentados (4 de cobertura/limpieza + 1 bloqueador operativo de commit/push). El cambio de contrato visible es retrocompatible: las props nuevas son opcionales con defaults consistentes.

### Acción

ATLAS debe:
1. **Aceptar el veredicto QA-20260820-07** (P3 aceptables, ninguno bloquea entrega).
2. **Decidir sobre commit/push FASE 2B** (autorización explícita separada de Frank — `git status` confirma árbol con FASE 2B sin commit + Fases 3/4/5 previamente sin commit).
3. **No delegar fixes a SOFIA** — no hay hallazgos P1/P2 que requieran corrección. Los P3 son aceptaciones del dueño o limpieza menor.
4. **Fases 3-7 siguen BLOQUEADAS** (consistente con `CURRENT.md` + `PROYECTO.md`); este PR no cambia ese estado.

### Gate siguiente

- Si Frank autoriza commit/push FASE 2B → CRONISTA aplica la transición de `PROYECTO.md` (marcar FASE 2B como DONE tras el merge). Si el lote incluye Fases 3/4/5, Frank debe autorizar explícitamente cada una.
- Si Frank difiere el commit → FASE 2B queda `READY_FOR_VERIFYING` + estado QA aprobado localmente, sin avance a staging hasta nueva OK.
- **No se aplica a staging/prod** sin OK explícito de Frank (AGENTS §11).

---

**Estado QA:** `PASS_WITH_WARNINGS` (5 P3 aceptables — 4 de cobertura/limpieza + 1 bloqueador operativo de commit/push).
**Estado SOFIA:** `READY_FOR_VERIFYING` (sin cambios — INTEGRA mantiene el estado).
**Estado CRONISTA:** no aplica hasta nueva instrucción de INTEGRA/Frank.
