# HANDOFF_ARCH-20260527-11

Dile a Sofia: Trabaja sobre `context/SPECs/SPEC_ARCH-20260527-11-SLICE-A-TRAZABILIDAD-CONVERGENCIA-EVENT.md`. Estado actual: `MedicalEvent` ya es la ancla del workspace clínico, pero hoy no preserva procedencia estructurada de admisión; el flujo manual de `frontend/src/components/CheckInModal.tsx` crea eventos directos y la convergencia entre cita, proyecto y futuros externos todavía no es auditable. Issue Jira: SIN-ISSUE. Objetivo: cerrar el Slice A mínimo de trazabilidad agregando origen explícito en `MedicalEvent`, persistiéndolo en el flujo actual de recepción y mostrándolo de forma visible en recepción y en la vista del evento, sin rediseñar admisión ni abrir nuevos carriles todavía. Salida esperada: código listo para validación con migración Prisma, persistencia mínima en creación de evento, badge visible de origen en recepción y contexto visible en `events/[id]`, más nota corta de validación manual. Restricciones: máximo 5 superficies de código más la migración Prisma; no tocar `Appointment`, no tocar `ProjectWorker`, no crear nuevas rutas, no abrir carril externo todavía; si necesitas un sexto archivo funcional, devuelve `BLOQUEO DE CONTEXTO`. Archivo ancla: `frontend/prisma/schema.prisma`. Validación esperada: `cd frontend && pnpm exec prisma validate && pnpm exec eslint src/actions/event.actions.ts src/app/reception/page.tsx src/app/events/[id]/page.tsx`.