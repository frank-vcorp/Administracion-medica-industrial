# HANDOFF_ARCH-20260527-24

Dile a Sofia: Trabaja sobre `context/SPECs/SPEC_ARCH-20260527-24-BUSQUEDA-EXTERNA-SERVER-SIDE-Y-REUTILIZACION.md`. Estado actual: en `Ingreso externo`, la búsqueda visual filtra en cliente una lista precargada y solo muestra personas sin empresa, mientras que la deduplicación real al crear ya existe en backend y consulta `workers` de forma más amplia. Issue Jira: SIN-ISSUE. Objetivo: reemplazar la búsqueda local del modo externo por una búsqueda server-side directa a DB, manteniendo intacto el guardia final de duplicados ya implementado en `createExternalWorkerIntake()`. Salida esperada: código listo para validación donde el modal muestra coincidencias reales desde DB al escribir, permite seleccionar una persona existente y conserva el alta mínima actual como fallback. Restricciones: máximo 2 archivos funcionales; no tocar Prisma schema; no crear rutas nuevas; no romper el modo `Trabajador existente`; no reescribir `createExternalWorkerIntake()` salvo para agregar una action de búsqueda acotada en el mismo archivo. Archivo ancla: `frontend/src/components/CheckInModal.tsx`. Validación esperada: `cd frontend && pnpm exec eslint src/components/CheckInModal.tsx src/actions/worker.actions.ts` más validación manual de búsqueda real, selección de existente y advertencia `ambiguous_match` en creación sin selección.