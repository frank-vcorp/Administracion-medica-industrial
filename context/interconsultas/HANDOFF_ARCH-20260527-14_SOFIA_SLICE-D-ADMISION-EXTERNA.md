# HANDOFF_ARCH-20260527-14

Dile a Sofia: Trabaja sobre `context/SPECs/SPEC_ARCH-20260527-14-SLICE-D-ADMISION-EXTERNA-SIN-EMPRESA.md`. Estado actual: el dominio ya permite `Worker.companyId = null`, pero la capa operativa sigue sesgada a empresa; recepción no puede admitir bien a una persona externa sin inventar empresa o salir del flujo. Issue Jira: SIN-ISSUE. Objetivo: habilitar admisión externa sin empresa desde recepción, permitiendo buscar o crear un `Worker` mínimo y converger directo a `MedicalEvent` con origen externo, sin usar `Project` ni `Appointment`. Salida esperada: código listo para validación con variante de ingreso externo en recepción, alta mínima inline y continuidad al workspace clínico, más nota corta de validación manual. Restricciones: máximo 5 archivos funcionales; no tocar Prisma schema, no volver opcional `Appointment.companyId`, no abrir ruta nueva, no tocar `Project` ni `ProjectWorker`; si necesitas un sexto archivo funcional, devuelve `BLOQUEO DE CONTEXTO`. Archivo ancla: `frontend/src/components/CheckInModal.tsx`. Validación esperada: `cd frontend && pnpm exec eslint src/components/CheckInModal.tsx src/actions/event.actions.ts src/actions/worker.actions.ts src/app/reception/page.tsx src/app/events/[id]/page.tsx`.