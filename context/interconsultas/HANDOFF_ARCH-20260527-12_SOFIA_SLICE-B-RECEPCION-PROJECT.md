# HANDOFF_ARCH-20260527-12

Dile a Sofia: Trabaja sobre `context/SPECs/SPEC_ARCH-20260527-12-SLICE-B-RECEPCION-POR-PROJECT.md`. Estado actual: `Project` ya resuelve calendario y alta masiva, pero todavía no existe cola operativa de recepción para campañas pre-registradas; hoy no se puede ver quién llegó, quién ya ingresó a clínica ni crear `MedicalEvent` contextual desde el proyecto. Issue Jira: SIN-ISSUE. Objetivo: convertir `Project` + `ProjectWorker` en carril operativo de recepción para pre-registrados, permitiendo marcar llegada, ingresar a clínica sin `Appointment` y abrir el evento resultante, todo dentro de la superficie existente de proyectos. Salida esperada: código listo para validación con evolución mínima de `ProjectWorker`, panel operativo en proyectos y navegación al workspace clínico, más nota corta de validación manual. Restricciones: máximo 5 superficies de código más migración Prisma; no tocar `Appointment`, no abrir ruta nueva, no cubrir alta rápida del mismo día, no tocar externos; si necesitas un sexto archivo funcional, devuelve `BLOQUEO DE CONTEXTO`. Archivo ancla: `frontend/src/components/ProjectsCalendar.tsx`. Validación esperada: `cd frontend && pnpm exec prisma validate && pnpm exec eslint src/actions/project.actions.ts src/actions/event.actions.ts src/components/ProjectsCalendar.tsx`.