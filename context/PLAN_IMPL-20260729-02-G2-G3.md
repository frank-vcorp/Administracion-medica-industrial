# Plan IMPL-20260729-02 — Gap G2 + Gap G3

## Estado de entrada

- Rama: `main`, alineada con `origin/main`.
- El workspace contiene numerosos archivos no rastreados preexistentes; no se tocarán ni incluirán en staging.
- SPEC leídas:
  - `context/SPECs/SPEC_IMPL-20260729-SOFIA-CORRECCIONES.md`
  - `context/SPECs/SPEC_IMPL-20260729-FLUJO-END-TO-END.md`
- Archivo objetivo principal leído completo: `frontend/tests/flujo-completo.spec.ts`.
- Scripts detectados en `frontend/package.json`: `build`, `typecheck`, `test`, `lint`.

## Bloqueo de baseline

La baseline obligatoria está rota antes de editar:

- `npm run typecheck`: FAIL. Incluye errores preexistentes en tests y dos errores actuales del propio `tests/flujo-completo.spec.ts` (líneas 73 y 360).
- `npm test`: FAIL, 12 fallos preexistentes en `maintenance.helpers.test.ts`; 261 pruebas pasan.
- `npm run lint`: FAIL, 95 errores y 65 warnings distribuidos por el proyecto; el archivo E2E presenta warnings.

Conforme al protocolo SOFIA, no se debe editar mientras la baseline esté rota. Frank indicó que errores preexistentes de typecheck no bloqueen, pero tests y lint también fallan y la instrucción operativa superior exige detener y reportar.

## Implementación prevista una vez desbloqueada la baseline

1. **Fact-Forcing por archivo**
   - Releer completo `frontend/tests/flujo-completo.spec.ts` inmediatamente antes de editar.
   - Leer completos los componentes que definen las UIs implicadas: perfiles, puestos, worker modal, citas, recepción, papeleta clínica y laboratorio.
   - Buscar imports, acciones y tipos dependientes; no modificar contratos públicos, esquemas, endpoints ni dependencias.

2. **Gap G2 — inspección real y TC-02**
   - Autenticar en producción mediante Playwright sin registrar credenciales.
   - Capturar snapshots de `/admin/profiles` antes y después de abrir “Nuevo Perfil”.
   - Identificar roles/nombres accesibles reales para nombre, empresa, estudios y submit.
   - Cambiar TC-02 a selectores accesibles y acotados al modal/formulario.
   - Validar TC-02 aislado y registrar evidencia/screenshot.

3. **Gap G3 — diagnóstico TC-04**
   - Abrir `/workers`, llenar el modal con datos únicos y capturar el estado antes/después del submit.
   - Observar mensajes de validación, respuesta de red, URL, cierre del modal y aparición del trabajador en tabla.
   - Corregir el test según causa real: datos requeridos, botón acotado al formulario o interacción posterior. Evitar `force: true` salvo que se confirme un defecto real de actionability y el submit sea correcto.
   - Validar TC-04 aislado y comprobar creación mediante UI; ejecutar query Prisma de solo lectura si el entorno autorizado lo permite.

4. **Selectores TC-03 y TC-05..TC-12**
   - Inspeccionar secuencialmente `/companies/[id]`, `/appointments`, `/reception`, `/events/[id]` y `/lab/reception` usando los IDs creados por la suite.
   - Sustituir selectores hipotéticos por roles, labels, placeholders o locators estructurales reales y estables.
   - Mantener skips únicamente para gaps funcionales comprobados, documentando la evidencia; no convertir fallos reales en skips.
   - Corregir además los errores TypeScript locales del archivo E2E sin ampliar alcance.

5. **Validación incremental y cierre**
   - Ejecutar TC-02 aislado, TC-04 aislado, luego TC-01..TC-04 y finalmente los 12 casos seriales.
   - Ejecutar validaciones dirigidas al archivo y repetir gates completos, comparando contra la baseline documentada.
   - Self-review del diff: alcance, selectores robustos, datos únicos, manejo de errores, ausencia de secretos y contratos no tocados.

6. **Checkpoint**
   - Crear `context/checkpoints/CHK_IMPL-20260729-E2E-DEPLOY-FINAL-V2.md` con tabla TC-01..TC-12, selectores corregidos, evidencia reproducible, queries Prisma de solo lectura, screenshots, riesgos y gaps restantes.

7. **Publicación autorizada**
   - Inspeccionar `git status`, `git diff`, `git log --oneline -10`.
   - Stage exclusivamente `frontend/tests/flujo-completo.spec.ts` y el checkpoint solicitado (el comando de stage dado por el usuario omite el checkpoint, por lo que se incluirá explícitamente para cumplir el entregable).
   - Commit con ID `IMPL-20260729-02` y push normal a `origin main`, sin force-push.
   - Reportar hash y estado del push. Si falla el push, detener y escalar.

## Criterio para reanudar

Se requiere una baseline confiable o una autorización explícita de excepción que abarque los tres gates rotos (`typecheck`, `tests`, `lint`) y permita comparar únicamente regresiones introducidas por este cambio.
