# Checkpoint FIX-20260519-07 — Hotfix GEMINI_MODEL residual + Triage /appointments

**Fecha:** 2026-05-19  
**Agente:** SOFIA - Builder  
**ID:** FIX-20260519-07  
**Commit:** `e7bd7d7`  

---

## Defecto A — NameError GEMINI_MODEL en subida IA (RESUELTO ✅)

### Causa raíz
`ARCH-20260519-13` eliminó las constantes Python `GEMINI_MODEL` y `GEMINI_MODEL_EXTRACTION` al migrar la extracción a Featherless/Qwen. Pero tres payloads de auditoría todavía las referenciaban como variables, causando `NameError` en runtime al procesar cualquier documento IA.

### Anclas corregidas en `backend/app/main.py`

| Línea (antes) | Campo | Valor anterior | Valor corregido |
|---|---|---|---|
| ~817 | `extraction_snapshot.audit.model_name` | `GEMINI_MODEL` | `FEATHERLESS_EXTRACTION_MODEL` |
| ~844 | `prediagnosis_snapshot.audit.model_name` | `predx_model_used or GEMINI_MODEL` | `predx_model_used or GEMINI_MODEL_CLINICAL` |
| ~907 | `v2_prediagnosis_from_params.audit.model_extraction` | `GEMINI_MODEL_EXTRACTION` | `FEATHERLESS_EXTRACTION_MODEL` |

### Validación
- **Gate 1 (Compilación):** `python -m py_compile app/main.py` → ✅ OK  
- **Gate 1B (Residuos):** `grep GEMINI_MODEL main.py` → solo definición de `GEMINI_MODEL_CLINICAL` y comentarios; cero usos runtime  
- **Gate 2 (Tests):** `pytest` no disponible en el entorno dev (no instalado); test_ai_pipeline.py no referencia `GEMINI_MODEL` directamente  
- **Gate 3 (Revisión):** diff de 6 líneas, cambio puntual sin alteración semántica  
- **Gate 4 (Commit/Push):** `e7bd7d7` pusheado a `origin/main` → Railway redeploy automático esperado  

---

## Defecto B — `/appointments` 500 en Vercel (FRENTE SEPARADO ⚠️)

### Conclusión del triage
El 500 en `/appointments` **no tiene conexión causal** con el `NameError` backend. La ruta de citas no llama al pipeline IA.

### Evidencia estática

1. `frontend/src/app/appointments/page.tsx` → **`'use client'`**, sin SSR de datos, DB calls solo en `useEffect`
2. `frontend/src/actions/appointment.actions.ts` → sin errores TypeScript
3. `frontend/src/auth.ts` + `frontend/src/app/layout.tsx` → sin errores TypeScript

### Hipótesis más probable (pendiente de confirmar con logs Vercel)

El commit `8213211` (IMPL-20260519-10 — QR operativo) agregó el campo `qrOperativo String?` al modelo `Appointment` en `schema.prisma` pero **no generó migración SQL**. La última migración en disco es `20260507000000_add_papeleta_timeline`.

- En Railway, la columna `qrOperativo` no existe en la tabla real  
- El Prisma Client generado en build sí espera el campo  
- Cualquier `findMany` sobre `Appointment` sin `select` explícito intentará `SELECT qrOperativo FROM Appointment` → error de columna  
- Si hay alguna ruta de la página que ejecuta un server action en SSR (o si hay un error de build previo), daría 500

### Próximo paso recomendado (segundo slice)
1. Confirmar en logs Vercel si el error es `column "qrOperativo" does not exist`  
2. Si confirma: crear migración `prisma migrate dev --name add_qr_operativo_appointment` y deployar  
3. Si los logs muestran otro error: abrir triage específico con ancla en el stack trace real

### Restricción activa
No se abre refactorización amplia de citas sin evidencia clara del error real en logs de producción.

---

## Archivos tocados
- `backend/app/main.py` — 3 líneas de lógica + 3 comentarios de trazabilidad

## Archivos NO tocados
- `backend/tests/test_ai_pipeline.py` — no referencia `GEMINI_MODEL`; no requiere ajuste
- `frontend/*` — fuera del alcance mínimo de este slice

---

_Checkpoint generado por SOFIA · FIX-20260519-07_
