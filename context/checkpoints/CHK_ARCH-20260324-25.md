# Checkpoint — ARCH-20260324-25
**Fecha:** 2026-03-24
**Agente:** INTEGRA - Arquitecto
**Objetivo:** Cerrar trazabilidad del paquete de navegación, perfiles médicos y bootstrap local antes de validación autenticada.

---

## Estado del paquete

1. Se consolidó la navegación nueva con aterrizaje en `/dashboard`.
2. La pantalla `/login` quedó sin sidebar ni header global.
3. `Perfiles Médicos` quedó expuesto como ruta oficial en `/admin/profiles`.
4. `start.sh` soporta arranque local usando base remota sin correr migraciones ni seed.
5. El árbol contenía checkpoint y dictamen previos generados durante la intervención, pero seguían sin versionarse; por eso no aparecían como parte del historial compartido.

---

## Evidencia mínima validada

- `GET /` responde con redirección a `/dashboard`.
- `GET /login` responde `200` y no renderiza el chrome global.
- `GET /admin/profiles` sin sesión redirige a `/login`.
- `GET /companies` sin sesión redirige a `/login`.

---

## Siguiente paso operativo

Ejecutar validación autenticada del punto 1 para confirmar visibilidad real por sesión en:

- `/dashboard`
- `/companies`
- `/companies/[id]`
- `/admin/profiles`
- `/appointments/overview`