# PROYECTO: Residente Digital

**Estado:** IN_PROGRESS — ARCH-20260820-01 (Calibración como fuente única — Fase 1 DONE, Fase 2 DONE, Fase 3 DONE, Fase 4 DONE (verificado localmente, sin commit/push); Fase 5 READY). SPEC v1.1 + ADR v1.1 + HANDOFF firmados. Fase 1: commit `22ba048`, GEMINI QA-20260820-02 APROBADO_CON_OBSERVACIONES (43/43 tests). Fase 2: commit `0cce88f`, GEMINI QA-20260820-03 PASS_WITH_WARNINGS (41 tests, P2 cerrados tras rework). Fase 3: árbol de trabajo (HEAD `0cce88f`, sin commit), GEMINI QA-20260820-04 PASS_WITH_WARNINGS (AC-3.1–3.4 PASS, 62/62 vitest, 43/43 pytest, 0 bloqueadores). Fase 4: árbol de trabajo (sin commit), IMPL-20260820-04 + fix F-1 `IMPL-20260820-05` (test AST multi-línea), GEMINI QA-20260820-05 PASS_WITH_WARNINGS (AC-4.1–4.5 PASS, 10/10 tests Fase 4, 43/43 resolver, typecheck 0, 0 regresión; F-1 P2 resuelto; F-2/F-3/F-4 P3 aceptados como riesgo). Frank autorizó encadenamiento autónomo Fases 1-7 (2026-08-20 14:20 CST) sin confirmación entre fases, con gates + GEMINI por fase. Previo: ARCH-20260819-02 (tarjetas por prueba, prototipo no persistente). Previo: IMPL-20260809-04 pusheado a `main` (commit `99dc46c`) — selector multi-proveedor de extracción IA (Gemini + MiniMax M3) operativo
**Fase:** Fase operativa: estabilización productiva | IA clínica (MedGemma/DR7) intacta | IA extractiva multi-proveedor | Calibración como fuente única (Fase 1 DONE — resolver V3 + clasificador `operationMode`; Fase 2 DONE — contrato V3 + estados + publicación + editor condicional; Fase 3 DONE — gate `enabled` + routing por canonicalStudyType + cleanup F-3 auth endpoint; Fase 4 DONE — clinicalCriteria reemplaza hardcodeos backend; Fase 5 READY — snapshot versionado / migración Prisma aditiva) | NOVA absorción (Slices A,B-v2,C,D,E,F,G cerrados; H parcial; I en curso)
**ID Actual:** ARCH-20260820-01 (Calibración como fuente única — Fases 1+2 DONE (commits `22ba048`+`0cce88f`); Fase 3 DONE verificada localmente sin commit (QA-20260820-04 PASS_WITH_WARNINGS); Fase 4 DONE verificada localmente sin commit (QA-20260820-05 PASS_WITH_WARNINGS + fix F-1 `IMPL-20260820-05`); Fase 5 READY — snapshot versionado; encadenamiento Fases 4-7 autorizado)
**Última Actualización:** 2026-08-20 20:25 CST (ARCH-20260820-01: Fase 4 DONE verificada localmente sin commit/push — IMPL-20260820-04 + fix F-1 `IMPL-20260820-05` (test AST multi-línea), GEMINI QA-20260820-05 PASS_WITH_WARNINGS (AC-4.1–4.5 PASS, 10/10 tests Fase 4, 43/43 resolver, typecheck 0, 0 regresión; F-1 P2 resuelto; F-2/F-3/F-4 P3 aceptados). Fase 5 READY — snapshot versionado con migración Prisma aditiva (columnas nullable; backup/validación de aditividad; detenerse ante staging/prod o irreversibilidad; GEMINI obligatorio). Commit/push de Fases 3 y 4 pendiente de autorización explícita separada de Frank)

## 📋 Descripción
Sistema de Administración Médica Industrial (AMI) para gestión de empresas, trabajadores, expedientes, citas y estudios médicos con integración de IA para lectura de documentos y pre-diagnóstico.

## 👥 Equipo (Agentes)
- **@INTEGRA**: Orquestación y Arquitectura
- **@SOFIA**: Desarrollo Frontend/Backend
- **@DEBY**: QA e Infraestructura

## 📅 Diario de Cambios
- **2026-08-20 (INTEGRA):** [✓ Fase 4 DONE / Fase 5 READY] **ARCH-20260820-01 — Cierre Fase 4 (clinicalCriteria reemplaza hardcodeos backend) + fix F-1.** GEMINI QA-20260820-05 PASS_WITH_WARNINGS: AC-4.1–4.5 PASS, 10/10 tests Fase 4, 43/43 resolver, typecheck 0, lint focal 0, 0 regresión (31 fallos preexistentes `M3_CREDENTIALS_UNAVAILABLE` idénticos a baseline). Hallazgo P2 F-1 (test `test_AC_4_3b` grep single-line no detectaba la única llamada multi-línea a `medical_calibration=` — shim deprecado autorizado por handoff §6.3) resuelto por `IMPL-20260820-05` (solo test, 0 archivos de producto): matcher AST multi-línea (`ast.parse`+`ast.walk` en `backend/tests/test_ai_pipeline.py:3012-3081`) que falla ante caller activo sin marcador de shim; verificado 10/10 Fase 4 + 43/43 resolver + detección empírica de regresión. F-2/F-3/F-4 (P3) aceptados como riesgo documentado (F-3 se cierra como cobertura en Fase 5). **Fase 4 DONE verificada localmente sin commit/push** (pendiente autorización explícita separada). **Fase 5 READY** — snapshot versionado / congelación histórica: migración Prisma **aditiva** (columnas nullable en `StudyExtractionSnapshot`/`AIPrediagnosisSnapshot`), persistencia en `main.py`, render histórico en `PapeletaWorkspace`/`ClinicalExtractionRenderer`; GEMINI obligatorio (cambio de schema/migración); backup + dry-run + validación de aditividad antes de cualquier apply; detenerse ante staging/prod o irreversibilidad. Handoff Fase 5: `context/interconsultas/HANDOFF_ARCH-20260820-01_FASE5_SOFIA_CALIBRACION-FUENTE-UNICA.md`.
- **2026-08-20 (INTEGRA):** [✓ Fase 1 DONE / Fase 2 IN_PROGRESS] **ARCH-20260820-01 — Calibración como fuente única de ejecución y presentación por prueba. Fase 1: clasificador `operationMode` + Resolver + adaptador V1/V2.** Escalamiento de ATLAS desde DEC-20260820-01 (Calibración como fuente única) + DEC-20260820-02 (clasificación operativa `operationMode`: `manual_service` | `document_extraction` | `clinical_interpretation`) + FND-20260820-01/02/03/04 + BR-20260820-01 (paridad obligatoria Calibración↔Events) + auditoría forense FIX-20260820-01 (DEBY, 11/11 hipótesis confirmadas). **Decisión arquitectónica (ADR-20260820-01 v1.1):** contrato `aiCalibration` V3 con estados de publicación (`draft/tested/published/superseded/disabled`) + `CalibrationResolver` como única fuente runtime + adaptador V1/V2→V3 de lectura (sin migración física) + clasificación `operationMode` en `MedicalTest.options` + herencia `familyTemplate`+`overrides` para laboratorio + degradación progresiva de hardcodeos (sin big-bang). **Anti-patrón H3 eliminado:** NO existe default silencioso a `Audiometria`; prueba sin `operationMode` confirmado cae a `manual_service`+`requires_review`. **Fase 1 DONE** (commit `22ba048` en main, GEMINI QA-20260820-02 APROBADO_CON_OBSERVACIONES 43/43 tests). **Fase 2 IN_PROGRESS** (contrato V3 + estados + publicación + editor condicional por operationMode). **Encadenamiento Fases 1-7 autorizado por Frank 2026-08-20 14:20 CST** (sin confirmación entre fases, con gates + GEMINI por fase). Alcance Fase 1: nuevo `backend/app/services/ai/calibration_resolver.py` (resolver V3 + adaptador V1/V2 + inferencia conservadora §11.3), `backend/app/api/v1/calibration.py` (endpoint `GET /resolve`), `backend/tests/test_calibration_resolver.py` (V1/V2/V3, manual_service no IA, document_extraction sin clinicalCriteria, clinical_interpretation con criterios, sin calibración nunca Audiometría, requires_review ambiguo). Restricciones: no tocar frontend Events, formularios clínicos, `prediagnostic.py`, Prisma/migraciones, ni `arch-20260819-02-tarjetas-muestra`; no eliminar hardcodeos todavía; no commit/push sin flujo de revisión. AC: AC-1.1 a AC-1.5 + CA-G01/G02/G19/G20. Validación: `pytest backend/tests/test_calibration_resolver.py -v`. Artefactos: `context/SPECs/SPEC_ARCH-20260820-01-CALIBRACION-FUENTE-UNICA.md` (v1.1, 658 líneas, 21 CA globales, 18 CB), `context/decisions/ADR-20260820-01-CALIBRACION-FUENTE-UNICA.md` (v1.1), `context/interconsultas/HANDOFF_ARCH-20260820-01_SOFIA_CALIBRACION-FUENTE-UNICA.md` (ACTIVE Fase 1). DoR implementación Fase 1: CUMPLIDA. Delegado a SOFIA vía task (WIP=1, secuencial). Próximo: recibir IMPL-REPORT → verificar AC-1.1..AC-1.5 → GEMINI audit → recomendar merge/push a Frank. (ARCH-20260820-01 Fase 1)
- **2026-08-19 (INTEGRA):** [IN_PROGRESS] **ARCH-20260819-02 — Tarjetas por prueba (prototipo no persistente) en Impresión y Aptitud.** Escalamiento de ATLAS desde DEC-20260819-03 (Frank: terminar visualmente Examen Médico antes de revisar Audiometría/Espirometría desde extracción). Dentro de la inner-tab "Impresión y Aptitud" de `ExamenMedicoEstudio` (sin mover navegación, DEC-20260819-02), añadir 3 tarjetas (Examen Médico, Audiometría, Espirometría) encima del consolidado existente (`LiveSummaryPreview` + selector de aptitud). Cada tarjeta: nombre, estado, resumen clínico, dictamen individual, acción ver detalle/papeleta; expandible; descargables `disabled` etiquetados "Disponible al validar resultado". **Hallazgo de factibilidad:** el modelo de datos ya soporta slots por prueba (`examen_medico_texto`/`audiometria_texto`/`espirometria_texto` en `physicalExamData`, leídos por `exam-summary.ts` y `verdict.builder.ts`); sólo falta la UI. **Decisión arquitectónica (ADR-20260819-03):** prototipo UI puramente presentacional, NO persistente, con fixtures anonimizados para Audiometría/Espirometría; la tarjeta Examen Médico consume el resumen real existente (`form`/`aptitud` read-only). Restricciones de privacidad: NO copiar PNG/PDF/DOCX/XLSX de `context/datos AMI/` a `public/`; NO persistir muestra en `MedicalExam`/`MedicalVerdict`; NO descargables funcionales ni ZIP; fixtures 100% sintéticos (sin PII); etiqueta obligatoria "Vista de muestra — no se guarda". Reversibilidad total (borrar 4-5 archivos + 1 import, 0 migración). Artefactos: `context/SPECs/SPEC_ARCH-20260819-02-TARJETAS-MUESTRA-POR-PRUEBA.md` (15 AC verificables), `context/decisions/ADR-20260819-03-TARJETAS-MUESTRA-POR-PRUEBA.md`, `context/interconsultas/HANDOFF_ARCH-20260819-02_SOFIA_TARJETAS-MUESTRA.md`. **Paralelismo:** 1 SOFIA secuencial (dependencia interna: `ExamenMedicoEstudio` importa `ResultadosPorPrueba`; conjuntos no disjuntos). **Autorización Frank:** delegación automática a SOFIA sin pausa; SOFIA deja commits locales SIN push; tras reporte, INTEGRA/GEMINI auditan y reportan. Pendientes fuera de alcance: entregables reales Audiometría/Espirometría (OQ-03), ZIP (OQ-02), condición dictamen integrado (OQ-01), aptitud sin Examen Médico (OQ-04 deferred). (ARCH-20260819-02)
- **2026-08-09 (INTEGRA):** [→] **ARCH-20260809-03 — Gestión runtime de API Keys de IA vía UI (sin env vars ni redeploys).** Escalamiento de ATLAS M3 desde necesidad explícita de Frank: endpoint UI para insertar/rotar/borrar API keys de proveedores IA (M3, Gemini, DR7/MedGemma) sin tocar env vars ni redeploys. INTEGRA redactó ADR + SPEC firmada + handoff a SOFIA. **Decisión de persistencia:** modelo dedicado `AIProviderKey` (tabla `ai_provider_keys`, un row por proveedor) con campos `keyCiphertext`/`keyNonce`/`keyTag`/`baseUrl`/`defaultModel`/`enabled`/`updatedBy`/`updatedAt` — descartada tabla plana/KV `SystemConfig` (acopla settings) y KMS/Vault externo (overkill single-tenant Railway). **Cifrado:** AES-256-GCM (cifrado autenticado) vía lib `cryptography`, con env var `ENCRYPTION_KEY` (32 bytes) que **solo existe en backend**; la UI jamás maneja la key descifrada. **Precedencia runtime:** BD gana si existe y `enabled=true`; env var como fallback (permite rollout incremental sin perder config actual). **Permisos:** PUT/DELETE solo SUPERADMIN (más conservador; Frank no especificó rol); GET listado mascareado ADMIN+SUPERADMIN. Guard real en server action (NextAuth + `isSuperAdmin`); backend defense-in-depth con header `x-ami-role` (patrón existente `maintenance.py:22`). **Rotación inmediata sin reinicio:** `KeyResolver` con caché TTL 60 s + invalidación en escritura (PUT/DELETE llaman `invalidate(provider)` tras commit); refactor obligatorio de `GeminiBase`/`M3VisionBase`/`FeatherlessVisionBase` (`__init__` cachea keys → mover a `call_*`) y de constantes de módulo en `prediagnostic.py:38-44` y `main.py:149-173/257-259`. **Audit:** reutiliza `AuditLog` existente con `action="ai_key_updated"`, `entity="AIProviderKey"`, `details.maskedKeySuffix` (últimos 4 chars) — nunca la key completa. **Trazabilidad:** `key_source:"env"|"db"` en `extraction_snapshot.audit` y `GET /api/v2/ai/status`. **Reversibilidad:** feature flag `AI_KEYS_FROM_DB_ENABLED` (default `false`) — flag off = comportamiento idéntico a hoy (env-var-only), cero riesgo en rollout. **Interconsulta DEBY:** la SPEC encargaba verificación previa del patrón de lectura de keys vía `task subagent_type='debugger'`; **el mecanismo `task` no está provisionado en esta sesión ni existe CLI `kilo`**, y `agent_manager` queda descartado por §14.7 (solo fan-out visible pedido explícitamente). INTEGRA ejecutó lectura focalizada sobre `base.py`, `prediagnostic.py`, `extractor.py`, `main.py` (lo indispensable para la decisión, no duplicación de ATLAS) y documentó el patrón en SPEC §1 / ADR §1.1; SOFIA debe re-verificar en su baseline (Fact-Forcing). **Hallazgo crítico:** las services cachean keys en `__init__`/import → sin el `KeyResolver` + refactor, la rotación runtime NO tomaría efecto sin reinicio. Entregables: `context/decisions/ADR-20260809-03-GESTION-API-KEYS-IA-RUNTIME.md`, `context/SPECs/SPEC_ARCH-20260809-03-MANAGE-AI-API-KEYS.md` (14 AC verificables, 13 casos borde), `context/interconsultas/HANDOFF_ARCH-20260809-03_SOFIA_MANAGE-AI-API-KEYS.md`. Alcance SOFIA: ~9 archivos backend (3 nuevos: `keys.py`, `test_ai_keys.py`, migración) + ~7 frontend (4 nuevos: `ai-keys.actions.ts`, `page.tsx`, `AIProviderKeyManager.tsx`, `types/ai-keys.ts`). Gates: typecheck/vitest/lint/pytest + GEMINI segunda mano (Qodo sunset). Pendiente: OK implícito para delegar a SOFIA (delegación es acción por defecto per §4). (ARCH-20260809-03, SPEC READY)
- **2026-08-09 (INTEGRA):** [✓] **IMPL-20260809-04 cerrado y pusheado a `main` (commit `99dc46c`).** **ARCH-20260809-02 — Selector de extracción multi-proveedor (Gemini + MiniMax M3).** Escalamiento de ATLAS M3 desde necesidad explícita de Frank: mejorar la capa de extracción documental del pipeline IA con un selector runtime de proveedor (`gemini` | `m3`) + modelo configurable, manteniendo el prediagnóstico clínico (MedGemma/DR7) **intacto** por decisión de Frank. Decisión arquitectónica resuelta por INTEGRA (confianza ≥85%, interna y reversibles): **(1)** selector **global por prueba** en `aiCalibration.extraction` (`provider`, `model` opcionales) **+ override opcional por payload** en `/api/v2/studies/upload-and-analyze` (A/B sin redeploys); **(2)** cliente M3 reutiliza el patrón OpenAI-compatible de `FeatherlessVisionBase` (`backend/app/services/ai/base.py:206-345`); **(3)** fallback **unidireccional M3→Gemini** ante 5xx/timeout/4xx-persistente (excluyendo 401/403 que son `M3_AUTH_ERROR` explícito) y caso especial `m3_not_configured` si `M3_API_KEY` ausente; **(4)** trazabilidad completa: `extraction_provider_requested/used`, `extraction_model_used`, `extraction_fallback_reason` (nullable) en `extraction_snapshot.audit` y `/api/v2/ai/status`; **(5)** Gemini sigue siendo default global de proceso (`extraction_provider_active="gemini"` sin cambio); **(6)** migración legacy implícita (lectura defensiva: `provider` ausente → `"gemini"`, sin script). Env vars nuevas: `M3_API_KEY`, `M3_BASE_URL` (default confirmado contra docs oficiales: `https://api.minimax.io/v1`), `M3_DEFAULT_MODEL` (default confirmado: `MiniMax-M3`, case-sensitive, 1M context, multimodal texto+imagen+video). Incógnitas a resolver por SOFIA: gestor de paquetes frontend (npm/pnpm). Justificación "¿por qué no solo M3?": un único proveedor extractivo es punto único de fallo — la lección de `ARCH-20260519-15` (rollback Featherless→Gemini por `503 capacity_exhausted`). ADR: `context/decisions/ADR-20260809-02-EXTRACCION-MULTI-PROVEEDOR-M3-GEMINI.md`. SPEC firmada: `context/SPECs/SPEC_ARCH-20260809-02-SELECTOR-EXTRACCION-MULTI-PROVEEDOR.md` (15 criterios de aceptación verificables CA-01..CA-15, 9 casos borde CB-01..CB-09). Handoff SOFIA: `context/interconsultas/HANDOFF_ARCH-20260809-02_SOFIA_SELECTOR-EXTRACCION.md`. Superficie: backend `base.py`+`extractor.py`+`main.py`(env vars 151-164, status 584-623, upload-and-analyze 1085-1257, calibración test)+`schemas/medical.py`(432-439, 584-593)+`test_ai_pipeline.py`; frontend `AICalibrationEditor.tsx`(233-290)+`types/calibration.ts`(131-137, 160-166)+`lib/calibration-schema.ts`+`CalibrationTestResults.tsx`+tests vitest. No toca: `prediagnostic.py`, `BASE_EXTRACTION_PROMPT`, schema Prisma, migraciones DB. Gates: `pytest backend/tests -v`, frontend typecheck/test/lint, test manual A/B + fallback. **IMPLEMENTADO** por SOFIA (commit `99dc46c`, 13 archivos / +1623/-25). Cliente `M3VisionBase` (OpenAI-compatible, `base.py`), dispatcher `_resolve_provider()` con fallback unidireccional M3→Gemini (`extractor.py`), UI `AICalibrationEditor` + `CalibrationTestResults`. Gates pytest 84/84, typecheck 0, lint 0 nuevos. GEMINI QA-20260809-04 APPROVED_WITH_OBSERVATIONS. Push `4b901aa..99dc46c main -> main` tras OK Frank. **Pendiente operativo**: confirmar `M3_BASE_URL` y nombre exacto del modelo contra el plan Pro de MiniMax (env vars ajustables sin tocar código). (ARCH-20260809-02 cerrado)
- **2026-08-09 (INTEGRA):** [✓] **IMPL-20260809-02 + IMPL-20260809-03 cerrados y pusheados a `main` (commit `4b901aa`).** Ciclo completo `ARCH-20260809-01`: outer-tab → sub-pestaña + botón "Continuar → Módulo 1". Frank aprobó ambas decisiones: (a) "Antecedentes" como **primera sub-pestaña dentro de "Examen Médico"** (no outer-tab separada); (b) incluir botón "Continuar → Módulo 1" al pie (nit de SPEC §6.9 detectado por GEMINI, corregido en micro-rework `IMPL-20260809-03`). Persistencia integrada en `saveExamenMedicoPapeleta` (eliminado `saveAntecedentesCaptura`; `AntecedentesCaptura` convertido a controlado `value`/`onChange`). Cambios: 5 archivos código (4 v2 + 1 amend v3) + 1 SPEC v2 + 1 ADR (mantenido) + 1 handoff + PROYECTO.md. Defensas conservadas: I-1 (`form` init filtra no-primitivos) + I-2 (`stripEmptyEnumKeys` aplica `''`→`undefined` en DP y No Patológicos). QA: `QA-20260809-01` PASS_WITH_NITS (v2), `QA-20260809-02` PASS (v3). Gates: typecheck 0, vitest 444/444, lint 0 nuevos errores. Push `a1b2f44..4b901aa main -> main` exitoso tras OK explícito de Frank. Frank validó en Vercel prod: UX más limpio, sub-pestaña accesible, sin amontonamiento. **Pendiente menor** (registrado en BACKLOG): al guardar Examen Médico completo, el panel "Trazabilidad IA" muestra error HTTP 422 del backend (`"detail":[{"type":"missing","loc":["query","study_type"],"msg":"Field required","input":null}]`). Frank indica que NO se atenderá todavía — solo queda registrado para iteración futura. (IMPL-20260809-02 + IMPL-20260809-03, ARCH-20260809-01 cerrado)
- **2026-08-09 (INTEGRA):** [~] **SPEC v2 `ARCH-20260809-01` — Antecedentes pasa de outer-tab a sub-pestaña de Examen Médico.** Frank **rechazó** la decisión de UX de v1 (`IMPL-20260809-01`, commit `a1b2f44`, ya en `main`) tras ver el resultado en producción: "Antecedentes" como 5ª outer-tab separada no convenció. Decisión nueva (aprobada): Antecedentes como **primera sub-pestaña dentro de "Examen Médico"** (al lado de Módulo 1, Exploración Física, Impresión/Aptitud). INTEGA redactó SPEC v2 `context/SPECs/SPEC_ARCH-20260809-01-ANTECEDENTES-SUB-PESTANA-EXAMEN-MEDICO.md` (v1 marcada SUPERSEDED con banner); resolvió las 4 preguntas abiertas: **(P1)** persistencia **integrada** en `saveExamenMedicoPapeleta` (se elimina `saveAntecedentesCaptura`, reduce superficie; `buildPayload()` ahora incluye `antecedentes_captured`, revertir defensa I-1); **(P2)** banner ámbar revertido a condición original (sin excluir `'antecedentes'`); **(P3)** tests: eliminar 13 tests del action eliminado, conservar 5 de schemas, añadir cobertura de `saveExamenMedicoPapeleta` con `antecedentes_captured`; **(P4)** nuevo commit `IMPL-20260809-02` (no `git revert`, preserva trazabilidad del rework). ADR `ADR-20260809-01` **se mantiene** en su decisión de datos (snapshot por cita en `physicalExamData.antecedentes_captured`); se añadió nota de revisión SPEC v2 que **supede el punto 4** ("action autónoma sin IA") — ahora antecedentes persiste vía `saveExamenMedicoPapeleta`, que dispara IA y cambia status a RESULT_REGISTERED, consistente con Módulo 1 (aceptable: antecedentes es parte del flujo del examen, no outer-tab independiente). Handoff a SOFIA: `context/interconsultas/HANDOFF_ARCH-20260809-01_v2_SOFIA_ANTECEDENTES-SUB-PESTANA.md`. Alcance: 4 archivos modificados + 1 test (menos superficie que v1: reutiliza helper/schema/loader de v1, solo refactoriza `ExamenMedicoEstudio` + `AntecedentesCaptura` a controlado + elimina action). Estado: **SPEC READY**. Pendiente OK explícito de Frank para delegar a SOFIA. No commit/push. Modo nocturno NO permitido (cambio de decisión con impacto UI). **Estado final:** `IMPL-20260809-02 + IMPL-20260809-03` cerrados y pusheados a `main` (commit `4b901aa`) — ver entrada previa [✓]. (ARCH-20260809-01 cerrado)
- **2026-08-05 (INTEGRA):** [IN_PROGRESS] **FIX-20260805-04 — Draft autosave (persistencia navegador) para SelfRegistrationForm.** Escalamiento de ATLAS M3 desde necesidad explícita de Frank: *"agrega persistencia al menos del navegador por si no completan los datos, cambia de venta, se les cierra, etc"*. El formulario público de auto-alta (`/solicitar-alta` y `/auto-alta/[token]`, componente `frontend/src/components/companies/SelfRegistrationForm.tsx`) NO persiste nada entre cierres. INTEGRA resolvió las 6 decisiones arquitectónicas abiertas (todas con confianza ≥85%, internas y reversibles, sin re-preguntar a Frank según §2): (1) Storage=localStorage (cubre "se les cierra"), (2) Alcance=Opción B (~350 líneas: uploads metadata + modal + TTL + versionado + keys separadas; NO cifrado AES, NO cleanup S3), (3) UX banner=modal bloqueante con preview, (4) TTL=30 días, (5) Multi-form=keys separadas por source+scope, (6) Uploads=solo metadatos, re-vincular al submit. SPEC firmada en `context/SPECs/SPEC_FIX-20260805-04-DRAFT-AUTOSAVE-SELF-REGISTRATION.md` (14 criterios verificables, 13 casos borde). 5 archivos: 4 nuevos (`self-reg-draft.ts`, `useSelfRegDraft.ts`, `DraftRestoreModal.tsx`, `__tests__/self-reg-draft.test.ts`) + 1 modificado (`SelfRegistrationForm.tsx` +~40 líneas). NO toca schema Prisma, backend, server actions ni server components. Delegado a SOFIA con handoff estructurado; GEMINI auditará tras implementación (Qodo sunset). Espera OK Frank para commit/push. (FIX-20260805-04)
- **2026-07-30 (INTEGRA):** [✓] **IMPL-20260730-01 — Eliminación masiva de empresas (SUPERADMIN) operativo en producción.** Frank solicitó 2026-07-30 04:47 un modal para eliminar empresas con multi-select y rol superadmin. Decisión bloqueante resuelta con Frank: añadir `SUPERADMIN` al enum `UserRole`, multi-select con checkboxes + barra flotante, modal con checkbox de aceptación + botón rojo. SPEC firmada en `context/SPECs/SPEC_ARCH-20260730-01-DELETE-COMPANIES-SUPERADMIN.md`. Implementación SOFIA (retry de BLOCKED inicial): nueva migración `20260730000000_add_superadmin_role` con `ALTER TYPE UserRole ADD VALUE 'SUPERADMIN'` + `DROP NOT NULL` en `appointments.companyId` / `job_positions.companyId` / `projects.companyId` (preservar historia clínica); service `deleteCompanies` con transacción de 14 pasos en `prisma.$transaction` (nullify → delete → audit log); server action `deleteCompaniesAction` con guard `role === 'SUPERADMIN'` + validación Zod-like (no vacío, max 100); UI client-only `CompanySelectableGrid` + `DeleteCompaniesButton` + `CompanyBulkDeleteShell`; render condicional según rol en `/companies/page.tsx` (no rompe experiencia para ADMIN/VENDEDOR); 7 tests unitarios del service. **Ajustes defensivos en cascada** por la nulificación de FKs: `project.actions.ts`, `ProjectFormModal`, `ProjectsCalendar`, `ProjectsTable`, `JobPositionsPanel`, `WorkerFormModal`, `WorkersTable`, `company.service.test.ts` (mock de `prisma.user.findUnique` por FIX-20260730-01). **Gates verificados independientemente**: typecheck 0 errores, vitest 280/280 (+7 nuevos), lint 0 errores. **GEMINI auditoría: LISTO PARA FRANK** (0 bloqueadores, 1 observación informativa sobre paridad de schemas). **Commit + push realizados** tras OK explícito de Frank vía `ask-frank.sh` (opción A: "Sí commit+push ahora"). Push `6bb88fe..2e2aba4 main -> main` exitoso. **Migración aplicada + SUPERADMIN asignado** tras OK explícito de Frank (opción A) ejecutado 2026-07-30 05:43 vía script one-off Node conectado a Railway DB: SUPERADMIN añadido al enum, 3 FKs nullable, admin@sistema.com actualizado a SUPERADMIN (id `b2bdf4d7-6094-40c9-bfd6-8a63be0cbc67`). Frank debe refrescar `/companies` (logout + login si es necesario para refrescar JWT) y verá los checkboxes por tarjeta + barra flotante con botón Eliminar. (IMPL-20260730-01 operativo)
- **2026-07-30 (INTEGRA):** [~] **FIX-20260730-01-LAB-TRIGGER implementado pero introduce regresión TC-07; estado final 10/12 + TC-11 FAIL esperado.** Bug raíz encontrado: `LAB_CATEGORY_ID = "64d3f863"` (string corto legacy) hardcoded en 6 lugares, mientras la categoría real en BD es UUID `16c16ef0-cf35-4fe5-9bef-311f6fc8674c`. Fix `72dc596` reemplazó el hardcode por UUID real pero **introdujo regresión en TC-07** (data loader de `events/[id]/page.tsx` lanza excepción capturada por `error.tsx` boundary con mensaje "Error al cargar el expediente"). Estado actual tras investigación: revert de `72dc596` (`ae8a2d7`) + reactivación de TC-11 (`12d0427`). E2E contra prod: **TC-01..TC-10 PASS (10/12), TC-11 FAIL por LAB_CATEGORY_ID mismatch (esperado), TC-12 NOT RUN**. SPEC de follow-up creada: `context/SPECs/SPEC_FIX-20260730-02-G-LAB-TRIGGER-02-REGRESION.md` cubre investigación de la regresión. **Pendiente decisión Frank**: (a) mantener estado actual 10/12 estable y abrir lote de investigación de la regresión, (b) intentar reaplicar `72dc596` + diagnosticar regresión con logs, (c) cerrar lote actual y abrir dos SPECs de follow-up separadas. Recomendación: opción (a) por estabilidad — 10/12 PASS sigue siendo progreso significativo vs iteración anterior. Historial de commits esta sesión: `72dc596` LAB fix → `90a6ec8` TC-11 reactivado → `132fed3` revert TC-11 → `ae8a2d7` revert 72dc596 → `12d0427` reaplica TC-11. (FIX-20260730-01 + regresión)
- **2026-07-29 (INTEGRA):** [~] **Frank aprobó `FIX-20260729-03-G-XML-01-PARSER-XML.md`; se inicia lote.** Aprobación registrada 2026-07-29 07:53 CST. Alcance: detectar XML de audiometría antes del pipeline IA, invocar `audiometry_xml_parser.py` directo (<100 ms, 0 tokens), persistir `fileUrl`/snapshot compatible con `EventTest`, renderizar 16 mediciones + PTA por oído, disparar prediagnóstico posterior (DR7.ai) por separado. Se preserva contrato `1e7265d` (XML > PDF). Estimación ~4 h. Handoff a SOFIA incluye archivos candidatos: `frontend/src/actions/event-test.actions.ts`, `backend/app/services/event_service.py` (o `backend/app/api/v1/event_tests.py`), extractor V2 Gemini y `audiometry_xml_parser.py`. Reejecución de TC-09..TC-12 obligatoria para desbloquear suite. Gates verdes deben preservarse. (FIX-20260729-03-G-XML-01)
- **2026-07-29 (INTEGRA):** [~] **FIX-20260729-03-G-XML-01 implementado y auditado; pendiente OK explícito de Frank para commit + push.** SOFIA implementó el parser XML directo en `uploadEventTestFile` con detección en 3 capas (MIME + extensión + magic bytes) y nuevo endpoint backend `POST /api/v2/event-tests/upload-xml-audiometry` que reusa `parse_audiometry_xml` sin modificarlo. Archivos tocados: `backend/app/main.py` (+336) y `frontend/src/actions/event-test.actions.ts` (+368/-1). NO se modificaron `audiometry_xml_parser.py`, `calibration.py`, schema Prisma ni `PapeletaWorkspace.tsx`. Gates frontend verdes (typecheck 0, vitest 273/273, lint 0). Parser XML verificado: `JESSICA GABRIELA.xml` → OD PTA=67 / OI PTA=83 (matches SPEC §6). Snapshot persiste `data_source: "xml_direct"`, `model_name: "xml_parser"`, `prompt_version: "xml_direct_v1"`. Manejo defensivo: si parser falla → 400 + cleanup; si IA caída → snapshot con `clinical_state: AI_PENDING_REVIEW` y extracción XML persistida. GEMINI APROBADO con verificación de integridad de archivos protegidos y análisis de race conditions. E2E local: TC-01..TC-08 PASS, TC-09 FAIL (esperado: prod no desplegado), TC-10..TC-12 NOT RUN por serial. Pendiente: respuesta de Frank vía Telegram (consulta `FIX-20260729-03-COMMIT-PUSH` clasificación ROJO, timeout 600s). Bajo metodología INTEGRA no se ejecuta commit/push sin OK explícito. (FIX-20260729-03-G-XML-01 implementado, pendiente OK Frank)
- **2026-07-29 (INTEGRA):** [✓] **IMPL-20260729-02 (Gap G2 + Gap G3) cerrado con commit `f4a60dc`.** Suite E2E `frontend/tests/flujo-completo.spec.ts` corregida con selectores accesibles reales contra producción: TC-01 empresa + RUN_TAG, TC-02 perfiles con `getByRole('button', name: '+ Nuevo Perfil')` + checkboxes accesibles, TC-03 puestos con `Crear Puesto`, TC-04 trabajadores con campos requeridos completos (fecha, género, empresa, puesto, email, teléfono) + datos únicos RUN_TAG para evitar colisiones en BD persistente, TC-05 citas con `AppointmentFormModal`, TC-06 check-in desde `/appointments` con corroboración de identidad, TC-07 papeleta con `PapeletaWorkspace` real, TC-08 examen con pestañas y placeholders clínicos reales, TC-09/10 subida IA con `input[type=file]`, TC-11 LAB con `Registrar muestra tomada`, TC-12 dictamen con `EventFlowController`. **Resultado E2E contra `BASE_URL` productiva:** TC-01..TC-08 PASS (8/8 verificados); TC-09 FAIL — gap funcional **G-XML-01** detectado (parser XML directo de audiometría no se invoca; upload entra al pipeline Gemini con HTTP 400); TC-10..TC-12 NOT RUN por serial gating de TC-09. NO se convirtió el gap en `test.skip`: queda documentado con evidencia y SPEC de follow-up `context/SPECs/SPEC_FIX-20260729-03-G-XML-01-PARSER-XML.md`. Gates frontend verdes (typecheck 0, vitest 273/273, lint 0). Único archivo de producto modificado: `frontend/tests/flujo-completo.spec.ts` (+346/-296). Push `efd26fe..f4a60dc main -> main` exitoso. Frank fue notificado vía Hermes Bridge (clasificación VERDE → decisión autónoma procedió con promoción del progreso válido + SPEC follow-up). Pendiente para Frank: aprobar lote `FIX-20260729-03-G-XML` cuando guste. (IMPL-20260729-02 cerrado con G-XML-01 documentado)
- **2026-07-29 (INTEGRA):** [✓] **FIX-20260729-01-BASELINE: baseline frontend 3 gates a 0 publicado en `main` (commit `efd26fe`).** Tras instrucción de Frank "Corregir baseline primero" con gating estricto, se abrió tarea de remediación que llevó `npm run typecheck` (30 → 0), `npm test` (12 fallos → 273/273) y `npm run lint` (95+65 → 0/0) a verde. Cambios: 60 archivos tracked modificados + 4 nuevos (`maintenance.helpers.ts`, `events/[id]/error.tsx`, `events/[id]/_lib/event-page-data.ts`) + 2 eliminados (`.eslintignore`, `types/vitest.d.ts`). Decisiones críticas: extracción real de `react-hooks/error-boundaries` con `error.tsx` + `_lib/event-page-data.ts`; `react-hooks/set-state-in-effect` documentado con disables inline justificados (25 casos); `no-unused-vars` global con `_`-prefix; overrides de ESLint restringidos a `tests/**`/`__tests__/**`/`scripts/**`. Snapshots en `context/audits/baseline-20260729-{before,after}-{typecheck,vitest,lint}.{txt,json}`. Sin cambios en backend, schema Prisma, migraciones ni comportamiento de producto. Gates verificados independientemente por INTEGRA tras la entrega de SOFIA. IMPL-20260729-02 desbloqueado tras gates verdes. (FIX-20260729-01-BASELINE)
- **2026-07-29 (INTEGRA):** [~] **Plan IMPL-20260729-02 G2/G3 bloqueado por baseline; se abre tarea de remediación FIX-20260729-01-BASELINE.** Frank instruyó **"Corregir baseline primero"** con gating estricto (IMPL-20260729-02 queda bloqueado hasta los 3 gates en verde). Baseline reproducida: typecheck 30 errores (28 preexistentes en tests + 2 locales en `tests/flujo-completo.spec.ts`), vitest 12 fallos concentrados en `src/actions/__tests__/maintenance.helpers.test.ts` (`calculateNextDueDate is not a function` por export faltante/renombrado), eslint 95 errores + 65 warnings en 59 archivos (reglas dominantes: `react-hooks/error-boundaries` 37, `react-hooks/set-state-in-effect` 25, `no-unused-vars` 46, `no-explicit-any` 16, `no-unescaped-entities` 10). SPEC firmada en `context/SPECs/SPEC_FIX-20260729-01-BASELINE.md`; baseline snapshots en `context/audits/baseline-20260729-before-{typecheck,vitest,lint}.{txt,json}`. Estrategia: 0 errores en los 3 gates, manteniendo contratos sin cambios funcionales ni regresiones. Se delega a SOFIA con handoff completo (FIX-20260729-01-BASELINE, Gating estricto respecto a IMPL-20260729-02). Plan original IMPL-20260729-02 continúa a la espera de baseline verde. (FIX-20260729-01-BASELINE, sigue IMPL-20260729-02 plan)
- **2026-07-29 (INTEGRA):** [✓] **Flujo E2E especificado y autenticación desbloqueada.** Frank solicitó validación end-to-end completa. Entregables: (1) SPEC `context/SPECs/SPEC_IMPL-20260729-FLUJO-END-TO-END.md` (450+ líneas); (2) Suite E2E `frontend/tests/flujo-completo.spec.ts` (~500 líneas, 12 test cases); (3) Handoff SOFIA documentado. **Bloqueo resuelto:** Credenciales admin actualizadas a `admin@sistema.com` / `Admin@2026!` via Railway. Login verificado 100% exitoso. **Estado actual:** Login funciona ✅, selectores UI de formularios empresa/trabajador requieren ajuste (TC-01, TC-04 fallan). Reporte completo en `context/checkpoints/CHK_IMPL-20260729-E2E-FINAL.md`. Próximo paso: SOFIA debe corregir selectores UI y ejecutar tests completos para identificar gaps funcionales (triggers EventTests, LabOrder, componente dictamen). (IMPL-20260729-E2E)

- **2026-07-20 (INTEGRA):** [✓] **Cierre ARCH-20260715-06: Extracción directa desde XML de audiómetro DD65 V2.** Parser XML directo sin IA para valores exactos de audiometría. Backend: `audiometry_xml_parser.py` identifica oídos por SignalOutput (ACL/ACR/BCL/BCR), calcula PTA, extrae 8 frecuencias por oído (250-8000 Hz). Endpoint de calibración modificado para aceptar PDF o XML con prioridad: XML (parser directo, <100ms, 100% exacto, 0 tokens) > PDF (IA calibrada, 5-10s, ±5 dB, ~2000 tokens). Frontend actualizado para ambos formatos. Commit `1e7265d` push exitoso a `origin/main`. SPEC: `context/SPECs/SPEC_ARCH-20260715-06-EXTRACCION-DIRECTA-XML-AUDIOMETRO.md`. Backup: `context/interconsultas/CALIBRACION-AUDIOMETRIA-2026-07-15.md`. Checkpoint: `context/checkpoints/CHK-20260720-1927-CIERRE-XML-PARSER.md`. Pruebas: XML de Jessica Gabriela extraído correctamente (8 frecuencias, PTA OD=67 dB, OI=83 dB). Pendiente: probar sistema completo en runtime (iniciar backend/frontend). (ARCH-20260715-06)

- **2026-07-08 (INTEGRA):** [✓] **CIERRE COMPLETO del módulo sustituto de NOVA: 7/9 slices cerrados.** Frank instruyó "procede con todo, intenta terminar todo el módulo sustituto de NOVA". Se implementaron las 4 fases finales en un solo turno con SOFIA (luego SOFIA se quedó sin créditos y yo (INTEGRA) cerré manualmente con commits + merges). Fases: **Fase 1 (B-v2 + E):** Re-arquitectura `/lab/reception` con bandeja de papeletas + trigger automático al cambiar `EventTest.status` a `SAMPLE_TAKEN` + editor avanzado de MedicalTest + seed de 5 estudios típicos (BH, QS, EGO, Perfil Lipídico, TP) con 34 analitos y 40 rangos. **Fase 2 (D + C-update):** Modelo `LabTraceEvent` (muestra→proceso→entrega) + componente `LabTraceTimeline` integrado en `/lab/results/[orderId]` + auto-record SAMPLE_RECEIVED al confirmar LabOrder + integración LabResult↔EventTest. **Fase 3 (F + G):** 3 endpoints PDF (etiquetas, resultados, recibos) con reportlab + 3 botones de impresión + schema `LabCashMovement` + `Courtesy` + páginas `/lab/cash` y `/lab/cash-closing`. **Fase 4 (H + I):** Scripts Python de migración NOVA (`migrate_nova.py` con --dry-run/--persistent-only/--operational, `sync_nova_metadata.py`, `validate_migration.py`) + UI `/admin/lab/migration` + UI `/admin/lab/cutover` con checklist de 9 fases + banner "NOVA deprecado". Verificación end-to-end con Playwright: Folio 1 creado vía trigger SAMPLE_TAKEN desde papeleta, admisión auto-llenada, confirmación → SAVED + SAMPLE_RECEIVED en trazabilidad. **Métricas finales:** 12+ commits a main, 27+ migraciones Railway aplicadas, 90+ pytest backend + 250+ vitest frontend todos verde, 14+ modelos LIS en Prisma schema, 10+ enums, 27+ endpoints REST, 11+ páginas frontend, 30+ componentes UI. **Estado del roadmap:** 7 de 9 slices cerrados (A, B-v2, C, D, E, F, G); Slice H Parcial (script listo, bloqueado por falta de dump NOVA); Slice I En curso (UI lista, falta comunicación a stakeholders). **Acciones pendientes para Frank:** (1) conseguir dump NOVA y correr migración Slice H, (2) coordinar cutover con Lolis/Leticia/Dra. Erika para Slice I, (3) notificar a NOVA para eliminar usuario FRANCISCO (comprometido en audit inicial). ID: `IMPL-20260708-FINAL`. Checkpoint: `context/checkpoints/CHK_ARCH-20260708-FINAL.md`. SPEC consolidado: `context/SPECs/SPEC_IMPL-20260707-SLICES-D-G-FINAL.md` (243 líneas). Docs migración: `context/SPECs/MIGRATION-NOVA-MAPPING.md` (156 líneas). SQL consolidado: `context/infra/{09,10,11}-migration-20260708-*.sql`. CONF-20260707-01 y CONF-20260707-02 con modelo operativo de Frank confirmado.
- **2026-07-08 (INTEGRA):** [✓] **Cierre del Slice C NOVA Resultados + integración papeleta AMI:** 4 modelos Prisma nuevos (LabAnalyte, LabReferenceRange, LabResult, LabResultAudit) + 3 enums (LabResultStatus, LabAnalyteDataType, LabSex) + 1 columna nueva (eventTestId en lab_order_items) + 8 back-relations. 9 endpoints REST backend (`/api/v1/lab/results` GET/POST/PATCH + `/report` `/authorize` `/validate` `/invalidate` + `/worklist`). 7 server actions frontend. 2 páginas nuevas (`/lab/results` lista con tabs por estado, `/lab/results/[orderId]` worklist con inputs + ciclo P/R/A/V + auditoría) + sección "Laboratorio" en `/events/[id]` (papeleta) + selector de MedicalEvent en `/lab/reception`. **Integración bidireccional papeleta ↔ NOVA lograda:** `LabOrder.medicalEventId` (ya existía) + `LabOrderItem.eventTestId` (NUEVO) + `LabResult.eventTestId` (NUEVO). Validación visual contra rangos de referencia (verde/amarillo/rojo) con flags `isOutOfRange`/`isCritical`/`isAbnormal`. Bitácora inmutable con snapshots before/after en JSON. 53/53 pytest backend + 212/212 vitest frontend verde. Migración `20260707120000_add_lab_results` aplicada en Railway (22/22 finalizadas, 4 tablas nuevas, 3 enums nuevos, columna eventTestId en lab_order_items). Issue detectado: columna `medicalTestId` faltante en DB al inicio, resuelto con ALTER TABLE manual. Branch `feature/lab-slice-c-results` mergeada. Commit `d37e4c2`. ID: `IMPL-20260708-01`. Checkpoint: `context/checkpoints/CHK_ARCH-20260708-SLICE-C.md`. SQL: `context/infra/09-migration-20260708-lab-results.sql`. SPEC: `context/SPECs/SPEC_IMPL-20260707-SLICE-C-RESULTADOS.md` (458 líneas).

- **2026-07-07 (INTEGRA):** [✓] **Cierre nocturno completo: Slice 1 (UI refactor + data loading) + Slice 2 (fix raíz Prisma Python) + limpieza de 10 branches mergeadas.** Backend FastAPI 100% funcional (verificado con curl: 10 unidades + 5 muestras + 38 pytest + 17 runtime tests). Frontend Vercel con bug de syntax (`};` duplicado en lab-catalog.actions.ts:66) detectado en logs, resuelto (commit `55aedeb`). 8 mods LIS cargan datos reales (43 items). Total: 47 commits en main. ID: `IMPL-20260707-09` (varios). Checkpoint: `context/checkpoints/CHK_ARCH-20260707-CIERRE-NOCTURNO.md`.

- **2026-07-01 (INTEGRA):** [✓] **Cierre del Slice B NOVA admisión: 2 modelos Prisma nuevos (LabOrder, LabOrderItem) + 3 enums + 6 back-relations, 11 endpoints REST + 4 autocomplete, 12 server actions + 7 componentes UI en `/lab/reception`, sidebar "🧬 Recepción Lab", 15/15 pytest backend + 19 nuevos vitest frontend verde, GEMINI APROBADO_CON_OBSERVACIONES, 0 bloqueadores. Conexión operativa lograda: LabOrder ⟶ Worker (FK) + MedicalEvent (FK opcional) + Company (FK opcional) + MedicalTest (catálogo estudios) + LabClassification. Merge commit `45cf045` a main. Migración `20260701010000_add_lab_orders` aplicada en Railway verificada (21/21 migraciones, 3 enums, 2 tablas, conteos en 0). ID: `IMPL-20260701-03`. Checkpoint: `context/checkpoints/CHK_ARCH-20260701-SLICE-B.md`.
- **2026-07-01 (INTEGRA):** [✓] **Migración Slice A NOVA en Railway PostgreSQL completada.** IMPL-20260630-06 aplicada directamente en Railway vía `prisma db execute`: 4 versiones SQL (digest → md5 → checksum → v4 final con md5+nativo). 20/20 migraciones en `_prisma_migrations` incluida `20260701000000_add_lab_catalogs`. 10 tablas LIS creadas: lab_units, lab_samples, lab_containers, lab_methods, lab_process_areas, lab_departments, lab_classifications, lab_indications, lab_signatures, _LabContainerDefaultFor (+ lab_records preexistente). 6 enums: LabUnitSystem (SI/CONVENTIONAL), LabRole (4 valores). 11 columnas extendidas en users/companies/medical_tests. Seed ejecutado OK: 10+5+5+5+5+5+5+3 = 43 items. Verificación: 20/20 ✓ migraciones, 10/10 tablas, 6/6 enums, 11/11 columnas. Demo funcional en `/admin/lab/catalogs?mod=unidades` (smoke test Frank pendiente). Slice B (`/lab/reception` admisión) siguiente. Commits: `13f7c4b`, `ca3d754`, `12f883d`, `60ec234`, `99c30d2`, `b2b2c98`.

- **2026-06-30 (INTEGRA):** [✓] **Slice A NOVA cerrado: 8 catálogos LIS (unidades, muestras, recipientes, métodos, lugares, clasificaciones, indicaciones, departamentos) end-to-end con schema Prisma + FastAPI + Next.js + seed + tests.** Slice A NOVA cerrado: commit `9c235314c51a46d2ae1238555297bd0c42a38301` en branch `feature/lab-slice-a-catalogs` con push OK. 23/23 pytest backend, 162/162 vitest frontend (26 nuevos del slice). GEMINI auditoría: APROBADO_CON_OBSERVACIONES (0 bloqueadores). Slices pendientes: B-I. Sin merge a main (esperando OK de Frank al regreso). Pendiente: notificar a NOVA eliminación usuario FRANCISCO (comprometido durante auditoría). ID de intervención: `IMPL-20260630-06`. Checkpoint: `context/checkpoints/CHK_ARCH-20260630-02-SLICE-A.md`.
- **2026-06-30 (INTEGRA):** [✓] **Se cierra ARCH-20260630-02: auditoría forense NOVA + ADR estrategia + SPEC demo Slice A.** Frank ejecutó instrucción "hasla completa de acuerdo generas un .md … para que sofia construya un demo funcional". Auditoría completa con Playwright + curl sobre `https://sem.novaconnection.mx/i` (credenciales temporales provistas por Frank; recomendación: rotar inmediatamente). Se mapearon los 45 módulos NOVA en 5 grupos (Captura 8, Catálogos 19 + Estudios 6, Operación 4, Reportes 4, Configuración 4) con HTML descargado de cada uno en `context/audits/nova-20260630/pages/`. Stack detectado: PHP/Apache + jQuery + Bootstrap 3 + AdminLTE + Vue 2.6 + Vuetify + DataTables server-side + axios, auth por cookie `PHPSESSID`. Modelo de datos inferido (26 entidades) en `AUDIT-NOVA-COMPLETO.md` §6 con cardinalidades marcadas como hipótesis. Hallazgo crítico: AMI **no tiene módulo dedicado de laboratorio**; las pruebas LIS viven como `MedicalTest.type='laboratorio'` dentro del flujo clínico unificado, sin workspace propio, sin flujo de toma de muestra, sin captura masiva ni impresión especializada. Estrategia adoptada (DA-2): los modelos NOVA se **extienden a AMI**, no se replican 1:1 — se crean 9 entidades nuevas (`LabUnit`, `LabSample`, `LabContainer`, `LabMethod`, `LabProcessArea`, `LabDepartment`, `LabClassification`, `LabIndication`, `LabSignature`) en Slice A; resto en B-I. Roadmap de 9 slices (A=catálogos, B=recepción, C=resultados, D=trazabilidad, E=estudios, F=reportes, G=caja, H=migración datos, I=cutover). **Política de datos confirmada 2026-06-30 22:16 CST** por Frank: persistentes = **TODO** (todos los catálogos); operativos = **solo último mes desde 2026-05-31**; sin acceso a BD NOVA → se mantiene inferencia; Frank solicita a NOVA eliminar usuario `FRANCISCO` al cerrar la auditoría. ADR §3 actualizado con tabla de política explícita. Demo Planificado (no obligado): Slice A entrega 8 catálogos editables en `/admin/lab/catalogs?mod=unidades` con seed, endpoints FastAPI REST modernos (no URLs planas NOVA), server actions Zod, sidebar AMI extendido, banner demo, tests pytest ≥ 12 + vitest ≥ 80% coverage. Estimación Slice A: 16h. **Estado y entregas:**
  - `context/audits/nova-20260630/AUDIT-NOVA-COMPLETO.md` (informe forense)
  - `context/decisions/ADR-20260630-02-ABSORCION-NOVA-ESTRATEGIA.md` (9 decisiones arquitectónicas + roadmap + política de datos)
  - `context/SPECs/SPEC_ARCH-20260630-02-DEMO-NOVA-ABSORBIDO.md` (Slice A ejecutable)
  - `context/interconsultas/HANDOFF_ARCH-20260630-02_SOFIA_DEMO-NOVA-SLICE-A.md` (listo para delegar)
  AMI productivo intacto, 0 cambios en código AMI, 0 commits en esta sesión. (ARCH-20260630-02)

- **2026-06-30 (INTEGRA):** [~] **Se planifica ARCH-20260630-01 v2: PDF Ebook reemplaza carpeta física por proyecto.** Frank identifica que Lolis actualmente arma manualmente carpetas físicas por proyecto. Tras iteración se confirman TODAS las decisiones arquitectónicas: (1) tamaño **Carta/Letter 8.5×11in** (México imprime en Carta, no A4), (2) max **300 trabajadores** (incremento desde 200), (3) logo placeholder hasta feedback Leticia, (4) **SÍ mini-gráficas matplotlib** en sección II de estadísticas (8 subsecciones: %HBC barras, trauma por área, patrón pastel, escoliosis barras, RX/ECG conteos, campimetría, 3 lab mini-barras), (5) orden alfabético por apellido confirmado, (6) print test físico en Carta confirmado, (7) validación con Lolis/Leticia/Dra. Erika al final, (8) **imágenes originales embebidas inline** en cada sección de trabajador (audiograma, espirometría, RX Columna 2 vistas, RX Tórax, ECG, laboratorio PDF), (9) **un solo PDF portable autocontenido** (Frank: "no tiene sentido otra carpeta... mejor en un solo PDF para que el archivo sea portable"). Implementación: STAY con reportlab + pypdf para merge de PDFs nativos + matplotlib Agg backend + PIL para compresión (max 1500px, JPEG 80%). Estimación 5 fases: (1) core + imagen embebida + 1 gráfica 5-6h, (2) completar 8 secciones con imágenes 6-8h, (3) estadísticas con 8 mini-gráficas 4-5h, (4) integración frontend 2-3h, (5) validación stakeholders 2-3h. Total 19-25h. Riesgos clave: file size 300-800MB para 300 trabajadores (mitigado con compresión), tiempo generación 2-4min, unicode (TTF DejaVu). Cambios técnicos críticos vs v1: helper `image_fetcher.py` para descargar de `/uploads` o `/api/files/{key}`, `pdf_ebook_writer.py` reemplaza `pdf_writer.py` (deprecado), enum `format` cambia `'PDF'`→`'EBOOK'`. SPEC v2: `context/SPECs/SPEC_ARCH-20260630-01-EBOOK-PDF.md`. Pendiente: OK explícito de Frank para generar handoff a SOFIA Fase 1. (ARCH-20260630-01 v2)

- **2026-06-30 (INTEGRA):** [✓] **Se cierra IMPL-20260630-03+04+05: Módulo de Reportes Masivos por Proyecto mergeado a productivo.** Commits `cc92947` (módulo, 30 archivos +4943/-233) + `ce24807` (SQL migración Railway, 1 archivo +91) en `main`. Migración Prisma `20260630170000_add_project_report` aplicada a Railway DB con verificación exitosa (tabla_existe=1, indices=2, FKs=2, migración registrada=1). Backend 100% funcional: 4 endpoints FastAPI (`POST /massive`, `GET /{id}`, `GET /{id}/download`, `GET /historial`), generadores con openpyxl (3 hojas: CONCENTRADO, LABORATORIOS, GRAFICAS) + reportlab (portada "Diagnóstico Situacional" + concentrado landscape), BackgroundTasks para async. Frontend 100% funcional: hook `useProjectReportStatus` con polling 2s, server actions, modal `ProjectMassiveReportModal` con preview de conteos reales, integrado en ruta `/projects/[id]/page.tsx` con RBAC {ADMIN, DOCTOR_GENERAL, RECEPTIONIST}. 13/13 pytest backend + 137/137 vitest frontend en verde. Decisión arquitectónica Opción E aprobada: deuda técnica preexistente (8 typecheck + 33 lint errors) NO introducida por este módulo queda documentada como pendiente para SPEC futura `IMPL-XXXX-XX-FIX-VITEST-TYPECHECK`. Pendientes de la junta abr-2026 con Lolis/Leticia/Dra. Erika separados en SPECs futuras (logo/colores marca, validación audiométrica, demo 50 trabajadores, dashboard cliente). Checkpoint final: `context/checkpoints/CHK_IMPL-20260630-05-PASE-PRODUCTIVO-MODULO-REPORTES.md`. SPEC funcional: `context/SPECs/SPEC_ARCH-20260623-01-MODULO-REPORTES-MASIVOS.md`. Handoffs y SPECs de implementación en `context/SPECs/SPEC_IMPL-20260630-{03,04}-*.md` y `context/interconsultas/HANDOFF_IMPL-20260630-03_SOFIA_MODULO-REPORTES.md`. Script SQL Railway: `context/infra/05-migration-20260630-project-reports.sql`. Demo standalone sigue funcional en `https://administracion-medica-industrial.vercel.app/demo/reports/valiant-umm-demo`. Smoke test manual en producción queda como tarea para Frank (login ADMIN → `/projects/[id]` → generar → descargar). (ARCH-20260623-01 / IMPL-20260630-03+04+05)

- **2026-06-30 (INTEGRA):** [✓] **Se cierra IMPL-20260630-01: Modal Pago y Recibo en Papeleta.** Entregable ARCH-20260630-01 implementado y deployed a main (commit 2f45759). Checkpoint: `context/checkpoints/CHK_IMPL-20260630-01-MODAL-PAGO-RECIBO.md`. SPEC: `context/SPECs/SPEC_ARCH-20260630-01-MODAL-PAGO-RECIBO-PAPELETA.md`.

- **2026-06-30 (INTEGRA):** [~] **Se documenta investigación de bugs críticos que bloquean el flujo público de auto-alta.** Test E2E con Playwright llenando el formulario de "Servicios Robles S.A. de C.V." en producción descubrió 2 bugs activos: (A) `POST /api/v1/upload-only` retorna HTTP 500 (endpoint implementado por Frank en `8babb03`, probablemente env vars de storage faltantes); (B) Catálogo de estados México vacío en el `<select>` de `/solicitar-alta` (tabla `estados_mexico` posiblemente sin seed). Ambos bugs rompen el flujo end-to-end de captación pública. Se aprueba `ARCH-20260624-04` con plan: DEBUGGER investiga (logs Vercel + SQL Railway + curl), INTEGRA valida diagnóstico, SOFIA implementa fix, GEMINI audita. Re-test con Playwright tras fix. SPEC: `context/SPECs/SPEC_ARCH-20260624-04-INVESTIGACION-BUGS-FLUIDO-PUBLICO.md`. (ARCH-20260624-04)

- **2026-06-24 (INTEGRA):** [✓] **Se mergea feature `ARCH-20260624-03` completo: gestión de datos completos de empresa con dos vías (link externo + edición interna).** Commit `c34fcc6` en `main`. SOFIA implementó Sub-A (botón en `CompanyFormModal` modo edición que genera link con `targetCompanyId`, `submitCompanySelfRegistrationCore` con nueva rama UPDATE, `validateCompanySelfRegToken` extendido, action `generateCompanyDataCompletionLinkAction`) y Sub-B (ruta `/companies/[id]/edit` con `await params`, `CompanyEditForm` con secciones, nueva función `updateCompanyFull` con optimistic locking + `AuditLog` completo, action `updateCompanyAction` solo ADMIN). Helper `computeChanges` para diff before/after. 30 tests nuevos en `company-update.test.ts` + 18 tests nuevos en `company.service.test.ts` (total 80/80 verde). GEMINI auditoría APROBADO_CON_OBSERVACIONES, 0 bloqueadores. Decisiones validadas: solo ADMIN edita internamente, VENDEDOR solo genera links, auditoría completa. Prereqs verificados: schema Prisma con `targetCompanyId` commiteado en `79bc763`, migración aplicada a Railway verificada (`check-migrations-state.ts` confirma columna + índice + FK + 4 entradas en `_prisma_migrations`). Deuda técnica post-merge (nice-to-have): tests E2E Playwright, fix cosmético `toBeInstanceOf`, refactor shim legacy. SPEC: `context/SPECs/SPEC_ARCH-20260624-03-EDICION-DATOS-COMPLETOS-EMPRESA.md`. Handoff: `context/interconsultas/HANDOFF_ARCH-20260624-03_SOFIA_EDICION-DATOS-COMPLETOS.md`. (ARCH-20260624-03)
- **2026-06-24 (FRANK):** [✓] **Se commitea feature ARCH-20260624-01: ruta pública sin token para upload y serving de archivos de self-registration.** Commit `8babb03` en `main`. Frank implementó endpoints `upload-only` y file serving públicos en `backend/app/main.py` con 143 líneas modificadas + 150 líneas de tests. Permite que prospectos suban documentos a `/solicitar-alta` sin token. (ARCH-20260624-01)
- **2026-06-24 (INTEGRA):** [✓] **Se aplica migración `20260624214342_add_target_company_id_to_self_reg` a Railway producción.** Script extendido `context/infra/apply-migrations.ts` ahora crea UNIQUE constraint en `_prisma_migrations.migration_name` antes del ON CONFLICT (fix del error `42P10`). Scripts auxiliares nuevos: `frontend/scripts/sync-prisma-migrations.ts` (sync standalone idempotente) y `frontend/scripts/check-migrations-state.ts` (diagnóstico). Estado verificado en Railway: ✓ columna + ✓ índice + ✓ FK + 4 entradas aplicadas en `_prisma_migrations`. Aplicado vía `railway run --service 'Administracion-medica-industrial' npx tsx scripts/sync-prisma-migrations.ts`. Commit `95d4a1d`. (IMPL-20260624-04)
- **2026-06-24 (INTEGRA):** [~] **Se documenta gestión de datos completos de empresa con dos vías: link externo + edición interna.** El humano identifica que el flujo actual solo cubre "prospecto nuevo se da de alta como empresa", pero hay empresas dadas de alta manualmente antes de iniciar operaciones que necesitan completar datos completos, y explícitamente pide que usuarios con rol adecuado puedan editarlos desde el sistema. Se aprueba `ARCH-20260624-03` con dos sub-pasos ejecutables: (A) botón "Generar link para que la empresa complete sus datos" en `CompanyFormModal` modo edición (VENDEDOR/ADMIN, estado HABILITADO), reusa `generateCompanySelfRegLink` con nueva opción `targetCompanyId`, hace UPDATE en vez de CREATE; (B) nueva ruta `/companies/[id]/edit` + action `updateCompanyAction` solo para ADMIN, con optimistic locking vía `expectedUpdatedAt` y `AuditLog` completo reusando tabla existente (snapshot before/after). Decisiones validadas: solo ADMIN edita internamente, VENDEDOR solo links, auditoría completa. Migración Prisma aditiva (nuevo campo `targetCompanyId` opcional). SPEC: `context/SPECs/SPEC_ARCH-20260624-03-EDICION-DATOS-COMPLETOS-EMPRESA.md`. Handoff: `context/interconsultas/HANDOFF_ARCH-20260624-03_SOFIA_EDICION-DATOS-COMPLETOS.md`. (ARCH-20260624-03)
- **2026-06-24 (INTEGRA):** [✓] **Se mergea fix del link de auto-alta: URL pública de producción + trazabilidad de emisor.** Commit `18bba39` en `main`. Helper `getPublicBaseUrl()` con jerarquía `NEXT_PUBLIC_BASE_URL` → `VERCEL_PROJECT_PRODUCTION_URL` → `VERCEL_URL` → `localhost:3000` (auto-detecta dominio custom de Vercel sin env var manual). Link ahora incluye `?ref=<userId>` con `encodeURIComponent` para identificar al emisor. 16 tests nuevos (9 helper + 7 service). GEMINI auditoría APROBADO_CON_OBSERVACIONES, 0 bloqueadores. Resuelve el bug activo donde el link salía con `http://localhost:3000/...`. SPEC: `context/SPECs/SPEC_ARCH-20260624-02-LINK-AUTO-ALTA-URL-PUBLICA-TRAZABILIDAD.md`. Handoff: `context/interconsultas/HANDOFF_ARCH-20260624-02_SOFIA_LINK-AUTO-ALTA-URL-PUBLICA.md`. (ARCH-20260624-02)
- **2026-06-04 (CRONISTA):** [✓] **Cierre formal de la sesión sobre presentación declarativa para estudios IA con publicación en `main`.** Quedan cerrados el ajuste de Espirometría alineado al payload real y el corte estructural que mueve la representación clínica hacia `aiCalibration.presentation`, con propuesta asistida desde `extracted_data`, editor en calibración, consumo prioritario en papeleta y fallback legado controlado. El cierre se consolida en `context/checkpoints/CHK_DOC-20260604-01-CIERRE-CALIBRACION-PRESENTACION-IA.md`. (DOC-20260604-01)
- **2026-06-04 (INTEGRA):** [✓] **Se implementa la tercera capa de calibración: presentation persistida.** El contrato `aiCalibration.presentation` permite schema declarativo editable por prueba, con propuesta asistida desde `extracted_data` vía Gemini text-only. Incluye `PresentationSchemaPanel` (531 líneas), server action `ai-presentation.actions.ts`, endpoint backend `/api/v2/studies/presentation-schema/propose` con heurística y fallback, y tipos compartidos en `calibration.ts`. La papeleta consume primero el schema persistido, con fallback a hardcode estático. (ARCH-20260604-01)
- **2026-06-03 (INTEGRA):** [✓] **Se completa la realineación visual de Espirometría con el payload exhaustivo vigente.** El schema estático `espirometriaSchema` migra de alias legacy (`fev1_fvc`, `edad`, `unidad`, `m1`, `ref`) a las claves reales del snapshot: `fev1_fvc_ratio`, `fvc_percent_predicho`, `edad_anios`, `unit`, `m1_value`, `ref_value`, `curva_flujo_volumen_presente`. La tabla `parametros` ahora expone las 10 columnas canónicas. Checkpoint: `context/checkpoints/CHK_IMPL-20260603-01-REALINEACION-RENDERER-ESPIROMETRIA.md`. Commit `7ab294c` en `main`. (ARCH-20260603-05)
- **2026-06-03 (INTEGRA):** [✓] **Se completa la migración clínica a DR7.ai reemplazando Featherless, manteniendo Gemini en extracción.** El código verifica que `PrediagnosticService` usa ahora `_call_dr7_medical_chat()` con endpoint `https://dr7.ai/api/v1/medical/chat/completions`, modelo `medgemma-4b-it`, y degradación honesta a `AI_NON_CONCLUSIVE` ante errores HTTP. La capa clínica queda funcional con proveedor DR7, Gemini reservado a extracción multimodal, y variable `DR7_API_KEY` como requisito de habilitación. (ARCH-20260603-04)
- **2026-06-03 (INTEGRA):** [✓] **Se completa hotfix de normalización de contenido Featherless para Audiometría.** Se implementa `_extract_openai_choice_text()` en `GeminiBase` (base.py) y `_sanitize_model_json_text()` endurecido para recuperar texto plano, bloques segmentados y manejar respuestas vacías, sin confiar en que `message.content` siempre sea un string directo. El mismo endurecimiento se replica en `FeatherlessVisionBase` para no duplicar el problema en la capa extractiva. La degradación honesta a `AI_NON_CONCLUSIVE` ahora usa el proveedor correcto (`dr7`) y no culpa a Gemini. (FIX-20260603-04)
- **2026-06-03 (INTEGRA):** [✓] **Se corrige colisión de símbolo `externalCandidates` en CheckInModal.** El componente ahora usa una sola declaración `const [externalCandidates, setExternalCandidates] = useState<ExternalSearchCandidate[]>([])` como estado React, con búsqueda server-side integrada vía `searchExternalIntakeCandidates()`. El build de frontend deja de fallar por `defined multiple times`. (FIX-20260603-03)
- **2026-06-03 (INTEGRA):** [~] **Se formaliza hotfix de parseo clínico para Audiometría ante respuestas contaminadas de Featherless.** A partir de un caso real donde la extracción documental llegó correcta pero la capa clínica degradó a `AI_NON_CONCLUSIVE` con motivo engañoso (`FeatherlessParseError` + `Respuesta de Gemini no es JSON parseable`), se valida que el defecto no está en la tabla de Audiometría sino en el gateway clínico Featherless/MedGemma y su saneamiento insuficiente de salida. Se documentan el dictamen `context/interconsultas/DICTAMEN_FIX-20260603-01.md`, la especificación `context/SPECs/SPEC_FIX-20260603-01-AUDIOMETRIA-FEATHERLESS-PAD-JSON.md` y el handoff a SOFIA `context/interconsultas/HANDOFF_FIX-20260603-01_SOFIA_AUDIOMETRIA-FEATHERLESS-PAD-JSON.md`, acotando el arreglo a 3 archivos de backend: `prediagnostic.py`, `base.py` y `test_ai_pipeline.py`. (FIX-20260603-01)
- **2026-05-27 (INTEGRA):** [✓] **Se cierra la sesión sobre admisión sin cita y recepción con publicación y validación del micro-slice externo.** Queda consolidado el marco de tres flujos de admisión con sus slices operativos, se aplican las migraciones remotas requeridas para recepción, y se publica en `main` el ajuste `ARCH-20260527-24` para que la búsqueda de ingreso externo consulte la base real antes de crear persona nueva. El cierre se consolida en `context/checkpoints/CHK_DOC-20260527-02-CIERRE-ADMISION-SIN-CITA-Y-RECEPCION.md`. (DOC-20260527-02)
- **2026-05-27 (INTEGRA):** [~] **Se congela micro-slice para alinear búsqueda externa con la fuente real de identidad.** Se verifica que `createExternalWorkerIntake()` ya resuelve duplicados por nombre/apellido y reutilización fuerte por DOB, pero la UI de `Ingreso externo` sigue filtrando localmente solo `workers` sin empresa. Para eliminar esa inconsistencia se documenta `context/SPECs/SPEC_ARCH-20260527-24-BUSQUEDA-EXTERNA-SERVER-SIDE-Y-REUTILIZACION.md` y se deja handoff puntual a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260527-24_SOFIA_BUSQUEDA-EXTERNA-SERVER-SIDE.md`, acotando el ajuste a 2 archivos: `CheckInModal` y `worker.actions`. (ARCH-20260527-24)
- **2026-05-27 (INTEGRA):** [~] **Se consolida roadmap secuencial para SOFIA sobre admisión en tres flujos.** Se documenta la tabla ejecutiva `context/interconsultas/RESUMEN_ARCH-20260527-15-ROADMAP-SOFIA-ADMISION.md` y se generan los handoffs faltantes para los slices B, C y D: `HANDOFF_ARCH-20260527-12_SOFIA_SLICE-B-RECEPCION-PROJECT.md`, `HANDOFF_ARCH-20260527-13_SOFIA_SLICE-C-ALTA-RAPIDA-MISMO-DIA.md` y `HANDOFF_ARCH-20260527-14_SOFIA_SLICE-D-ADMISION-EXTERNA.md`. El orden recomendado queda congelado como A → B → C → D para preservar dependencia y evitar que SOFIA abra varios frentes sin trazabilidad de origen en `MedicalEvent`. (ARCH-20260527-15)
- **2026-05-27 (INTEGRA):** [~] **Se autoriza el Slice D para admisión externa sin empresa con convergencia directa a `MedicalEvent`.** Tras confirmar que `Worker` ya soporta `companyId = null` pero la operación todavía obliga contexto empresarial, se documenta `context/SPECs/SPEC_ARCH-20260527-14-SLICE-D-ADMISION-EXTERNA-SIN-EMPRESA.md`. El corte separa explícitamente el carril externo del flujo empresarial: no usa `Project`, no fuerza `Appointment` y resuelve el ingreso clínico directo desde recepción. (ARCH-20260527-14)
- **2026-05-27 (INTEGRA):** [~] **Se autoriza el Slice C para alta rápida empresarial del mismo día sobre `Project`.** Tras separar el caso pre-registrado del caso ad hoc, se documenta `context/SPECs/SPEC_ARCH-20260527-13-SLICE-C-ALTA-RAPIDA-EMPRESARIAL-MISMO-DIA.md` para resolver el escenario de mostrador donde llegan varios trabajadores sin Excel previo. El corte reutiliza `Project`, `BulkWorkerImportModal` y la lógica de alta masiva existente, pero limita la operación a un lote corto manual sin crear citas ni eventos clínicos todavía. (ARCH-20260527-13)
- **2026-05-27 (INTEGRA):** [~] **Se autoriza el Slice B para convertir `Project` en cola operativa de recepción de campañas empresariales pre-registradas.** Tras confirmar que `Project` + alta masiva ya resolvieron calendario y padrón, se documenta `context/SPECs/SPEC_ARCH-20260527-12-SLICE-B-RECEPCION-POR-PROJECT.md` para cerrar el siguiente hueco: ver trabajadores del proyecto, marcar llegada e ingresar a clínica sin pasar por agenda normal ni crear citas artificiales. Este slice queda acotado solo al caso pre-registrado; la alta rápida del mismo día se reserva para un slice posterior. (ARCH-20260527-12)
- **2026-05-27 (INTEGRA):** [~] **Se autoriza el Slice A de convergencia clínica: trazabilidad mínima de admisión en `MedicalEvent`.** A partir de la SPEC marco de tres flujos se separa un primer corte implementable y barato para no mezclar todavía `ProjectWorker`, citas y externos. Se documenta `context/SPECs/SPEC_ARCH-20260527-11-SLICE-A-TRAZABILIDAD-CONVERGENCIA-EVENT.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260527-11_SOFIA_SLICE-A-TRAZABILIDAD-EVENT.md`. El objetivo del slice es que recepción y la vista del evento ya muestren origen de admisión sin duplicar workspace clínico. (ARCH-20260527-11)
- **2026-05-27 (INTEGRA):** [~] **Se congela la arquitectura de admisión en tres flujos con convergencia obligatoria a `MedicalEvent`.** Tras revisar el frente de recepción, alta masiva, `Project` y el caso adicional de pacientes externos sin empresa, se define el marco único `context/SPECs/SPEC_ARCH-20260527-10-ADMISION-TRES-FLUJOS-Y-CONVERGENCIA-A-EVENT.md`. Queda cerrado que la diferencia entre programado empresarial, sin cita empresarial y sin cita externo ocurre en admisión; a partir del alta, los tres desembocan en el mismo workspace clínico basado en `MedicalEvent`. La implementación se divide en slices hijos para no mezclar trazabilidad, recepción por proyecto y admisión externa en un mega-cambio. (ARCH-20260527-10)
- **2026-05-27 (INTEGRA):** [~] **Se aprueba cierre de hueco operativo en ficha de Empresa Cliente para alta de perfiles médicos y asignación natural a puestos.** El modelo ya soporta `MedicalProfile.companyId` y `JobPosition.defaultProfileId`, pero la ficha `/companies/[id]` todavía obliga a salir a `/admin/profiles` para crear perfiles antes de poder asignarlos a puestos. Se documenta el corte `context/SPECs/SPEC_ARCH-20260527-04-PERFILES-MEDICOS-EN-EMPRESA-Y-ASIGNACION-A-PUESTOS.md` para exponer CRUD acotado de perfiles médicos dentro de la empresa cliente, sin tocar Prisma, y reutilizar el selector existente de puestos como segundo paso natural del flujo. (ARCH-20260527-04)
- **2026-05-27 (INTEGRA):** [✓] **Se cierra el frente operativo de proyectos con calendario y alta masiva contextual publicados en `main`.** Quedaron implementados y publicados la vista calendario mensual de `/projects`, el acceso visible en navegación, el flujo `crear proyecto -> alta masiva inmediata`, y el microajuste de consumo del banner contextual tras revisión QA. Se consolida el cierre en `context/checkpoints/CHK_DOC-20260527-01-CIERRE-PROYECTOS-Y-ALTA-MASIVA.md`. (DOC-20260527-01)
- **2026-05-27 (INTEGRA):** [✓] **Se formaliza el flujo encadenado crear proyecto → alta masiva inmediata.** Tras validar en operación que el flujo actual obliga a salir de `/projects` para ir a `/workers`, se aprueba un corte de continuidad para que el usuario pueda lanzar la carga masiva justo después de crear el proyecto, con empresa/proyecto preseleccionados y sin romper el flujo manual existente. La implementación quedó publicada en `main`. SPEC: `context/SPECs/SPEC_ARCH-20260527-03-ALTA-MASIVA-DESDE-PROYECTO.md`. (ARCH-20260527-03)
- **2026-05-19 (INTEGRA):** [✓] **Se formaliza el siguiente corte visual para `Project`: calendario operativo de visitas médicas.** Tras validar que la entidad `Project` ya opera como base de alta masiva pero aún se consume como tabla administrativa, se documenta `context/SPECs/SPEC_ARCH-20260519-16-CALENDARIO-PROYECTOS-VISITAS.md` para construir una vista calendario mensual por rango de fechas, con filtros por empresa y estado, y reutilización del `ProjectFormModal`. La implementación quedó publicada en `main` como base operativa del módulo. (ARCH-20260519-16)
- **2026-05-19 (INTEGRA):** [~] **Se aprueba rollback operativo del frente extractivo desde Featherless/Qwen a Gemini.** Tras la validación productiva del corte Qwen se confirma que el problema vigente no es de integración sino de continuidad operativa: el proveedor devuelve `503 capacity_exhausted` en corridas reales de extracción. Por instrucción directa del usuario, se revierte la decisión de arquitectura para que Gemini vuelva a operar clasificación documental y extracción estructurada, manteniendo intacta la capa clínica. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260519-15-ROLLBACK-EXTRACCION-A-GEMINI.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260519-15_SOFIA_ROLLBACK-EXTRACCION-A-GEMINI.md`. (ARCH-20260519-15)
- **2026-05-19 (INTEGRA):** [/] **Se estabiliza parcialmente el frente de Audiometría separando presentación y extracción residual.** El Slice A de frontend ya quedó publicado en `e71d2f2` para compatibilizar `pta`/`pta_visible`, normalizar `notas_calidad` y respetar el orden clínico completo de frecuencias (`125, 250, 500, 750, 1000, 1500, 2000, 3000, 4000, 6000, 8000`). En paralelo, DEBY dejó el dictamen `context/interconsultas/DICTAMEN_FIX-20260519-02.md`, que confirma que el defecto restante ya no es visual sino extractivo: persiste un corrimiento tabular residual en el oído izquierdo (`125=10`, `500=null` frente al formato fuente con `125` vacío y `500=10`). El corte queda estabilizado en presentación y abierto en backend para remediación posterior del extractor. (ARCH-20260519-04)
- **2026-05-19 (INTEGRA):** [~] **Se detecta desalineamiento entre la SPEC visual de Audiometría y el payload real que hoy entrega extracción.** La validación del estudio real confirma que la extracción ya resolvió el problema crítico de leer la tabla en vez de la gráfica, pero la UI sigue incumpliendo la SPEC `context/SPECs/SPEC_ARCH-20260518-14-RENDERER-CLINICO-AUDIOMETRIA.md` porque el schema visual todavía busca rutas legacy como `oido_derecho.via_aerea` y `oido_izquierdo.via_osea`, mientras el payload vigente ya usa `oido_derecho.va`, `oido_derecho.vo`, `pta_visible`, `paciente_detalle` y `notas_calidad` estructurada. El resultado es que no se renderizan las tablas comparativas acordadas y el bloque técnico aparece degradado o serializado. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260519-02-REALINEACION-RENDERER-AUDIOMETRIA-PAYLOAD-REAL.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260519-02_SOFIA_REALINEACION-RENDERER-AUDIOMETRIA.md`. (ARCH-20260519-02)
- **2026-05-18 (INTEGRA):** [/] **Se sincroniza la ejecución del corte ARCH-20260518-16 con implementación mínima no destructiva ya aplicada por SOFIA.** El catálogo visible quedó filtrado en la consulta de `MedicalTest` para ocultar `GEN-01` (Somatometría / Peso, Talla, Signos Vitales) y `GEN-02` (Agudeza Visual), preservando intactos los registros históricos, `EventTest` previos y relaciones existentes. La validación técnica disponible reporta sin errores el slice tocado, pero el checkpoint `context/checkpoints/CHK_IMPL-20260518-16-DEPURACION-CATALOGO.md` mantiene Gate 2 de testing como pendiente por ausencia de prueba dedicada; por eso este corte se registra como avance implementado y no como cierre total. (ARCH-20260518-16)
- **2026-05-18 (INTEGRA):** [~] **Se detecta catálogo visible con pruebas legacy que ya no reflejan el flujo operativo vigente.** La revisión del repositorio y de la documentación operativa confirma una inconsistencia: el catálogo sigue exponiendo entradas como `Somatometría` y `Agudeza Visual` como pruebas independientes en “Estudios Generales / Sala”, aunque varias decisiones posteriores de producto y operación las recolocan como parte o prerrequisito interno del flujo de `Examen Médico`. Dado que `MedicalTest` participa en historial y `EventTest`, no se aprueba hard delete directo; el corte correcto es depurar visibilidad/selección del catálogo sin romper trazabilidad histórica. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260518-16-DEPURACION-CATALOGO-PRUEBAS-LEGACY.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260518-16_SOFIA_DEPURACION-CATALOGO-PRUEBAS-LEGACY.md`. (ARCH-20260518-16)
- **2026-05-18 (INTEGRA):** [~] **Se aprueba mover la síntesis clínica derivada de Audiometría a la capa clínica de MedGemma y definirla en configuración.** Tras validar que frases como “audición normal bilateral”, clasificación preliminar por oído, lateralidad global y severidad no pertenecen a extracción documental sino a interpretación sobre umbrales y PTA, se define un nuevo corte para endurecer el prompt clínico configurable de Audiometría y exigir una salida estructurada específica para resumen por oído, resumen bilateral, tipo de hipoacusia, severidad, justificación y limitaciones. Se conserva la regla de que la descripción audiométrica escrita en el formato no se persiste como extracción documental. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260518-15-AUDIOMETRIA-PROMPT-CLINICO-DERIVADO.md` y el prompt consolidado en `context/interconsultas/PROMPTS_DOC-20260518-02-AUDIOMETRIA.md`. (ARCH-20260518-15)
- **2026-05-18 (INTEGRA):** [~] **Se aprueba extender el renderer clínico general a Audiometría como siguiente corte incremental.** Tras validar que la estrategia correcta es crecer estudio por estudio sobre la misma infraestructura visual, se define que Audiometría será el segundo schema del renderer clínico general ya implementado para Espirometría. El objetivo es mantener un solo componente base y añadir configuración específica para tablas por frecuencia, resumen por oído y campos fuente documentales (`faringe`, `cad`, `cai`, `mtd`, `mti`) cuando existan. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260518-14-RENDERER-CLINICO-AUDIOMETRIA.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260518-14_SOFIA_RENDERER-CLINICO-AUDIOMETRIA.md`. (ARCH-20260518-14)
- **2026-05-18 (INTEGRA):** [~] **Se aprueba reemplazar la tabla azul de valores capturados por un renderer clínico general, legible y configurable por tipo de estudio.** A partir de la validación operativa de Espirometría se confirma que el problema ya no está en el JSON extraído sino en su representación: la lista vertical actual hace ilegible la tabla clínica para médicos. Se define un corte para sustituir `CapturedValuesPanel` por una vista estructurada común, basada en secciones y tablas configurables por `studyType`, preservando el JSON solo como soporte DEV/QA temporal. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260518-13-RENDERER-CLINICO-GENERAL-EXTRACCION.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260518-13_SOFIA_RENDERER-CLINICO-EXTRACCION.md`. (ARCH-20260518-13)
- **2026-05-18 (INTEGRA):** [~] **Se define base universal de extracción médica y plantilla guiada para calibración por estudio.** A partir de la necesidad de maximizar exhaustividad sin duplicar instrucciones en cada prueba, se aprueba separar la capa extractiva en dos partes: una base universal fija en backend con reglas transversales de extracción médica estructurada y un bloque específico editable desde `aiCalibration.extraction.prompt`. Además, el módulo de calibración debe mostrar una plantilla visible para que futuros calibradores sepan exactamente qué pedir y cómo capturar reglas particulares del estudio. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260518-06-BASE-EXTRACCION-Y-PLANTILLA-CALIBRACION.md`. (ARCH-20260518-06)
- **2026-05-18 (INTEGRA):** [~] **Se aprueba doble flujo para estudios IA: reemplazar archivo o limpiar análisis sin romper auditoría.** A partir del incidente operativo en Espirometría y de la necesidad explícita del usuario de poder “dejar limpio” un estudio antes de una nueva captura, se define que el módulo debe separar dos acciones: `Reemplazar archivo` como flujo no destructivo con historial, y `Eliminar archivo y limpiar análisis` como limpieza operativa controlada con confirmación, sin hard delete silencioso de snapshots históricos. El corte incluye además corregir la dependencia exclusiva de `router.refresh()` para actualizar el snapshot vigente en cliente y volver legible el panel de `Valores capturados` cuando existan arreglos de objetos complejos. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260518-04-DOBLE-FLUJO-REEMPLAZAR-O-LIMPIAR-ARCHIVO-IA.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260518-04_SOFIA_DOBLE-FLUJO-ARCHIVO-IA.md`. (ARCH-20260518-04)
- **2026-05-18 (INTEGRA):** [~] **Se corrige la gobernanza de prompts: extracción sin fallback, clínica con fallback general y contrato clínico explícito preservado.** El usuario precisó la regla operativa real del sistema: el prompt de extracción siempre debe existir en la superficie de calibración y no debe tener respaldo hardcodeado; si falta, la corrida debe fallar como error de configuración. En cambio, la capa clínica sí puede conservar un fallback general prudente. Se confirma además que el contrato clínico actual ya exige explícitamente `justification`, `citations` y `limitations`, por lo que cualquier cambio de resolución de prompts debe preservarlos. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260518-03-EXTRACCION-SIN-FALLBACK-CLINICA-CON-FALLBACK.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260518-03_SOFIA_EXTRACCION-SIN-FALLBACK-CLINICA-CON-FALLBACK.md`. (ARCH-20260518-03)
- **2026-05-18 (INTEGRA):** [~] **Se formaliza la gobernanza de prompts: calibración primaria y fallback general mínimo para todos los estudios.** Se confirma una deuda estructural del pipeline IA: mientras los prompts del backend sigan embebidos como mecanismo principal, la capa de calibración por prueba no gobierna realmente el runtime. Se aprueba un corte transversal para que extracción y prediagnóstico resuelvan primero desde `aiCalibration`, dejando en backend solo prompts mínimos y genéricos como fallback seguro cuando no exista calibración personalizada válida. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260518-02-RESOLUCION-PROMPTS-CALIBRACION-PRIMARIA-Y-FALLBACK-GENERAL.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260518-02_SOFIA_RESOLUCION-PROMPTS-PRIMARIA-CALIBRACION.md`. (ARCH-20260518-02)
- **2026-05-16 (INTEGRA):** [~] **Se formaliza segundo frente de calibración para Espirometría: coherencia clínica del prediagnóstico.** Tras revisar el prompt actual de prediagnóstico se confirma que, aunque ya usa reglas ATS/ERS, sigue siendo posible que el modelo produzca cierres demasiado libres o contradictorios frente a la tabla fuente. Se aprueba un corte específico para gobernar el resumen clínico por jerarquía de evidencia tabular, calidad técnica y reglas explícitas de degradación a `AI_NON_CONCLUSIVE`, además de habilitar una `recommendation` prudente. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-13-ESPIROMETRIA-PREDIAGNOSTICO-COHERENTE.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-13_SOFIA_PREDX-ESPIROMETRIA-COHERENTE.md`. (ARCH-20260516-13)
- **2026-05-16 (INTEGRA):** [~] **Se formaliza calibración de extracción exhaustiva para Espirometría a partir de layout real.** La revisión del formato fuente confirma que el contrato actual de Espirometría es demasiado corto para AMI: hoy solo captura un núcleo mínimo, mientras el documento expone identificación del estudio, datos antropométricos, condiciones de adquisición, equipo, repetibilidad y tabla completa con `M1`, `M2`, `M3`, `%REF`, `REF` y `LLN`. Se aprueba un corte específico para ampliar el prompt y el schema extractivo hacia un contrato exhaustivo, manteniendo la separación estricta entre extracción documental e interpretación clínica. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-12-ESPIROMETRIA-EXTRACCION-EXHAUSTIVA.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-12_SOFIA_ESPIROMETRIA-EXTRACCION-EXHAUSTIVA.md`. (ARCH-20260516-12)
- **2026-05-16 (INTEGRA):** [~] **Se redefine el problema del RAW clínico como visibilidad condicionada por datos y no solo por estilo.** La revisión del flujo de render confirma que `StudyPrediagnosisRawPanel` desaparece por completo cuando el snapshot no trae `input_debug`, lo que hace que el usuario interprete que “no está poniendo nada” aunque el componente exista. Se aprueba un corte mínimo de UX/observabilidad para que el panel `RAW de entrada clínica` siempre se renderice con el mismo lenguaje visual técnico del `RAW de extracción`, mostrando un estado vacío explícito cuando el estudio no tenga RAW clínico persistido. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-11-RAW-PREDIAGNOSTICO-SIEMPRE-VISIBLE.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-11_SOFIA_RAW-PREDX-SIEMPRE-VISIBLE.md`. (ARCH-20260516-11)
- **2026-05-16 (INTEGRA):** [~] **Se detecta problema de descubribilidad en el RAW clínico: existe pero no se percibe como panel técnico equivalente al RAW de extracción.** La comparación directa de componentes confirma que el `RAW de entrada clínica` quedó embebido como `details` blanco dentro del card de prediagnóstico, mientras el `RAW de extracción` usa un bloque oscuro claramente visible. Se aprueba un ajuste de UX para dar paridad visual al RAW clínico sin tocar persistencia ni lógica clínica. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-09-RAW-PREDIAGNOSTICO-PARIDAD-VISUAL.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-09_SOFIA_PARIDAD-VISUAL-RAW-PREDX.md`. (ARCH-20260516-09)
- **2026-05-16 (INTEGRA):** [~] **Se aprueba observabilidad técnica de la entrada clínica a MedGemma.** El usuario requiere revisar exactamente qué le llega al modelo clínico. La revisión del backend confirma que hoy solo se persiste la salida del prediagnóstico, no el input exacto enviado a la capa clínica. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-08-RAW-INPUT-MEDGEMMA-EN-PAPELETA.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-08_SOFIA_RAW-INPUT-MEDGEMMA.md` para persistir y mostrar un panel `RAW de entrada clínica`, con opción deseable de incluir el prompt renderizado si no expone secretos ni vuelve excesiva la UI. (ARCH-20260516-08)
- **2026-05-16 (INTEGRA):** [~] **Se detecta déficit de fidelidad extractiva en Audiometría frente al formato real de AMI.** La revisión del prompt y del schema confirma que la extracción actual solo captura umbrales por frecuencia y metadata mínima, dejando fuera campos visibles del formato como `Faringe`, `CAD`, `CAI`, `MTD` y `MTI`. Por indicación clínica de la doctora, la descripción audiométrica escrita en el formato queda excluida del contrato extractivo y no debe persistirse. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-07-AUDIOMETRIA-EXTRACCION-CAMPOS-FUENTE-DIAGNOSTICOS.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-07_SOFIA_AUDIOMETRIA-CAMPOS-FUENTE.md`. (ARCH-20260516-07)
- **2026-05-16 (INTEGRA):** [~] **Se reprioriza el frente clínico: la salida del prediagnóstico carece de recomendación útil y ese es el defecto principal.** La revisión del contrato actual confirma que el problema no está en extracción ni en observabilidad del snapshot, sino en que la capa clínica fue diseñada sin campo de recomendación y además los prompts restringen explícitamente recomendaciones. Se aprueba el corte `context/SPECs/SPEC_ARCH-20260516-06-PREDIAGNOSTICO-CON-RECOMENDACION-CLINICA.md` para agregar recomendación clínica prudente y visible en la tarjeta del prediagnóstico, manteniendo los guardrails de no emitir aptitud laboral, dictamen final ni tratamiento. Se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-06_SOFIA_RECOMENDACION-PREDIAGNOSTICO.md`. El slice de RAW clínico queda como mejora secundaria posterior. (ARCH-20260516-06)
- **2026-05-16 (INTEGRA):** [~] **Se detecta brecha de observabilidad: el prediagnóstico IA sí se persiste, pero no existe RAW clínico visible en la papeleta.** La validación operativa de calibraciones queda bloqueada porque la tarjeta de prediagnóstico muestra `v{snapshot.version}` y eso induce a confundir la versión ordinal del snapshot con la versión real del prompt clínico. Se aprueba un corte mínimo de observabilidad para exponer el RAW del prediagnóstico y su trazabilidad visible en UI, sin tocar motor clínico ni persistencia. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-05-RAW-PREDIAGNOSTICO-IA-TRAZABILIDAD.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-05_SOFIA_RAW-PREDIAGNOSTICO-IA.md`. (ARCH-20260516-05)
- **2026-05-16 (INTEGRA):** [~] **Se identifica fallo de UX/observabilidad cuando el upload IA se interrumpe en cliente con `ERR_NETWORK_CHANGED`.** El análisis local del flujo en la papeleta confirma que `handleFileUpload()` y `handleRegenerateAI()` esperan la server action sin `try/catch`, por lo que un fallo de red del POST a `/events/[id]` deja un `Uncaught (in promise) TypeError: Failed to fetch`, sin limpiar adecuadamente el estado visual ni registrar contexto útil para diagnóstico. Se documenta la SPEC `context/SPECs/SPEC_FIX-20260516-01-INSTRUMENTACION-UPLOAD-NETWORK-CHANGED.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_FIX-20260516-01_SOFIA_UPLOAD-NETWORK-CHANGED.md` para agregar logging estructurado, manejo explícito del error y limpieza robusta de timers/estado. (FIX-20260516-01)
- **2026-05-16 (INTEGRA):** [~] **Se define mejora UX para mostrar progreso por etapas durante upload/procesamiento IA en la papeleta.** A partir de la revisión del flujo real se confirma que el estado actual `isUploading` oculta varias fases en una sola espera y genera incertidumbre para el usuario. Se aprueba un corte ligero para renderizar barra y stepper honestos sobre las etapas reales del pipeline (`subiendo`, `clasificando`, `extrayendo`, `prediagnosticando`, `guardando`), sin rediseñar aún la arquitectura a jobs asíncronos. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-04-PROGRESO-POR-ETAPAS-UPLOAD-IA.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-04_SOFIA_PROGRESO-POR-ETAPAS-UPLOAD-IA.md`. (ARCH-20260516-04)
- **2026-05-16 (INTEGRA):** [~] **Se redefine la capa de configuración de calibración para operar con solo dos prompts por prueba.** Tras alinear con el usuario el modelo operativo real del pipeline IA, se descarta seguir capturando en la configuración campos, contratos clínicos o metadata que pertenecen a extracción/salida renderizada. La configuración por prueba debe reducirse a una capa mínima: prompt/version de extracción documental para Gemini y prompt/version de diagnóstico clínico para MedGemma, manteniendo únicamente el mínimo de activación y tipo canónico. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-03-CALIBRACION-CONFIG-SOLO-DOS-PROMPTS.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-03_SOFIA_CONFIG-SOLO-DOS-PROMPTS.md`. (ARCH-20260516-03)
- **2026-05-16 (INTEGRA):** [~] **Se define calibración V2 dirigida para Audiometría y Espirometría a partir de casos reales de AMI.** Con evidencia de expediente en producción se confirma que Audiometría ya entrega extracción clínica útil pero todavía puede perder precisión fina al leer gráfico vs tabla, y que Espirometría ya capta señal clínica relevante pero su extracción sigue corta frente al PDF y su síntesis final puede contradecir sus propias premisas numéricas. Se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-02-CALIBRACION-V2-CASOS-REALES-AUDIO-ESPIRO.md` y se deja handoff a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-02_SOFIA_CALIBRACION-V2-CASOS-REALES-AUDIO-ESPIRO.md` para endurecer extractor, reglas de síntesis clínica y prueba comparativa posterior. (ARCH-20260516-02)
- **2026-05-16 (INTEGRA):** [~] **Se formaliza corrección ejecutiva para visor inline de documentos en bucket y remediación de acceso clínico MedGemma.** Tras validar en producción que el PDF de estudio dispara descarga en vez de embebido y que el prediagnóstico falla con `403 model_gated_needs_oauth`, se documenta la SPEC `context/SPECs/SPEC_ARCH-20260516-01-VISOR-INLINE-BUCKET-Y-ACCESO-MEDGEMMA.md` y se deja handoff puntual a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260516-01_SOFIA_VISOR-INLINE-Y-MEDGEMMA.md`. El frente queda dividido en: (1) corrección técnica del upload/resolución S3 para preservar `Content-Type` y `inline`, y (2) checklist de remediación para acceso real al modelo clínico en Featherless con fallback honesto mientras se habilita el modelo gated. (ARCH-20260516-01)
- **2026-05-14 (INTEGRA):** [~] **Desviación detectada y corregida en fuente de verdad para corroboración de identidad.** Se verificó que la SPEC histórica `context/SPECs/SPEC_ARCH-20260507-12-CORROBORACION-IDENTIDAD-CHECK-IN.md` ya exigía corrección de nombre completo contra la INE, pero la implementación actual del modal de corroboración quedó limitada a teléfono y correo. Se formaliza el ajuste ejecutivo en `context/SPECs/SPEC_ARCH-20260514-01-ALINEACION-CORROBORACION-NOMBRE-INE.md` y se deja handoff puntual a SOFIA en `context/interconsultas/HANDOFF_ARCH-20260514-01_SOFIA_ALINEACION-CORROBORACION-NOMBRE-INE.md`. (ARCH-20260514-01)
- **2026-05-13 (INTEGRA):** [~] **Ruta activa de persistencia migrada a Railway Storage Bucket ya provisionado.** Tras confirmarse que el usuario ya creo un bucket S3-compatible en Railway, se formalizo la especificacion `context/SPECs/SPEC_ARCH-20260513-15-STORAGE-BUCKET-RAILWAY.md` como camino activo para persistencia de archivos clinicos privados, sustituyendo el enfoque de volume como opcion principal. Se deja handoff de implementacion para SOFIA en `context/interconsultas/HANDOFF_ARCH-20260513-15_SOFIA_STORAGE-BUCKET-RAILWAY.md`. (ARCH-20260513-15)
- **2026-05-13 (CRONISTA):** [/] **Sincronización de estado: uploads persistentes en Railway quedan formalmente en planificación ejecutable.** Se valida que `ARCH-20260513-14` permanezca en estado `[~]` porque existe SPEC y handoff listos para implementación, pero todavía no hay checkpoint técnico ni evidencia de despliegue/restart exitoso con Railway Volume. Se corrige además el encabezado maestro de este documento para alinearlo con los cortes vigentes de mayo. (DOC-20260513-01)
- **2026-05-13 (INTEGRA):** [~] **Persistencia real de uploads en Railway formalizada para ejecucion inmediata.** Se documento la especificacion `context/SPECs/SPEC_ARCH-20260513-14-UPLOADS-PERSISTENTES-RAILWAY.md` para resolver los `404 Not Found` de archivos subidos mediante un Railway Volume montado en `/uploads`, conservando el contrato actual de URLs publicas. Se deja handoff de implementacion para SOFIA en `context/interconsultas/HANDOFF_ARCH-20260513-14_SOFIA_UPLOADS-PERSISTENTES-RAILWAY.md`. (ARCH-20260513-14)
- **2026-05-13 (INTEGRA):** [~] **Handoff de apertura para nuevo repo de MEDGEMMA APIS preparado para SOFIA.** Se documento un handoff especifico para cuando el frente se abra en un repositorio separado, dejando claro que ya esta listo, que puede implementar SOFIA de inmediato, que validaciones siguen pendientes y que insumos concretos debe entregar el usuario para cerrar despliegue y activacion. (ARCH-20260513-09)
- **2026-05-13 (INTEGRA):** [~] **Integracion MedGemma via SDK compatible de OpenAI autorizada para implementacion inmediata.** Se definio el corte tecnico para conectar Featherless como proveedor clinico real usando el SDK de OpenAI con `base_url` configurable, manteniendo Gemini como fallback de interpretacion. Se deja handoff de implementacion puntual a SOFIA sobre la capa `PrediagnosticService`, configuracion y pruebas acotadas. (ARCH-20260513-08)
- **2026-05-13 (INTEGRA):** [~] **Linea API B2B y operacion con Featherless documentadas para MEDGEMMA APIS.** Se definio como vender una modalidad solo endpoint/API, que se cobra, que contrato requiere, que endpoints exponer y como se integra Featherless como proveedor clinico aguas abajo sin entregar acceso directo al modelo al cliente. (ARCH-20260513-07)
- **2026-05-13 (INTEGRA):** [~] **Modelo comercial y operativo de MEDGEMMA APIS definido para venta y activacion de clientes.** Se documento el proceso completo desde lead hasta arranque: discovery, demo, cotizacion, contratacion, onboarding, activacion y expansion, incluyendo que se vende realmente, que se cotiza, que se firma y que entregables deben generarse al cerrar un cliente. (ARCH-20260513-06)
- **2026-05-13 (INTEGRA):** [~] **Micrositio comercial estatico de MEDGEMMA APIS definido para repo separado y despliegue en Vercel/GitHub Pages.** Se formalizo una SPEC para construir un sitio comercial estatico, desacoplado del repo principal, con landing de conversion, CTA a WhatsApp y compatibilidad de despliegue tanto en Vercel como en GitHub Pages. Se deja handoff a SOFIA para scaffold local listo para empujar a un repositorio nuevo. (ARCH-20260513-05)
- **2026-05-13 (INTEGRA):** [~] **Paquete comercial MedGemma APIs definido para salida a mercado.** Se creo la carpeta separada `MEDGEMMA APIS/` con especificacion maestra del producto "Copiloto Clinico Ocupacional", demo minimo vendible, pricing justificado, brief y copy de landing page, catalogo de casos de uso, guia tecnica de implementacion, onboarding de cliente y FAQ comercial-operativo para preparar la venta inmediata del servicio una vez se cierre la integracion tecnica de MedGemma. (ARCH-20260513-03)
- **2026-05-13 (INTEGRA):** [~] **Piloto MedGemma via API gestionada formalizado.** Se definio la especificacion `context/SPECs/SPEC_ARCH-20260513-02-PILOTO-MEDGEMMA-FEATHERLESS.md` para validar MedGemma 27B Text en Featherless como proveedor de prediagnostico clinico en modo sombra, manteniendo Gemini 2.5 como capa de extraccion. El piloto queda acotado a Audiometria y Espirometria, sin infraestructura propia y con plan objetivo Featherless Premium de 25 USD/mes. (ARCH-20260513-02)
- **2026-05-13 (INTEGRA):** [~] **Siguiente corte de IA para estudios prioritarios formalizado.** Se definio la especificacion `context/SPECs/SPEC_ARCH-20260513-01-CALIBRACION-V1-AUDIOMETRIA-ESPIROMETRIA.md` para continuar con Audiometria y Espirometria bajo el modelo de dos momentos de IA, sin alterar el flujo clinico vigente. Se dejo handoff preparado para implementacion en `context/interconsultas/HANDOFF_ARCH-20260513-01_SOFIA_CALIBRACION-V1-AUDIO-ESPIRO.md`. (ARCH-20260513-01)
- **2026-05-07 (CRONISTA):** [✓] **Cierre operativo del cronograma admin persistente y backlog futuro de recepcion documentado.** Quedo estabilizado el despliegue en linea del cronograma admin persistente de papeleta, con migracion remota aplicada en Railway y hotfix de tipado publicado para destrabar el build. Se formalizo backlog futuro sin alterar el flujo actual mediante las SPECs `context/SPECs/SPEC_ARCH-20260507-11-QR-IDENTIFICACION-OPERATIVA-MINIMA.md` y `context/SPECs/SPEC_ARCH-20260507-12-CORROBORACION-IDENTIDAD-CHECK-IN.md`. Checkpoint de cierre: `context/checkpoints/CHK_ARCH-20260507-12-CIERRE-SESION.md`. (DOC-20260507-01)
- **2026-03-06 (DEBY):** [✓] **Corrección de Error 500 en Expedientes (Vercel).** Se identificó un crash provocado por la falta de `SessionProvider` y fallos de serialización de objetos `Date` en Server Components. Se implementó un wrapper de proveedores global y serialización defensiva en `EventPage`. Documentación técnica en `context/interconsultas/DICTAMEN_FIX-20260306-01.md`. (FIX-20260306-01)
- **2026-02-27 (INTEGRA):** [✓] **Corrección de Flujo Crítico (Doctor): Finalización de Cita.** Se eliminó la capacidad de "Finalizar Cita" desde el tablero de Recepción, restringiéndola exclusivamente al Médico desde su vista de expediente. Se actualizó la etiqueta del botón a "✅ Finalizar Cita y Pasar a Validación" para mayor claridad. Documentación técnica en `context/checkpoints/CHK_FIX-20260227-02.md`. (FIX-20260227-02)
- **2026-02-27 (CRONISTA):** [✓] **Cierre de Sprint Citas: Mejoras de UX y Planificación Futura**. Se realizaron ajustes finales y definición de roadmap: (1) **Navegación Fluida:** Corrección del flujo `Crear Trabajador -> Agendar Cita` usando redirección con parámetros URL (evitando race conditions de eventos). (2) **Pase de Confirmación Enriquecido:** El modal de éxito ahora muestra fecha formateada, hora, sucursal y dirección. (3) **Definición de Roadmap:** Se documentaron tareas para próxima fase: integración de Google Maps en Sucursales y diseño del Widget de Citas Externas (Self-Service). Repositorio actualizado y listo para Handoff a Antigravity. (DOC-20260227-02)
- **2026-02-27 (CRONISTA):** [✓] **Sprint: Gestión de Citas y Piso Clínico - Correcciones de Calidad COMPLETADO**. Se finalizó el refinamiento de módulos con validaciones y estandarización técnica: (1) **Renombrado "Piso Clínico"** y ajustado flujo Kanban (Sala → Consultorio → Validación). (2) **Modal de Check-in con QR** implementado y validado. (3) **Atomicidad en Backend:** Implementación de `prisma.$transaction` en `checkInAppointment()` para garantizar que la operación compuesta (Update Appointment Status + Create Medical Event) sea indivisible, evitando race conditions. (4) **Corrección de Auditoría:** Integración de logs en transacciones críticas. (5) **Validación de Timezones:** Normalización de fechas a formato ISO en el cliente antes de envío. (6) **Validaciones de WhatsApp:** Verificación de `workerPhone` antes de generar enlaces `wa.me`. Auditoría de calidad completada por DEBY: `DICTAMEN_DEBY_20260227-01.md` (Resultado: APROBADO CON OBSERVACIONES - Yellow Flag). Deuda técnica documentada: bug visual en `borderColor` de Lane, refactorización de `handleSubmit` en modal de citas. Sistema listo para próxima iteración de polish visual en Antigravity. (DOC-20260227-01)
- **2026-02-26 (SOFIA/DEBY):** [✓] Hotfix: Corrección de error de build en Vercel. Se agregó la directiva `"use client"` a `EventFlowController.tsx` para permitir el uso de Hooks (`useRef`, `useSession`) en el entorno de Client Components. Build de producción validado exitosamente. (FIX-20260226-02)
- **2026-02-26 (INTEGRA/SOFIA/DEBY):** [✓] Estabilización y Automatización E2E v2.2 COMPLETADA. Se logró el "Ejercicio Ciego" exitoso solicitada por el usuario: (1) **Identificación Universal**: Algoritmo automático `[INICIALES]-[FECHA]-[G]-AMI-CLI` para trabajadores (eliminación de carga manual). (2) **Agenda Premium**: Rediseño del Módulo de Citas a vista de Agenda Diaria con generación de expedientes `EXP-YYYYNNN` y Pase de Entrada con código QR (🎫). (3) **Dictamen IA & Firma**: Mapeo de hallazgos extraídos por IA (`extractedData`) en la sección III del PDF y estabilización del flujo de firma con persistencia síncrona (eliminación del error "No hay dictamen para firmar"). (4) **Auditoría Qodo**: Limpieza total de inseguridades de tipo y advertencias de linter en módulos críticos. Git Push `INFRA-20260226-01` realizado. Sistema 100% estabilizado para operación real. (DOC-20260226-01)
- **2026-02-25 (CRONISTA):** [✓] Sprint 7 COMPLETADO 100% - Módulos Complementarios Fase 2. Se implementó exitosamente: (1) MOD-CITAS: schema Prisma actualizado (Appointment, AppointmentStatus), Server Actions para CRUD y gestión de agenda, UI componentes (AppointmentForm, AppointmentList, AppointmentCalendar) e interfaces en `/admin/appointments`. (2) MOD-DASHBOARD: schema Prisma extendido (AuditLog), Server Actions para métricas (getDashboardMetrics con KPIs de citas y eventos), widgets de UI (AppointmentMetrics, EventStatusMetrics, CompanyPerformance) e interfaz `/dashboard` operativa. (3) MOD-BITACORA: servicio de auditoría implementado, logging integrado en eventos críticos (createMedicalEvent, updateMedicalVerdict, updateAppointmentStatus, NextAuth callbacks), UI de historial con filtros en `/admin/audit-logs`. Validación E2E, performance (<2s en consultas) y testing de integridad completados. Sistema backend 100% funcional, producción-ready. **LISTO PARA ANTIGRAVITY (Fase 2 de refinamiento UI con Tailwind y polish visual).** (DOC-20260225-08)
- **2026-02-25 (CRONISTA):** [✓] Fase 6 - COMPLETADA 100%. Refinamiento de Pipeline IA y Ofimática finalizado exitosamente. Se implementaron: (1) Clasificador Inteligente de Documentos con Vision API, (2) Extracción de datos estructurados por tipo de estudio, (3) Firma Digital Avanzada en PDFs, (4) Motor de Reportes Masivos (Excel), (5) Integración Frontend (Botones de Exportar y Firmar). Sistema lista para producción. (DOC-20260225-06)
- **2026-02-25 (CRONISTA):** [✓] Fase 5 - COMPLETADA 100%. Auditoría y transición a Fase 6 iniciada. Todas las tareas de seguridad (NextAuth.js, rutas protegidas, testing multi-tenant E2E) validadas y cerradas. Sistema producción-ready para Vercel. Fase 6 inicia con enfoque en refinamiento de pipeline IA y ofimática avanzada. (DOC-20260225-04)
- **2026-02-25 (SOFIA):** [✓] Fase 5 - Testing E2E de Autenticación Completado. Se ejecutó testing exhaustivo del flujo de autenticación (Login, Rutas Protegidas, Multi-tenant, Logout, RBAC). Todos los tests pasaron exitosamente. Aislamiento de datos multi-tenant validado. Reporte detallado: `E2E-AUTH-REPORT-20260225.md`. Sistema de autenticación validado como production-ready. (IMPL-20260225-03)
- **2026-02-25 (CRONISTA):** [✓] Fase 5 - Auditoría de Seguridad y QA Finalizada. Validación exitosa de aislamiento de datos multi-tenant. Testing manual completado confirmando integridad referencial y control de acceso por rol. Todas las tareas de seguridad cerradas. Sistema production-ready para despliegue en Vercel/Railway. (DOC-20260225-03)
- **2026-02-25 (CRONISTA/SOFIA):** [✓] Fase 5 - Implementación de Seguridad NextAuth.js Completada. Se implementó autenticación formal con NextAuth.js, rutas protegidas en `/api/*` y rutas UI privadas. Auditoría de seguridad finalizada con Qodo CLI. Dictamen técnico generado: `DICTAMEN_FIX-20260225-02.md`. Vulnerabilidades críticas resueltas. Testing E2E y multi-tenant validados. (DOC-20260225-02)
- **2026-06-23 (INTEGRA):** [~] **Se autoriza el Módulo de Reportes Masivos para proyectos con carga masiva (UMM o clínica).** Tras revisar la necesidad operativa de entregar concentrado a empresas clientes, se aprueba la creación de un módulo que aplica a cualquier proyecto con `ProjectWorker` activos. Genera XLSX (3 hojas: CONCENTRADO, LABORATORIOS, GRAFICAS) y PDF (portada diagnóstica situacional + concentrado tabular). Micro-Sprint 1: XLSX + PDF simple, async con polling, sin drill-down interactivo. SPEC: `context/SPECs/SPEC_ARCH-20260623-01-MODULO-REPORTES-MASIVOS.md`. ADR: `context/decisions/ADR-20260623-01-MODULO-REPORTES-MASIVOS.md`. Handoff: `context/interconsultas/HANDOFF_ARCH-20260623-01_SOFIA_MODULO-REPORTES-MASIVOS.md`. (ARCH-20260623-01)
- **2026-06-09 (CRONISTA):** [✓] **Cierre total de backlog junio 2026.** Se verifica y marcan completas todas las tareas del backlog: ARCH-20260604-01 (presentation schema persistida con `aiCalibration.presentation`, panel de calibración y renderer priorizado), ARCH-20260603-04 (DR7.ai), ARCH-20260603-05 (Espirometría alineado), FIX-20260603-04 (Content Normalization), FIX-20260603-03 (Duplicate Symbol), **ARCH-20260527-24 (búsqueda externa server-side: `searchExternalIntakeCandidates()` en worker.actions.ts y consumo en CheckInModal.tsx)**. Próximo workspace identificado: integraciones comerciales MEDGEMMA APIS. Build falló por Prisma 7 incompatibility (node_modules missing). Checkpoint: `context/checkpoints/CHK_DOC-20260609-02-CIERRE-TOTAL-BACKLOG-JUNIO.md`. (DOC-20260609-02)
- **2026-02-25 (INTEGRA):** [✓] Sprint 4 Completado: Panel de Administración y Motor PDF de Dictámenes. Especificación `ARCH-20260225-04-ADMIN-Y-PDF.md` implementada exitosamente. Endpoint `/api/pdf/[eventId]` funcionando.
- **2026-02-25 (INTEGRA/DEBY):** Aprovisionamiento de DB de Producción en Railway. Vercel conectado. Resolvimos vulnerabilidades reportadas por Qodo (DICTAMEN_FIX-20260225-01): **Login formal y control multi-tenant diferidos a Fase 5** por decisión de negocio.
- **2026-02-24 (INTEGRA):** Inicio del Sprint 2: Portal B2B Empresarial. Especificación creada (`ARCH-20260224-02-PORTAL-EMPRESAS.md`) y handoff a `@SOFIA` (Planificado).
- **2026-02-03 (INTEGRA):** Inicio del proyecto. Definición de módulos iniciales.
- **2026-02-03 (INTEGRA/SOFIA):** Infraestructura desplegada (Next.js + FastAPI + Postgres). Verificación exitosa en puerto 3001.
- **2026-02-03 (INTEGRA/SOFIA):** Modelado de Base de Datos completado (Prisma). Diccionario de Datos creado.
- **2026-02-03 (SOFIA):** Implementación de Core API (Services + Server Actions). Lógica de Expediente y soporte No-Lineal verificado.
- **2026-02-03 (SOFIA):** UI de Recepción implementada (Listado de Trabajadores y Detalle). Layout Principal activo.
- **2026-02-03 (SOFIA):** UI de Tablero Médico implementada (Vista de Expediente y Dictamen). Flujo "punta a punta" a nivel UI disponible.
- **2026-02-03 (SOFIA):** Configuración de Archivos completa. Volumen `uploads/` compartido entre Frontend y Backend.
- **2026-02-03 (SOFIA):** Refactorización a "Smart Uploads". Se implementó Drag & Drop masivo con estado "Analizando..." para clasificación futura por IA.
- **2026-02-03 (SOFIA):** Alineación de UI con Mockup. Layout de 2 columnas (Clínicos vs Lab) y Header corporativo.
- **2026-02-03 (SOFIA):** Integración Backend AI completa. Conexión Next.js -> Python operativa.
- **2026-02-03 (SOFIA):** Implementación de **OCR Real** (Tesseract) en Backend.
- **2026-02-04 (SOFIA):** Integración con **Google Gemini 1.5 Flash** (Vision API). Reemplazo del OCR local por análisis multimodal en la nube para diagnósticos y extracción de datos estructurados.
- **2026-02-04 (SOFIA):** Verificación de Flujo Completo (`verify-full-journey.ts`) exitosa. Ciclo: Cita -> Check-in -> Carga -> IA -> Dictamen validado.
- **2026-02-04 (SOFIA/DEBY):** Implementación de "Full Wiring" (Cableado Completo).
    - Módulos de Empresas (companies), Sucursales (branches) y Servicios (services) conectados a PostgreSQL.
    - Implementación de Integridad Referencial: Empresa -> Sucursal -> Trabajador -> Evento.
    - "Caso Francisco Saavedra": Demostración E2E de creación de cliente, trabajador y operación médica.
    - Integración WhatsApp Dinámica: Generación de enlaces `wa.me` personalizados con teléfono del trabajador para entrega de resultados.
- **2026-03-24 (CRONISTA):** [✓] **Consolidación de Navegación, Perfiles Médicos y Validación Local.** Se completó la integración de navegación fluida y perfiles médicos oficiales, junto con la validación local de roles. Cambios principales:
  1. **Bootstrap Local con Base Remota:** Configuración validada.
  2. **Checkpoints:** `context/checkpoints/CHK_IMPL-20260324-01.md`, `context/checkpoints/CHK_ARCH-20260324-25.md`.
  3. **Dictamen Técnico:** `context/interconsultas/DICTAMEN_FIX-20260324-01.md`.
  4. **Validación por Rol:** Corroborada localmente.

  **Pendientes diferidos para iteración posterior:**
  - Restringir acceso directo de `COMPANY_CLIENT` a `/companies` y revisar si también debe bloquearse `/companies/[id]`.
  - Evaluar ajuste de UX para que accesos no autorizados de `COMPANY_CLIENT` redirijan a `/portal` en lugar de `/dashboard`.

  Sistema listo para próximas iteraciones. (ARCH-20260324-26)
- **2026-03-25 (INTEGRA):** [✓] **Limpieza Operativa del Catálogo Legacy.** Se verificaron y ajustaron los servicios relacionados en `frontend/src/app/services/page.tsx` y `frontend/src/actions/admin.actions.ts`. Cambios documentados en `context/checkpoints/CHK_ARCH-20260325-02.md`. (ARCH-20260325-02)
- **2026-03-25 (INTEGRA):** [✓] **Separación de Somatometría y Agudeza Visual como estudios independientes.** Se implementó la especificación `SPEC_ARCH-20260325-05-SEPARACION-SOMATOMETRIA-AGUDEZA-EXAMEN.md`, separando ambos estudios en el flujo de Examen Médico. Build del frontend validado con `pnpm build`. Checkpoint generado: `CHK_ARCH-20260325-05.md`. (ARCH-20260325-05)
- **2026-03-26 (CRONISTA):** [✓] **Corte Clínico Consolidado: Historial Clínico Longitudinal y Fallback Inline.** Se completó la implementación del Historial Clínico como maestro longitudinal, reduciendo la recaptura del Examen Médico y agregando un fallback inline cuando no existe snapshot de portal. Validación y commit publicados en `18c340b` (origin/main). Sistema listo para operación. (ARCH-20260326-12)

---

## Cola de ejecución (canónica IDL §7)

> **Nota INTEGRA 2026-08-05:** Esta sección se inicializa con FIX-20260805-04. Las entradas históricas del "Diario de Cambios" arriba usan términos no canónicos (`[✓]`/`[~]`/`[/]`, prohibidos por §15.2); su migración a estados canónicos queda como tarea de auditoría para CRONISTA.

### BACKLOG
- **ID:** FIX-20260809-04-IA-EXAMEN-MEDICO | Prioridad: P3 (diferida por Frank) | Resultado: Resolver HTTP 422 del backend IA al guardar Examen Médico completo | Síntoma: panel "Trazabilidad IA" muestra error `{"detail":[{"type":"missing","loc":["query","study_type"],"msg":"Field required","input":null}]}` (HTTP 422, FastAPI V2) tras `IMPL-20260809-02`. Persistencia OK (datos se guardan); falla la generación de prediagnóstico IA posterior al guardado. | Origen: integración `saveExamenMedicoPapeleta` → endpoint IA V2 con `study_type` faltante en query. Frank instruyó 2026-08-09: "anótalo por ahí en pendientes, aún no lo tocaremos". | Estado: pendiente de análisis. No requiere SPEC todavía.

### READY
- **ID:** ARCH-20260820-01-FASE2B | Prioridad: P1 | Resultado: Cableado visible del editor V3 en `/admin/services/[id]/calibration`. Header/panel muestran estado V3 (draft/tested/published + superseded); `AICalibrationEditor` precarga desde draft V3 o raw V1/V2, guarda `draft`/`tested` vía `saveAICalibrationV3`; nuevo panel invoca `publishAICalibrationV3` con gates visibles (G0-G9, N/A justificado G5/G8/G9); `manual_service` sin editor; fallback V1/V2 intacto; RBAC editar ADMIN+, publicar SUPERADMIN. | SPEC: `context/SPECs/SPEC_ARCH-20260820-01-CALIBRACION-FUENTE-UNICA.md` (v1.1, §14 Fase 2) | ADR: `context/decisions/ADR-20260820-01-CALIBRACION-FUENTE-UNICA.md` (v1.1) | Handoff SOFIA: `context/interconsultas/HANDOFF_ARCH-20260820-01_FASE2B_SOFIA_EDITOR-V3.md` | Referencias funcionales: DEC-20260820-03 (confirmada), DEC-20260820-01/02, BR-20260820-01 | Archivos: editar `page.tsx`, `CalibrationWorkspaceClient.tsx`, `AICalibrationEditor.tsx`; crear `lib/calibration-v3-ui.ts`, `CalibrationV3StatusPanel.tsx`, `__tests__/calibration-v3-ui.test.ts` | AC: AC-2B.1 a AC-2B.11 | Validación: `npm run typecheck` (0), `npm test` (verde), `npm run lint` (0 nuevos), `npm run build` (Vercel) | Delegado a: SOFIA (WIP=1, secuencial) | Estado delegación: READY (SIN delegar) | Restricciones: no tocar `calibration-v3.actions.ts`, `calibration-v3-shared.ts`, `types/calibration.ts`, backend, Events, Prisma/migraciones, `arch-20260819-02-tarjetas-muestra`; no commit/push sin OK Frank | GEMINI: evaluar tras implementación (RBAC + contrato visible en UI)

### IN_PROGRESS
- **ID:** ARCH-20260820-01 (Fase 1) | Prioridad: P1 | Resultado: Clasificador `operationMode` (`manual_service`/`document_extraction`/`clinical_interpretation`) + `CalibrationResolver` como única fuente runtime + adaptador V1/V2→V3 de lectura (sin migración física). No default silencioso a Audiometría; en duda `manual_service`+`requires_review`. | SPEC: `context/SPECs/SPEC_ARCH-20260820-01-CALIBRACION-FUENTE-UNICA.md` (v1.1) | ADR: `context/decisions/ADR-20260820-01-CALIBRACION-FUENTE-UNICA.md` (v1.1) | Handoff SOFIA: `context/interconsultas/HANDOFF_ARCH-20260820-01_SOFIA_CALIBRACION-FUENTE-UNICA.md` (ACTIVE Fase 1) | Referencias funcionales: DEC-20260820-01/02, FND-20260820-01/02/03/04, BR-20260820-01, FIX-20260820-01 | Alcance Fase 1: nuevo `backend/app/services/ai/calibration_resolver.py` + endpoint `GET /api/v1/calibration/resolve` en `backend/app/api/v1/calibration.py` + `backend/tests/test_calibration_resolver.py` | AC: AC-1.1 a AC-1.5 + CA-G01/G02/G19/G20 | Validación: `pytest backend/tests/test_calibration_resolver.py -v` | Delegado a: SOFIA (WIP=1, secuencial) | Estado delegación: en curso | Restricciones: no tocar frontend Events, formularios clínicos, `prediagnostic.py`, Prisma/migraciones, `arch-20260819-02-tarjetas-muestra`; no eliminar hardcodeos; no commit/push sin OK Frank | Fase 2B autorizada por DEC-20260820-03 → READY (Fases 3-7: BLOQUEADAS hasta nueva autorización) | GEMINI: obligatorio tras implementación (toca contrato público `operationMode` + nuevo endpoint)
- **ID:** FIX-20260805-04 | Prioridad: P2 | Resultado: Draft autosave (persistencia navegador) para SelfRegistrationForm | SPEC: `context/SPECs/SPEC_FIX-20260805-04-DRAFT-AUTOSAVE-SELF-REGISTRATION.md` | Delegado a: SOFIA | Estado delegación: en curso | GEMINI: pendiente tras implementación (Qodo sunset)
- **ID:** IMPL-20260809-04 | Prioridad: P1 | Resultado: Selector runtime de proveedor (`gemini` | `m3`) + modelo para la capa de extracción documental, configurable desde `aiCalibration` con override por payload, fallback M3→Gemini y trazabilidad completa. Capa clínica (MedGemma/DR7) intacta. | SPEC: `context/SPECs/SPEC_ARCH-20260809-02-SELECTOR-EXTRACCION-MULTI-PROVEEDOR.md` | ADR: `context/decisions/ADR-20260809-02-EXTRACCION-MULTI-PROVEEDOR-M3-GEMINI.md` | Handoff SOFIA: `context/interconsultas/HANDOFF_ARCH-20260809-02_SOFIA_SELECTOR-EXTRACCION.md` | Commit: `99dc46c` (push `4b901aa..99dc46c main -> main`) | Estado: **DONE** — Gates pytest 84/84, typecheck 0, lint 0 nuevos. GEMINI QA-20260809-04 APPROVED_WITH_OBSERVATIONS (F1 UTF-8 corregido pre-commit; F2 race condition y F3 divergencia modelo documentados como debt consciente). Pendiente operativo: confirmar `M3_BASE_URL` y nombre exacto del modelo contra el plan Pro de MiniMax (ajustable vía env vars sin tocar código).

### VERIFYING

### BLOCKED

### DONE

---

## Definition of Ready
SPEC activa con criterios verificables; dependencias disponibles; sin bloqueantes; validación detectable; alcance dentro de autorizaciones.

## Definition of Done
Criterios aceptados con evidencia; gates aplicables aprobados (typecheck, vitest, lint); revisión SOFIA; GEMINI si no trivial; PROYECTO.md con una sola representación.

## Autorizaciones autónomas vigentes
- **Ninguna** (sesión interactiva con Frank presente). Modo nocturno no activado. Acciones estándar permitidas: edición local, validación local, tests/docs, delegación entre agentes. NO permitido sin OK explícito: commit, push, PR, deploy, producción, datos irreversibles, secretos.












